# 公开类型的私有表示类型修复

> 状态：待实现

## 1. 问题

公开类型可以使用私有类型作为私有成员的类型。该规则与是否泛型无关。

合法示例：

```feng
open module example;

type A<T> {
  func A() {}
}

open type B<T> {
  seal let value: A<T>;

  func B() {
    self.value = A<T>();
  }
}
```

`B<T>.value` 是私有成员，未向外暴露 `A<T>`，因此 `B<int>` 在同模块、跨模块和跨包使用时都必须正常。

当前行为：

| 场景 | 结果 |
|---|---|
| 私有非泛型类型作为公开类型的私有成员类型，同包跨模块使用 | 正常 |
| 私有非泛型类型作为公开类型的私有成员类型，跨包使用 | `CE0032` |
| 私有泛型类型作为公开泛型类型的私有成员类型，同包跨模块使用 | `CE0031` |
| 私有泛型类型作为公开泛型类型的私有成员类型，跨包使用 | `CE0031` |
| 公开成员使用私有类型 | 语义阶段未报可见性不一致 |

## 2. 语言规则

成员签名中使用的类型，其有效可见范围必须覆盖该成员的有效可见范围。

成员的有效可见范围由 module、所属 type 和成员自身的可见性共同决定。

- 私有成员可以使用私有类型。
- 公开成员不能使用更私有的类型。
- 泛型不改变该规则。
- 泛型实参、数组元素和指针目标类型递归适用该规则。
- 字段类型、方法参数与返回类型、构造函数参数和泛型约束统一适用该规则。

非法示例：

```feng
open module example;

type A<T> {
  func A() {}
}

open type B<T> {
  open let value: A<T>;

  func B() {
    self.value = A<T>();
  }
}
```

`value` 对外可见，但 `A<T>` 不可见，必须在语义阶段报告可见性不一致。

本规则统一定义在 `docs/feng-visibility.md`。其他文档只引用，不重复定义。

## 3. 根因

1. 语义分析没有检查公开成员签名与其类型的可见性是否一致。
2. 公开包 `.ft` 包含公开类型的私有字段，但没有包含字段所依赖的私有类型声明，跨包 codegen 无法解析其布局。
3. codegen 使用调用方的源码可见性查找私有表示依赖，导致同包跨模块的私有泛型实例注册失败。

## 4. 实施 TODO

TODO 按顺序实施。每个子项完成后立即标记为 `[x]`；全部子项完成后才能标记父 TODO。每步均可单独提交和 Review；每步必须补齐对应测试用例，测试必须包含 fcts 用例，且全量回归测试 `make test` 通过后，才可标记完成。

- [ ] **TODO 1：增加可见性一致性检查**

  - [ ] 先在 `docs/feng-visibility.md` 定义第 2 节规则，其他文档只引用。
  - [ ] 新增统一的类型可见性检查，递归处理泛型实参、数组元素和指针目标类型。
  - [ ] 检查字段类型、方法参数与返回类型、构造函数参数和泛型约束。
  - [ ] 为可见性不一致分配语义错误码，并更新错误码文档。
  - [ ] 补齐语义用例：私有成员使用私有类型通过；公开成员直接或间接使用私有类型时报语义错误。
  - [ ] 补齐 fcts 用例：验证私有成员使用私有类型的合法行为，覆盖非泛型和泛型。
  - [ ] 运行并通过全量回归测试 `make test`。
  - [ ] 交付验收：非法公开 API 在语义阶段终止，不进入 codegen。

- [ ] **TODO 2：修复同包跨模块的私有泛型依赖解析**

  - [ ] 本地 reifiable 依赖保存目标声明身份。
  - [ ] 泛型替换后保留目标声明身份。
  - [ ] codegen 内部表示依赖按声明身份解析，不使用具化调用方的源码可见性。
  - [ ] 用户源码的普通类型查找保持现有可见性规则。
  - [ ] 补齐编译器用例：验证声明身份在收集、替换和 codegen 注册过程中保持不变。
  - [ ] 补齐 fcts 用例：同包跨模块具化 `B<int>` 时，私有字段类型 `A<int>` 正常注册和使用。
  - [ ] 运行并通过全量回归测试 `make test`。
  - [ ] 交付验收：消除该场景的 `CE0031`，不改变跨包行为。

- [ ] **TODO 3：修复跨包非泛型私有表示类型**

  - [ ] 在公开包 `.ft` 中，从公开类型的全部实例字段递归收集私有表示类型。
  - [ ] 导出 codegen 所需的类型种类、私有标记、实例字段和布局信息。
  - [ ] 只导出公开类型可达的私有表示依赖，不导出无关私有声明和函数体。
  - [ ] 类型引用使用符号身份关联目标声明，避免按名称猜测。
  - [ ] 源码查询仍只返回公开声明。
  - [ ] 编译器内部查询可以按符号身份读取私有表示类型。
  - [ ] 私有表示类型进入内部类型注册表，不进入用户可见符号集合。
  - [ ] codegen 通过内部导入通道解析公开类型私有字段依赖的非泛型类型。
  - [ ] 递归处理私有类型继续依赖其他私有类型的情况。
  - [ ] 处理实例字段通过泛型实参、数组或指针间接依赖私有非泛型类型的情况。
  - [ ] 补齐符号表和 codegen 用例：验证直接、递归和组合依赖的闭包、私有标记、公开查询隔离、内部查询及布局解析。
  - [ ] 补齐 fcts 跨包用例：提供方定义私有 `A` 和公开 `B`，覆盖直接字段、递归字段及通过泛型实参、数组或指针形成的私有表示依赖，消费方构造并使用 `B()` 正常。
  - [ ] 补齐不可访问用例：消费方直接引用 `A` 仍报不可访问。
  - [ ] 运行并通过全量回归测试 `make test`。
  - [ ] 交付验收：消除该场景的 `CE0032`。

- [ ] **TODO 4：修复跨包泛型私有表示类型**

  - [ ] 在 `.ft` 私有表示依赖闭包中包含泛型形参、约束和 reifiable 依赖。
  - [ ] 跨包 reifiable 依赖保存并恢复目标符号身份。
  - [ ] codegen 按私有泛型声明骨架注册 `A<T>` 的具体实例。
  - [ ] 递归处理私有泛型类型继续依赖其他私有泛型类型的情况。
  - [ ] 处理实例字段通过泛型实参、数组或指针间接依赖私有泛型类型的情况。
  - [ ] 补齐符号表和 codegen 用例：验证直接、递归和组合泛型依赖的声明骨架、目标符号身份、导出、导入及具化。
  - [ ] 补齐 fcts 跨包用例：提供方定义私有 `A<T>` 和公开 `B<T>`，覆盖直接字段、递归字段及通过泛型实参、数组或指针形成的私有表示依赖，消费方构造并使用 `B<int>()` 和 `B<string>()` 均正常。
  - [ ] 补齐不可访问用例：消费方直接引用 `A<T>` 仍报不可访问。
  - [ ] 运行并通过全量回归测试 `make test`。
  - [ ] 交付验收：消除跨包场景的 `CE0031`。

## 5. 验收矩阵

| 场景 | 预期 |
|---|---|
| 私有成员使用私有非泛型类型 | 通过 |
| 私有成员使用私有泛型类型 | 通过 |
| 同包跨模块使用上述公开类型 | 通过 |
| 跨包使用上述公开类型 | 通过 |
| 公开成员直接或间接使用私有类型 | 语义错误 |
| 外部源码直接引用私有表示类型 | 不可访问错误 |
| 公开查询或补全查找私有表示类型 | 不可见 |
| 编译器内部按符号身份查找私有表示类型 | 可用 |

## 6. 非目标

- 不把私有表示类型改为公开类型。
- 不导出包内全部私有声明。
- 不修改 runtime ABI。
- 不在本修复中修改 `Map`、迭代器或 `Option<T>`。

本修复只影响语义检查、符号元数据和编译期类型解析，不增加运行时开销。

## 7. 公开成员使用私有类型的可选方案

C# 与 Go 方案的共同底座、行为差异和额外工作见
[公开成员使用私有类型的方案](./feng-private-type-public-api-options.md)。

当前第 2 节和 TODO 1 按 C# 方案编写。若选择 Go 方案，必须先更新对应规则和 TODO，再开始实现。
