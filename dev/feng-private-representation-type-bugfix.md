# 公开类型的私有表示类型修复

> 状态：待实现，已选择 C# 方案

## 1. 问题

公开类型的私有成员可以使用私有类型，泛型不改变该规则：

```feng
open module example;

type Entry<T> {
  func Entry() {}
}

open type Box<T> {
  seal let entry: Entry<T>;

  func Box() {
    self.entry = Entry<T>();
  }
}
```

`Box<T>.entry` 是私有成员，不向外暴露 `Entry<T>`。`Box<int>` 和
`Box<string>` 在同模块、跨模块和跨包使用时都必须正常。

公开签名使用私有类型则形成不可完整使用的 API，应在提供方报可见性不一致：

```feng
type Hidden {}

open func create() -> Hidden;          // 错误
open func consume(value: Hidden);      // 错误
open let values: List<Hidden>;         // 错误
```

当前行为：

| 场景 | 当前结果 |
|---|---|
| 私有非泛型类型作为公开类型的私有成员类型，同包跨模块使用 | 正常 |
| 私有非泛型类型作为公开类型的私有成员类型，跨包使用 | `CE0032` |
| 私有泛型类型作为公开泛型类型的私有成员类型，同包跨模块使用 | `CE0031` |
| 私有泛型类型作为公开泛型类型的私有成员类型，跨包使用 | `CE0031` |
| 公开签名使用私有类型 | 提供方语义阶段通过；消费方可能产生 `AE0306`、`CE0031` 或 `CE0032` |

## 2. 语言规则

### 2.1 可见性一致性

声明签名中每个组成类型的有效可见范围不得小于该声明的有效可见范围。
有效可见范围由 module、所属类型和成员的可见性共同决定。

规则如下：

1. 私有成员可以使用在其声明位置可访问的私有类型。
2. 公开声明不得直接或间接使用有效可见范围更小的类型。
3. 检查顶层绑定和函数，以及类型、`spec`、`fit` 的实例与静态成员。
4. 检查字段类型、参数、返回类型、构造函数参数、泛型约束、父 `spec`、
   `spec` 组成类型和 `fit` 公开签名。
5. 递归检查泛型实参、数组元素、指针目标及 callable 的参数和返回类型。
6. 泛型参数本身不参与可见性比较，其约束类型参与检查。
7. 显式类型和推导形成的公开类型都必须检查；推导类型在推导完成后检查。
8. 可见性不一致在提供方语义阶段报错，不生成公开 `.ft`。

示例：

```feng
type Hidden {}

open let value: Hidden;                 // 错误
open func create() -> Hidden;           // 错误
open func consume(value: Hidden);       // 错误
open func process<T: Hidden>(value: T); // 错误：约束类型不可见
open let values: List<Hidden>;          // 错误：递归检查泛型实参

open type Public {
  seal let hidden: Hidden;              // 合法：私有成员
  let exposed: Hidden;                  // 错误：type 成员默认公开
  static let shared: Hidden;            // 错误：静态成员默认公开
}
```

### 2.2 私有表示依赖

通过可见性检查后，公开 `.ft` 按以下规则收录声明：

1. 公开声明及其签名按现有公开导出规则收录。
2. 公开类型的字段继续携带对象布局和泛型具化所需事实，不受字段可见性影响。
3. 已收录公开类型的字段引用私有泛型类型时，收录该泛型类型的声明骨架和
   codegen 所需事实，不受字段可见性影响。
4. 私有泛型依赖递归包含其类型参数、约束、泛型实参和 reifiable 依赖。
5. 仅被私有成员引用的非泛型私有类型，不因本修复新增顶层类型声明记录。
6. 不相关的私有声明不收录。

收录的私有依赖保持私有，只进入编译器内部声明查询，不进入用户名称查找、
导入和补全。语义分析和 codegen 按声明身份使用这些依赖。

本节规则实施时统一定义在 `docs/feng-visibility.md` 和
`docs/feng-symbol-table.md`；其他正式文档只引用，不重复定义。

## 3. 根因

1. 语义分析未检查公开声明与其组成类型的有效可见范围。
2. 公开 `.ft` 未完整携带私有泛型表示类型的声明骨架和具化依赖。
3. 外部模块语义视图只注册公开顶层声明，编译器内部无法解析已收录的私有依赖。
4. 具名类型和 reifiable 依赖未稳定保留目标声明身份，部分流程仍按名称和调用方
   源码可见性查找。

## 4. 实施 TODO

TODO 按顺序实施。每个子项完成后立即标记为 `[x]`；全部子项完成后才能标记父
TODO。每步均可单独提交和 Review；每步必须补齐对应测试用例，测试必须包含
fcts 用例，且全量回归测试 `make test` 通过后，才可标记完成。

当前代码的阶段边界：

1. 同包编译直接使用源码 AST 和语义分析结果，不读取 `.ft`。
2. codegen 的泛型声明注册和具化逻辑由同包、跨包共用；跨包只增加 `.ft` 到内部
   声明的恢复链路。
3. 可见性一致检查在提供方语义类型收敛后、符号图和 `.ft` 生成前执行。

- [ ] **TODO 1：修复同包私有泛型表示类型具化**

  - [ ] 在 `docs/feng-visibility.md` 明确：私有成员可以使用其声明位置可访问的
        私有类型，泛型不改变该规则。
  - [ ] 为 codegen 建立统一的具名泛型目标解析结果，包含目标声明及其所属
        program/module，不再仅返回按当前调用方可见性匹配的名称。
  - [ ] 源码类型引用按声明所在 program/module 解析私有泛型目标；用户源码中的
        名称访问仍按现有可见性规则解析。
  - [ ] 泛型实参替换、级联实例收集、实例 shell 注册和成员类型解析统一使用
        上述目标声明；调用点 program 不得覆盖声明上下文。
  - [ ] codegen 对字段显式类型和语义推导类型使用同一路径，并递归处理字段类型
        中的泛型实参和级联私有泛型依赖。
  - [ ] 补齐 codegen 用例：同模块及同包跨模块覆盖显式和推导字段类型、直接和
        级联私有泛型依赖，以及 `int`、`string` 具化。
  - [ ] 补齐语义不可访问用例：其他模块直接引用、导入或构造私有类型仍失败。
  - [ ] 补齐 fcts 同包用例：公开泛型类型的私有字段使用私有泛型类型，
        `Box<int>` 和 `Box<string>` 均可构造和使用。
  - [ ] 确认本步骤不修改 `.ft` writer、reader 或 imported-module cache。
  - [ ] 运行并通过全量回归测试 `make test`。
  - [ ] 交付验收：合法同包场景不再产生 `CE0031`，且形成可供 TODO 2 复用的
        唯一 codegen 具化路径。

- [ ] **TODO 2：修复跨包私有表示类型具化**

  - [ ] 在 `docs/feng-symbol-table.md` 定义第 2.2 节的依赖闭包、私有标记、
        `sym_ref` 编码和内部查询规则；`docs/feng-package.md` 只保留引用。
  - [ ] 依赖闭包从公开 `.ft` 已收录类型的全部字段类型及其 reifiable 依赖开始；
        不从未收录的私有方法、构造函数或函数体收集依赖。
  - [ ] 私有泛型声明骨架收录类型参数、约束、父 `spec`、全部字段及 reifiable
        依赖，并递归收集其私有泛型依赖。
  - [ ] 仅被私有字段引用的非泛型私有类型不新增顶层声明记录；字段已有表示事实
        必须足以让消费方 codegen 不再按用户可见名称查找该类型。
  - [ ] 不收录无关私有声明，所有收录的私有依赖保持私有标记。
  - [ ] `FengSymbolTypeView` 的 `NAMED`、`NAMED_GENERIC` 和
        `TYPE_PARAM_REF` 保存目标声明身份；symbol graph 构建时按源码声明映射填写。
  - [ ] `.ft` writer 先确定本次收录闭包并分配全部 symbol id，再序列化类型，
        将目标 symbol id 写入 `sym_ref`。
  - [ ] `.ft` reader 分两阶段创建声明和恢复类型引用，按 `sym_ref` 还原目标声明；
        不把已绑定类型降格为纯名称。
  - [ ] imported-module cache 同时注册公开声明和已收录的私有依赖；内部编译查询
        可以取得两者，用户名称查询、`use` 和补全仍只返回可见声明。
  - [ ] 将恢复出的目标声明接入 TODO 1 的 codegen 具化路径；不得新增独立的
        跨包名称查找或具化分支。
  - [ ] 补齐 symbol 用例：验证最小闭包、私有标记、symbol id 预分配、
        `sym_ref` writer/reader round-trip、递归依赖和无关私有声明排除。
  - [ ] 补齐 semantic/provider 用例：验证内部查询可用且用户查询、直接引用、
        导入和构造私有依赖仍失败。
  - [ ] 补齐 codegen 用例：消费 `.ft` 后覆盖私有非泛型字段、直接和级联私有
        泛型字段，以及 `int`、`string` 具化。
  - [ ] 补齐 fcts 跨包用例：在 `fcts_lib` 定义私有表示类型，在 `fcts_bin`
        构造并使用公开类型的 `int`、`string` 实例。
  - [ ] 运行并通过全量回归测试 `make test`。
  - [ ] 交付验收：合法跨包场景不再产生 `CE0031` 或 `CE0032`，且私有依赖未进入
        用户可见符号集合。

- [ ] **TODO 3：增加公开签名可见性一致检查**

  - [ ] 在 `docs/feng-visibility.md` 完整定义第 2.1 节规则；同步
        `docs/feng-symbol-table.md`、`docs/feng-package.md` 的引用，不重复规则。
  - [ ] 统一表示并计算类型私有、模块私有、包内可见和包外公开四级有效可见范围；
        module、所属类型和成员逐级取最小范围。
  - [ ] 基于已解析的目标声明递归检查组成类型，不以名称或 `.ft` 导出结果代替
        可见性判断。
  - [ ] 在所有声明类型解析和公开绑定类型推导完成后、符号图构建前执行检查。
  - [ ] 覆盖实例与静态字段、顶层绑定、函数、方法、构造函数、泛型约束、
        父 `spec`、`spec` 组成类型和 `fit` 公开签名。
  - [ ] 递归覆盖泛型实参、数组元素、指针目标及 callable 的参数和返回类型；
        泛型参数本身不参与比较，其约束类型参与检查。
  - [ ] 增加可见性不一致诊断，以产生不一致的类型引用为位置，并同步错误码文档。
  - [ ] 补齐 semantic 用例：覆盖显式和推导类型、直接和递归依赖、实例和静态
        成员、顶层声明、泛型约束，以及四级有效可见范围。
  - [ ] 补齐 symbol 用例：语义失败时不生成公开 `.ft`。
  - [ ] 补齐 fcts 正向用例：公开类型的私有成员使用私有非泛型和泛型类型仍可
        在同包及跨包正常具化。
  - [ ] 运行并通过全量回归测试 `make test`。
  - [ ] 交付验收：所有可见性不一致在提供方语义阶段失败；合法私有表示场景和
        TODO 1、TODO 2 的具化行为不回归。

## 5. 验收矩阵

| 场景 | 预期 |
|---|---|
| 私有成员使用私有非泛型类型 | 通过 |
| 私有成员使用私有泛型类型 | 通过 |
| 公开字段直接或间接使用私有类型 | 提供方可见性错误 |
| 公开函数或方法参数、返回类型使用私有类型 | 提供方可见性错误 |
| 公开静态成员或顶层绑定使用私有类型 | 提供方可见性错误 |
| 公开泛型约束使用私有类型 | 提供方可见性错误 |
| 仅被私有成员引用的非泛型私有类型声明 | 不新增到公开 `.ft` |
| 已收录公开类型的字段引用的私有泛型类型 | 进入 `.ft`，保持私有 |
| 不相关的私有声明 | 不进入公开 `.ft` |
| 同包跨模块使用合法公开类型 | 通过 |
| 跨包使用合法公开类型 | 通过 |
| 外部源码直接引用、导入或构造私有类型 | 不可访问错误 |
| 公开查询或补全查找私有类型 | 不可见 |
| 编译器内部按声明身份查找已收录私有依赖 | 可用 |

## 6. 非目标

- 不把私有表示类型改为公开类型。
- 不导出包内全部私有声明。
- 不实现 Rust 的提供方警告和消费方延迟类型隐私错误。
- 不实现 Go 的不可命名类型外部流动。
- 不新增 opaque type 或其他类型隐藏语法。
- 不修改 runtime ABI。
- 不修改 `Map`、迭代器或 `Option<T>`，也不针对特定类型增加特殊处理。

本修复只影响语义分析、符号元数据和编译期类型解析，不增加运行时开销。

## 7. 方案选择

C#、Go 方案的共同底座、行为差异和额外工作见
[公开成员使用私有类型的方案](./feng-private-type-public-api-options.md)。

本修复采用 C# 方案。

## 8. 代码调查

当前实现、临时验证和完整交付复杂度结论见
[私有表示类型方案的代码调查](./feng-private-type-code-investigation.md)。
