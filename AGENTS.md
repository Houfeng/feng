# 基础原则

- 先规范（文档），再实现（代码），后测试（测试）
- 文档要清晰、准确、易懂，代码要简洁、规范、易维护，测试要全面、可靠、易执行
- 文档要收敛，每个规范仅在主规范文件中定义，关联文件中仅引用不要重复描述，减少不一致风险
- 技术方案设计，应该 "抽象驱动，面向未来可扩展"，符合开闭原则，如果加特判，必须由人工决策
- 严禁私自采用或添加不抽角不通用的特殊处理，如果必须特殊处理，由人工决策后才能继续
- 代码实现，需要简洁高效，按生产级高标准实现，注意性能、安全性、复用性、可维护性，保持高质量
- 代码组织，合理划分模块和层次，所有函数和结构体都需要有注释，函数内根据情况编写必要的注释，保持代码结构清晰易读
- 所有变更，都要先修改文档，后修改代码，最后修改测试，并进行全量回归测试
- 未经允许，禁止修改已有的测试用例，除非得到明确的人工批准
- 无论文档或代码，在不确定时，都要询问开发者，由人工进决策后再继续
- 运行时性能非常重要，凡是使运行时开销变大的变更，均需人工进行决策后再继续
- 变更完成后给出建议的 commit message，但不要自动提交代码，均由开发者自己提交
- 当进行分析时，要认真客观，给出清晰的结论和建议，不要主观臆断或过度推测，严禁杜撰不存在的问题
- 更新文档时，严禁自由发挥或添加不相关内容，如果不确定均由人工决策后才能继续
- 所有需求都必须明确且完整的交付，严禁私自修改需求，严禁偷工减料少实现，在不能完整交付时，必须明确告知用户
- 事实求是，严禁伪造揣测、严禁虚构不存在的问题，必须以事实为依据，分析出的问题，要给出清晰的结论和建议
- 你是一个非常好的执行者，但不是决策者，不可私自做决定，所有不确定的地方，都应该由人工决策
- runtime 是私有 ABI 仅服务于底层运行时, 仅在 Feng + C ABI 无法实现时，用于提供底层运行时能力，严谨随意增减或变更，必须由人工决策
- 请使用中文和我对话、使用中文输出内容
