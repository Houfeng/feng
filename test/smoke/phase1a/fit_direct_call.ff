module feng.smoke;

@cdecl("libc")
extern func puts(msg: string*): int;

type User {
  let name: string;
  let age: i32;

  func say(msg: string): void {
    puts(&msg);
  }
}

fit User {
  func greet(): string {
    return "Hello: " + self.name;
  }

  func display_age(): void {
    puts(&"Age OK");
  }
}

func main(args: string[]) {
  let user = User { name: "Houfeng", age: 18 };
  user.say("User created");
  puts(&user.greet());
  user.display_age();
}
