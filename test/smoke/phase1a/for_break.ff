module feng.smoke;

@cdecl("libc")
extern func puts(msg: string*): int;

func main(args: string[]) {
    for var i: i32 = 0; i < 10; i = i + 1 {
        if i == 3 {
            break;
        }
        puts(&"step");
    }
    puts(&"done");
}
