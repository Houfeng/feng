module feng.smoke;

@cdecl("libc")
extern func puts(msg: string*): int;

func classify(n: i32): string {
    let label = if n >= 0 {
        "non-negative";
    } else {
        "negative";
    };
    return label;
}

func main(args: string[]) {
    puts(&classify(7));
    puts(&classify(-3));
}
