mod feng.smoke;

@cdecl("libc")
extern fn puts(msg: string): int;

type Box<T> {
    var value: T;
}

fn main(args: string[]) {
    let box: Box<i32> = Box:<i32>();
    box.value = 7;
    if box.value == 7 {
        puts("generic type constructor ok");
    }
}
