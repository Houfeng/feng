module feng.smoke;

@cdecl("libc")
extern func puts(msg: string*): int;

spec Reader<T> {
    func read(): T;
}

type Box<T> {
    var value: T;
}

fit Box<T>: Reader<T> {
    func read(): T {
        return self.value;
    }
}

func readInt(reader: Reader<int>): int {
    return reader.read();
}

func main(args: string[]) {
    let box: Box<int> = Box<int>();
    box.value = 42;
    if readInt(box) == 42 {
        puts(&"generic fit ok");
    }
}