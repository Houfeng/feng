module feng.smoke.type_field_callable_lambda;

@cdecl("libc")
extern func puts(msg: string*): i32;

spec Reader(): i32;

type Box {
    var n: i32;
    let read: Reader = () -> self.n;
}

func main(args: string[]) {
    let box = Box{n: 7};
    if box.read() == 7 {
        puts(&"member-lambda-ok");
    } else {
        puts(&"member-lambda-fail");
    }
}