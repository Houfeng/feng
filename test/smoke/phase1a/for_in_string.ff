module feng.smoke;

@cdecl("libc")
extern func puts(msg: string*): int;

func main(args: string[]) {
    let words: string[] = ["alpha", "beta", "gamma"];
    for let w in words {
        puts(&w);
    }
    puts(&"done");
}
