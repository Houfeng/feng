module feng.smoke;

@cdecl("libc")
extern func puts(msg: string*): int;

func label_for(score: i32): string {
    let s = if score >= 60 {
        "pass";
    } else {
        "fail";
    };
    return s;
}

func main(args: string[]) {
    puts(&label_for(80));
    puts(&label_for(40));
}
