mod feng.smoke;

@cdecl("libc")
extern fn puts(msg: string*): int;

type Box<T> {
    var value: T;
}

type Holder<T> {
    var inner: Box<T>;
}

fn main(args: string[]) {
    let holder: Holder<i32> = Holder:<i32>();
    holder.inner.value = 11;
    if holder.inner.value == 11 {
        puts(&"generic nested default ok");
    }
}
