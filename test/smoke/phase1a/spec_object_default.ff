module feng.smoke;

@cdecl("libc") extern func puts(msg: string*): i32;

spec Named {
    var name: string;
    func greet(): string;
}

func main(args: string[]) {
    let s: Named;
    puts(&s.name);
    puts(&s.greet());
    s.name = "hi";
    puts(&s.name);
    puts(&s.greet());
}
