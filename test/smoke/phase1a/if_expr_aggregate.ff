module feng.smoke;

@cdecl("libc")
extern func puts(msg: string*): int;

spec Named {
    func greet(): string;
}

type User: Named {
    let name: string;

    func greet(): string {
        return self.name;
    }
}

func pick(flag: bool, left: Named, right: Named): Named {
    return if flag {
        left;
    } else {
        right;
    };
}

func main(args: string[]) {
    let left: Named = User{name: "if-left"};
    let right: Named = User{name: "if-right"};
    puts(&pick(true, left, right).greet());
    puts(&pick(false, left, right).greet());
}