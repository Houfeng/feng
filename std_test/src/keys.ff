module std_test;

import std.collections;

/**
 * An integer key type implementing Hashable<IntKey>,
 * used as the key type in Map and Set tests.
 */
open type IntKey: Hashable<IntKey> {
  let value: i32;

  func hash(): u64 {
    return (u64)self.value;
  }

  func same(other: IntKey): bool {
    return self.value == other.value;
  }
}

fit i32: Hashable<i32> {
  func hash(): u64 {
    return (u64)self;
  }

  func same(other: i32): bool {
    return self == other;
  }
}
