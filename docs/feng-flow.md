# Feng 语言流程控制规范

本文档用于补充 [feng-language.md](./feng-language.md) 中的流程控制概要说明,聚焦 Feng 语言的条件判断、`if` 表达式、循环与控制转移规则。

## 1 流程控制概览

- Feng 提供条件分支、`if` 表达式、循环和结构化异常处理。
- 条件判断支持 `if / else if / else` 形式。
- `if` 支持作为表达式使用,用于条件分支求值。
- 循环支持 `while`、`for`、`break` 和 `continue`。
- `try / catch / finally` 与控制流的详细异常语义见 [Feng 语言异常模型规范](./feng-exception.md)。

## 2 普通条件判断

规则说明:

- `if` 和 `else if` 的条件表达式必须是 `bool`。
- 条件分支按从上到下顺序依次判断,命中第一个为 `true` 的分支后即停止后续判断。
- `else` 分支可省略; 省略时表示条件全部不满足则直接离开整个分支结构。

```feng
if a > b {
    // 逻辑代码
} else if a == b {
    // 逻辑代码
} else {
    // 逻辑代码
}
```

## 3 `if` 表达式

`if` 可作为表达式参与赋值、返回值计算或参数传递。表达式形式的 `if` 会返回被选中分支的值。

规则说明:

- `if` 表达式必须包含 `else` 分支,以保证所有路径都有结果。
- 各分支结果类型必须一致,或当前上下文已提供明确目标 `spec` 类型且各分支结果都通过显式契约关系满足该 `spec`。
- 仅被选中的那个分支会真正执行。

```feng
let label = if age >= 18 {
    "adult"
} else {
    "minor"
};
```

## 4 条件匹配形式

Feng 额外支持以 `if 目标值 { ... }` 形式书写的条件匹配表达式,其本质是按顺序执行的相等性分支匹配。

规则说明:

- 匹配目标表达式先求值一次。
- 每个分支标签必须是可与目标值进行 `==` 比较的常量表达式。
- 分支按书写顺序匹配,命中第一个相等标签后返回对应结果。
- 表达式形式必须包含 `else` 分支。
- 同一条件匹配中不允许出现重复的分支标签; 重复标签的后续分支为永远不可达的死代码,编译期报错。

```feng
let stage = if age {
    0: "婴儿",
    18: "成年",
    60: "老年",
    else: "青年"
};
```

## 5 `while` 循环

- `while` 条件表达式必须为 `bool`。
- 每次进入循环体前都会重新计算条件。
- 条件为 `false` 时立即退出循环。

```feng
var i = 0;
while i < 3 {
    print(i);
    i = i + 1;
}
```

## 6 `for` 循环

Feng 的 `for` 使用三段式循环头:

```feng
for 初始化; 条件; 更新 {
    // 循环体
}
```

规则说明:

- 初始化子句可为空,也可使用局部绑定或赋值语句。
- 条件子句可为空; 为空时表示恒为 `true`。
- 更新子句可为空,通常写赋值或函数调用。
- 初始化子句中声明的绑定仅在当前 `for` 循环内部可见。

```feng
for var i = 0; i < 10; i = i + 1 {
    print(i);
}
```

## 7 `break` 与 `continue`

- `break` 仅可用于 `while` 或 `for` 循环内部,用于立即退出当前最近的一层循环。
- `continue` 仅可用于循环内部,用于跳过当前轮剩余语句并进入下一轮判断。
- `break` 和 `continue` 都不携带返回值。

## 8 与异常模型的关系

- `try / catch / finally` 本身属于控制流结构的一部分。
- `finally` 会在 `return`、`break`、`continue` 和异常展开前执行。
- `throw`、异常传播和 ABI 边界限制的详细规则见 [Feng 语言异常模型规范](./feng-exception.md)。

## 9 与主规范的关系

- [feng-language.md](./feng-language.md): 语言总体规范、流程控制概要、模块、类型、函数、异常、GC、包分发与完整示例。
- [feng-exception.md](./feng-exception.md): `throw`、异常传播与 `try/catch/finally` 的异常语义。
- 本文档: 条件判断、`if` 表达式、模式匹配、循环与控制转移规则的独立补充文档。
