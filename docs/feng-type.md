# FC语言类型系统规范

本文档用于补充 [feng-language.md](./feng-language.md) 中的类型系统概要说明,聚焦 FC 语言的数据类型、类型声明和类型约束细节。

## 1 类型系统概览

- FC 采用强类型、静态类型系统,所有变量、参数和返回值均在编译期完成类型检查。
- 基础值类型覆盖有符号整数、无符号整数、浮点与布尔,并提供 `int` 和 `float` 作为常用别名。
- 统一使用 `type` 定义 FC 原生类型,使用 `extern type` 定义与 C 兼容的结构体或函数指针类型。
- 支持鸭子类型(鸭型),通过对象成员签名或函数签名完成编译期兼容性检查。
- 支持类型推导,但仅限带初始值的变量和可推导返回值的函数。
- 所有类型必须先定义、后引用,禁止嵌套、匿名或内联类型定义。

## 2 基础数据类型

### 2.1 数值与布尔类型

| FC 类型 | 别名 | 对应 C 类型 | 说明 | 默认零值 |
| --- | --- | --- | --- | --- |
| `i8` | - | `int8_t` | 8 位有符号整数 | `0` |
| `i16` | - | `int16_t` | 16 位有符号整数 | `0` |
| `i32` | - | `int32_t` | 32 位有符号整数 | `0` |
| `i64` | `int` | `int64_t` | 64 位有符号整数 | `0` |
| `u8` | - | `uint8_t` | 8 位无符号整数 | `0` |
| `u16` | - | `uint16_t` | 16 位无符号整数 | `0` |
| `u32` | - | `uint32_t` | 32 位无符号整数 | `0` |
| `u64` | - | `uint64_t` | 64 位无符号整数 | `0` |
| `f32` | - | `float` | 单精度浮点数 | `0.0` |
| `f64` | `float` | `double` | 双精度浮点数 | `0.0` |
| `bool` | - | `bool` | 布尔类型,值为 `true` / `false` | `false` |

### 2.2 字符串类型

- `string` 是 GC 托管字符串类型,默认零值为空字符串 `""`。
- `string` 不可用于 C 互操作。

### 2.3 数组类型

- 一维数组使用 `T[]` 语法,默认零值为空数组 `[]`。
- 多维数组沿用 `T[][]`、`T[][][]` 形式递进定义。
- 动态数组由 GC 托管,不可用于 C 互操作。

### 2.4 C互操作指针类型

- `*T` 对应 C 的 `T*`,默认零值为空指针。
- 指针类型仅用于 C 互操作。
- `extern type`、`*T` 指针和 C 侧分配的内存不受 GC 管理。

补充说明:

- `string`、动态数组和 FC 原生 `type` 对象由 GC 托管。

## 3 变量声明与初始化

- 有初始值: 可省略类型,编译器自动推导,变量赋值为初始值。
- 无初始值: 必须显式标注类型,编译器自动赋予对应类型默认零值。
- `void` 不参与变量初始化规则,不可作为变量、成员、数组元素或普通值使用。
- 禁止使用未初始化的野值,全程保障类型安全。

```fc
// 有初始值,自动推导类型
let num = 10;
let str = "FC 语言";

// 无初始值,显式声明类型,自动赋零值
let age: int;
var p: Point; // C 兼容结构体自动零值初始化
```

## 4 FC原生自定义类型

统一使用 `type` 定义 FC 内部对象类型,支持成员变量、构造函数、成员方法和访问控制,不可与 C 直接互通。

规则说明:

- 成员必须显式添加 `var` 或 `let` 修饰,可通过 `pu` 和 `pr` 设置访问权限。
- 构造函数与类型同名,无返回值,无需 `new` 关键字即可实例化。
- 支持使用 `self` 引用自身,支持成员方法封装。

```fc
type User {
    pu var name: string;
    pr let id: int;
    var age: int;

    // 构造函数
    fn User(name: string, id: int, age: int) {
        self.name = name;
        self.id = id;
        self.age = age;
    }

    // 成员方法
    pu fn info() {
        return self.name;
    }
}
```

## 5 FC原生函数类型

使用 `type` 定义 FC 内部函数类型,仅用于 FC 内部,不可与 C 互通,禁止内联嵌套定义。函数类型始终通过 `type 名称(参数): 返回值` 形式声明; `Lambda` 不是单独的函数类型,只是函数实现或函数字面量的一种写法,表达式语法中使用 `->`。

```fc
// FC 内部函数类型
type IntToInt(x: int): int;
type StringHandle(s: string): bool;
```

规则说明:

- 函数类型必须先定义,再作为参数、返回值或变量类型使用。
- `Lambda` 赋值、返回或传参时,其本质仍需满足目标函数类型签名。
- 函数类型同样参与鸭型兼容性检查,只要参数列表和返回值签名一致即可兼容。
- FC 原生函数类型不可作为 C 函数指针传递。
- `extern type` 定义的函数类型仅用于 C ABI 兼容回调。

## 6 鸭子类型(鸭型)规则

- 遵循“只要像鸭子一样行走、鸣叫,就是鸭子”的设计逻辑。
- 类型无需显式实现接口或继承,对象类型只要拥有相同名称和签名的成员方法即可兼容,函数类型只要参数列表和返回值签名一致即可兼容。
- 编译期完成鸭型兼容性检查,保障运行期安全。

对象类型示例:

```fc
type Duck {
    fn quack(): string {
        return "quack";
    }
}

type Bird {
    fn quack(): string {
        return "bird quack";
    }
}

// 兼容所有包含 quack() 方法的类型
fn make_sound(obj) {
    print(obj.quack());
}
```

函数类型示例:

```fc
type IntHandler(x: int): int;
type NumberMapper(v: int): int;

fn apply(handler: IntHandler, value: int): int {
    return handler(value);
}

fn run(mapper: NumberMapper, value: int): int {
    // NumberMapper 与 IntHandler 签名一致,因此可直接兼容
    return apply(mapper, value);
}
```

## 7 类型相关核心约束

- 强类型、静态类型,无隐式类型转换。
- 无 `any` 类型,未初始化变量自动赋零值。
- 变量有初始值可自动推导类型。
- 参数必须显式标注类型,默认只读。
- `void` 代表空无类型,无返回值函数可省略 `void`,有返回值时可自动推导。
- 所有类型先定义后引用,禁止嵌套匿名类型。
- 数组使用 `T[]`,支持多维数组,动态数组由 GC 托管。
- 函数类型定义统一使用 `type ... : 返回值` 形式,`->` 仅用于 `Lambda` 表达式语法,不引入新的函数类型。
- 鸭型规则同时适用于对象类型和函数类型。
- 无冗余关键字,无结构体专用关键字,统一 `type` 体系。
- 成员必须使用 `var` 或 `let` 修饰,支持 `pu` 和 `pr` 访问控制。

## 8 与主规范的关系

- [feng-language.md](./feng-language.md): 语言总体规范、模块、函数、流程控制、异常、GC、包分发与完整示例。
- 本文档: 类型系统细化规则,作为主规范的独立补充文档维护。