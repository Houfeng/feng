# Feng callable 异常集合分析与 N08 后续优化方案

> **状态**：2026-09-21，阶段一已交付并由人工提交；阶段二实现、定向验证及沙箱外完整 `make test` 已完成，等待人工 Review，未自动提交。既有 release 异常展开问题 S11 按人工决定独立处理；P25 的性能脚本 stderr 改动已获人工确认保留。
>
> **人工决定**：先完成[嵌套 catch 生命周期修复](./feng-nested-catch-exception-lifetime-bugfix.md)；
> N08 独立跟踪，触发用例完整保留并注释。后续记录函数、方法等 callable 可能向外
> 传播的异常，为 N08 的处理及异常信息提示提供依据。本次不要求普通调用必须显式处理异常。
> 阶段一先交付异常摘要与 LSP 提示；阶段二再使用摘要处理 defer 异常逃逸。
> 两个阶段各自补齐覆盖、完成定向验证及沙箱外 `make test`，分别交付。

## 1 范围与规范归属

本任务按顺序完成两个目标：先建立可跨调用、跨包使用的异常集合分析并接入 LSP；
再基于该信息解决 N08 的 defer 异常逃逸问题。本文记录事实、设计、实施问题及验证结果，
语言规则集中于主规范，分阶段实施与验收见 §5。

异常传播及具体类型匹配以[异常规范](../specifications/feng-exception.md)为准；
清理块限制以 [defer 规范](../specifications/feng-defer.md)为准。
两个阶段已审定的规则均归入对应主规范，本文只引用规范结论；阶段二完成交付回归后等待人工代码 Review。

2026-09-21 人工确认：本次不新增普通调用必须捕获异常或显式声明继续传播的要求。
未被当前函数处理的异常沿用[异常规范 §1](../specifications/feng-exception.md#1-异常模型概览)
的自动向上传播规则。记录异常摘要及提供 LSP 提示不改变该行为；既有 ABI 边界约束
仍遵循主规范，N08 涉及的 defer 清理边界按 §4.3 的严格检查原则在阶段二落实。

## 2 N08：展开期间 defer 再次抛出且逃逸

### 2.1 复现形态

以下展示阶段二启用边界检查前的行为;阶段二应在编译期拒绝此源码。

```feng
module n08_example;

/** 用 code 区分原异常与清理异常。 */
type Error { let code: int; }

/** 向调用方抛出独立异常。 */
func fail(code: int): void { throw Error { code: code }; }

/** 原异常退出函数时，defer 调用再次抛出且没有内部处理。 */
func work(): void {
    defer { fail(2); }
    fail(1);
}

/** 当前实现会在进入该 catch 之前因清理链损坏而终止。 */
func main(args: string[]): void {
    try work() catch error: Error {
        // 这里最终应如何处理，取决于后续审定的 defer 异常逃逸规则。
    }
}
```

异常 A（`code == 1`）触发 `work()` 的栈展开；清理过程中执行 defer，异常 B
（`code == 2`）又启动一次向外展开。当前 runtime 在 personality 回调中执行函数
清理，因此第二次 throw 时，第一次 throw 及其 unwinder 仍在物理调用栈上。

LLDB 确认的关键调用顺序为：

```text
feng_throw(B)
  → fail(2)
  → defer 生成函数
  → __feng_personality_v0
  → unwind_phase2 / _Unwind_RaiseException(A)
  → feng_throw(A) / fail(1)
  → work()
  → 调用方
```

现有 `feng_cleanup_release_to_frame_marker` 按清理链顺序移除一个 function marker，
没有记录哪些 native frame 已在第一次展开中清理。第二次展开再次经过这些帧时，
会错误移除外层 try 边界，最终报告边界不在清理链上。仅忽略缺失边界不能修复资源
归属，也没有解决被中断的原异常记录应由谁持有和释放的问题。

### 2.2 已确认是固有缺陷

2026-09-21，Darwin arm64，使用 `85806657` 的原 `codegen.c`、`feng_exception.c`
及 runtime 头文件重建基线，复用其他未修改目标文件。由原 codegen 直接生成 C，
没有手工替换 landing pad；旧产物使用 `FengFrameMarker`、
`feng_release_unwind_exception` 和 `g_current_unwind`。

| 版本 | 同一源程序的实际结果 |
| --- | --- |
| 嵌套 catch 修复前 | `feng_frame_release_to: marker is not on cleanup chain` |
| 嵌套 catch 修复后 | `feng cleanup: boundary is not on cleanup chain` |

两者均未进入预期 catch，确认 N08 是补充用例时发现的固有缺陷。
基线源码和产物核查时位于 `build/n08-baseline/`；全量测试会清理该目录，正式复现
内容保留在 §2.1 及下述 FCTS 中。

早期仅替换旧 runtime 并手工恢复 landing pad 的探索探针曾得到退出码 137、无正常
输出；该观察不用于判断具体根因，以以上直接发码对照和 LLDB 调用栈为准。
调试日志保存在工程根目录 `nested-exception-escape-debug.log`。

### 2.3 用例保留与本次交付边界

在 [`test_nested_exception_lifetime.ff`](../../fcts/fcts_bin/src/test_nested_exception_lifetime.ff)
完整注释保留以下内容，不删除源码或断言：

- `nestedExceptionEscapingFunctionCleanup`：触发 N08 的专属辅助函数。
- `escaping function-unwind defer releases its abandoned exception`：相应行为用例，
  包含原异常析构、替换异常身份及最终析构次数检查。

上述断言是发现问题时用于验证“新异常替换旧异常”的候选预期，尚未成为已批准规则。
阶段二按 §4.3 的禁止逃逸方案增加相应的编译期诊断用例，原注释源码和候选断言
作为问题追溯记录保留，不作为新的运行时行为要求。

以下已通过场景继续作为嵌套 catch 修复的活动用例：defer 调用内部捕获错误并返回；
landing pad 清理期间处理另一异常；catch 的 defer 处理内层异常后继续读取原绑定。
原 landing pad 中的清理异常向外传播场景按阶段二获批的 M03 迁移,完整注释保留并
增加编译期负例。它不触发本节所述整函数二次展开缺陷,但同样受新的 defer 边界限制。

## 3 实施前的异常分析基础及缺口

以下记录阶段一开始时的代码状态；这些旧布尔分析入口已由 §5.0 的统一摘要替换。

| 位置 | 已核实实现 | 后续需要处理 |
| --- | --- | --- |
| `analyzer.c` 的 `CallableExceptionEscapeCacheEntry` | 保存 callable 与 `bool escapes`，通过迭代分析传播变化，供 `@abi` 边界检查使用 | 将布尔结果发展为可表达可能异常类型的摘要 |
| `callable_may_escape_exception` | 无函数体或无缓存时返回 false | 无信息不能直接当作已证明不抛错；跨包摘要和未知情况需要明确定义 |
| `resolve_try_expr` | 有 catch 子句即提升 `exception_capture_depth` | 集合分析需按实际覆盖范围扣除已处理异常，并计入 catch 本身逃逸的异常 |
| callable 值分析 | 可以追踪部分已知函数来源和 Lambda；无法唯一解析目标时返回 false | 动态 callable、spec 方法和泛型约束调用需要契约或保守摘要 |
| symbol / FT | callable 导出参数、返回类型及既有声明属性，未携带异常集合 | 导出、读取、重建及缓存需要保留异常信息 |

代码依据：[semantic analyzer](../../src/semantic/analyzer.c)、
[symbol 数据结构](../../src/symbol/internal.h)、[FT 编写](../../src/symbol/ft_write.c)
及 [FT 格式](../../src/symbol/ft_internal.h)。这些现有机制可作为基础，不能直接当作
完整的无异常逃逸证明。

## 4 设计方向与审定状态

### 4.1 记录 callable 可能向外传播的异常

分析对象是未被 callable 内部完整处理的异常。函数内部出现 throw，但该异常已被
内部 catch 完整处理，则不计入该 callable 的对外异常摘要。

后续摘要应能表达已知的可能异常类型、依赖泛参的异常信息及无法静态确定的部分。
已证明没有逃逸异常与尚无足够信息需要明确区分；具体数据结构由后续方案确定。

2026-09-21 人工确认以下收集与传播规则：

- 直接 `throw` 的已知异常按类型记录：具体类型保留精确身份，开放泛参保留声明身份，并按 §4.1.1 在调用点代入。
- 普通分支不依据条件真假裁剪异常信息，各分支的可能异常取并集，并按类型身份去重；本阶段不引入普通值条件的路径推导。
- 内部调用合并被调用者在该调用点的对外异常摘要，包括代入后的已知类型、仍开放的依赖和未知部分；不能将无法确定的调用视为无异常。
- 每个 `try/catch` 只从自身保护范围的摘要中扣除实际被其 catch 处理的异常，再合并相应处理代码可能向外传播的异常。捕获覆盖仍按异常类型及子句顺序分析，不能把普通分支取并集的规则用于忽略 catch 的类型匹配。
- 存在兜底 catch 时，保护范围传入的异常全部被捕获，包括已知类型、开放依赖和未知部分；这些原异常不再直接向外传播。各 catch 处理代码自身产生的异常仍须独立计算。
- catch 中的调用、再次 `throw expr` 和匿名重抛都要计入处理代码的摘要。`throw expr` 按表达式结果类型记录，不能自动恢复全部原异常；匿名 `throw;` 则使用当前 catch 实际接收的摘要，即排除前置具体 catch 已匹配部分后的剩余异常，可能包含已知类型、开放依赖或未知部分。

因此，单个 `try/catch` 的对外摘要是“保护范围内未被捕获的异常”与“可能执行的
catch 处理代码向外传播的异常”的并集。不能在汇总整个函数后统一减去所有 catch
类型：同类型的异常在 catch 中再次抛出时，仍需记录；同组其他 catch 不会处理该
异常，外围 `try/catch` 则继续按其保护范围分析。处理代码自身的嵌套捕获也按同一
规则计算。

以下假设保护范围的异常摘要为 `{A, B}`，处理代码没有表中未列出的异常来源：

| 捕获与再次抛出方式 | 该 `try/catch` 的对外摘要 |
| --- | --- |
| 兜底 catch 完整处理，不再抛出 | 空集合 |
| 兜底 catch 执行匿名 `throw;` | `{A, B}` |
| `catch ex: A` 执行 `throw ex;`，后续兜底 catch 完整处理其余异常 | `{A}` |
| 先由具体 catch 完整处理 A，后续兜底 catch 执行匿名 `throw;` | `{B}`，不重新计入 A |
| 兜底 catch 仅抛出结果类型为 C 的表达式 | `{C}`，不恢复 A 或 B |

同理，兜底 catch 处理未知来源后不再产生异常，摘要可以为空；匿名重抛则须重新
计入它接收的未知部分。若匿名重抛位于普通条件分支中，按已确认的分支并集规则，
仍记录该 catch 可能重抛其整个接收摘要，不能仅凭值条件将其缩成某个具体类型。

分析需要覆盖直接 throw、匿名重抛、调用链、递归、catch 内再次抛出、defer 调用，
以及函数、方法、构造函数、Lambda 等 callable 边界。异常身份和 catch 覆盖范围
复用主规范，避免另建一套类型匹配规则。

#### 4.1.1 泛型异常摘要与调用点替换

阶段一必须正确记录开放泛参，并在调用点按已经解析的类型实参代入异常摘要。
泛参引用需要保留所属声明及参数身份；仅保存名称 `T` 不足以区分不同声明中的同名
泛参，也不能将类型级与函数／方法级参数混为一个绑定域。

声明处保留参数化摘要，调用处取得替换后的摘要；调用处仍然开放时，继续映射到
调用者的泛参，直到具体使用点闭合。以下为摘要示意，不是新增 `throws` 语法：

| 场景 | 必须取得的摘要 |
| --- | --- |
| `raise<T: throw>` 直接抛出 T 的值 | `{T}`，其中 T 属于 `raise` 的声明 |
| `relay<U: throw>` 调用 `raise<U>` | `{U}`，已转换为调用者 `relay` 的参数身份 |
| 调用 `relay<MyError>` | `{MyError}`，LSP 与调用者分析均使用该具体身份 |
| 方法摘要同时依赖 owner 参数 T 和方法参数 U | 同时使用接收者类型实参与方法实参替换，不能遗漏任一绑定域 |

显式实参与推导实参必须得到一致结果。同一共享体可以在不同调用点产生不同的
异常摘要；替换不得修改声明处的通用摘要或污染其他调用点。开放泛参是有身份的
符号信息，不能仅因尚未闭合就退化成未知；能由已解析实参确定的类型必须准确还原。
该过程属于编译期分析，不需要为异常提示单态化函数体或读取运行时描述符。

捕获覆盖也可能依赖代入结果。例如泛型函数可能接收到类型 T 的异常，在内部仅
捕获 `IoError`，且处理代码不再抛出：T 为 `IoError` 时逃逸集合为空，T 为
`StateError` 时仍有 `{StateError}`。摘要必须保留这类依赖，待替换后正确求值；
不能在开放阶段丢掉捕获条件，之后只做类型名称替换。条件的具体表示及归约方式
在阶段一方案中审定，并与 FT 编码一并设计。

现有 [语义替换实现](../../src/semantic/analyzer.c) 的
`callable_type_bindings_init` / `substitute_type_ref_for_callable_instance`
已经联合处理 owner 与 callable 参数，可作为复用基础；这不表示异常摘要已经实现。

#### 4.1.2 摘要模型与实现 Review 范围

§4.1 的收集与传播规则已经确认。阶段一使用“声明处的参数化异常摘要 + 调用点代入
求值”实现这些规则，具体落地见 §5.0，FT 格式见符号表主规范；实现与测试完成后
一并交付人工 Review，不增加用户必须填写的语法。

**声明摘要与调用点结果分开保存。** 声明摘要引用真实类型、泛参和 callable 身份，
保持只读；调用点使用 owner／callable 实参及已知调用目标求值，得到具体类型集合、
仍然开放的依赖及未知部分。已证明为空可作为统一查询结果，不再独立维护另一套
`mayThrow` 事实。编译过程中的“尚未完成分析”须与最终的“合法但静态未知”分开，
不能在递归尚未收敛时作出无异常结论或发布最终提示。

**摘要保留必要的结构。** 建议至少支持以下含义；它们是编译期表示，不是 Feng 语法：

| 摘要组成 | 作用 |
| --- | --- |
| 空集合 | 已证明没有异常逃逸 |
| 类型项 | 具体异常类型或带声明身份的开放类型表达式 |
| 合并 | 汇总可能执行路径和调用产生的异常 |
| 调用依赖 | 记录被调用目标及其参数绑定，或对 callable 参数／spec 方法的调用依赖 |
| 捕获类型筛选 | 按精确类型和子句顺序得到被捕获及剩余异常；仅在类型匹配可能进入相应 catch 时计入其处理代码的异常 |
| 未知项 | 表示无法静态确定的异常，保留结构化的捕获排除信息；不保存原因文本，不能冒充空集合 |

例如，原异常类型为 T，仅捕获 `IoError`，且该 catch 改为抛出 `LogError`。
代入 T 为 `IoError` 后，逃逸集合应为 `{LogError}`；代入 `StateError` 后，应为
`{StateError}`。摘要既要保留“移除已捕获异常”的条件，也要保留“何时执行 catch
处理代码”的条件。这里的条件仅指捕获的类型匹配关系；普通分支仍按 §4.1 取并集，
不按值条件真假裁剪。捕获类型条件在代入后能判定时应归约，不把上述两种调用结果
都无条件记录成 `{T, LogError}`。

**间接调用与 callable 值分开建模。** 能解析具体目标时使用该目标摘要；目标由
callable 参数或泛型约束决定时，保留相应依赖，在调用点能继续解析时求值；动态
目标无法确定时保留未知。创建、传递或返回 callable 值，与执行该 callable 是
不同事件：工厂返回一个会抛异常的闭包，不等于工厂本次调用会抛出闭包内的异常。
返回值或捕获环境中需要继续传递的调用依赖，必须纳入跨包摘要设计。

**递归使用依赖图和固定点求解。** 以反向调用依赖和工作队列传播变化，复用规范化
摘要节点，避免逐调用展开整条链或每次重新扫描全项目。
参数化递归的终止与有限化实现见 §5.0；需要扩大为保守上界时保留结构化 unknown，
不能把达到迭代上限当成已证明安全，也不能将本可确定的普通泛型调用随意降为未知。

**诊断保留来源，格式化与证明共用结果。** 同一份求值结果供 FT、LSP、`@abi` 和
后续 defer 检查使用；集合按身份去重，来源位置作为相关信息保留。这样既能显示
可能的类型，也能解释“通过哪次调用传播”或“为何仍无法证明为空”。

### 4.2 跨包 FT 与间接调用

可跨包调用的 callable 需要导出异常摘要；私有辅助 callable 对外部的影响汇总到
导出的调用者，不要求为了该分析额外公开私有实现。

FT 导入方在看不到源码时仍需取得摘要。缺失或无法确定的信息应在分析模型中保留，
不能解释为“没有异常”。具体字段、兼容校验、缓存失效及重编要求见
[符号表规范](../specifications/feng-symbol-table.md#异常摘要元数据)。此前嵌套 catch
修复未改 FT；本阶段增加编译期摘要节，沿用原版本并要求重编。

callable-form spec、object-form spec 方法及泛型约束调用，需要明确声明契约、
实现方法和动态实际目标之间的异常信息关系。仅给每个具体函数增加一个标志不能
覆盖这些调用入口。

阶段一的跨包支持是交付必需项，不能留到阶段二：

- 生产包导出完整的参数化异常摘要，包括具体类型身份、泛参所属关系、必要的捕获条件及未知信息；消费包导入后按 §4.1.1 继续替换和分析。
- 泛参及具体异常必须按声明身份恢复，覆盖 owner／callable 参数、多模块同名类型、导入别名和多层依赖转传，不依赖字符串短名猜测。
- 消费者在依赖源码不可见、仅有正常发布包产物时仍须得到与源码分析一致的摘要和 LSP 提示；不能以读取依赖源码弥补 FT 缺失的信息。
- 缺失、不兼容、损坏或过期摘要必须按审定方案诊断、失效或保留未知，不能解释为空集合；正常构建的完整摘要不得无故降级为未知。
- 验证发布用 FT 与 workspace 缓存路径；LSP 对本地缓存的可选性仍遵循 [LSP 已交付方案](./feng-lsp-delivered.md)，不要求用户先手动构建当前项目才能获得提示。

现有 [FT 类型记录](../../src/symbol/ft_internal.h) 的 `TYPE_PARAM_REF` 含泛参
符号引用，[symbol 类型视图](../../src/symbol/internal.h) 也保存 `target_decl`。
这些是跨包恢复身份的已有基础；仍需增加异常摘要自身的读写、恢复与完整性验证。

对应 §4.1.2，增加专门的异常摘要记录，导出根引用既有符号 ID，名称复用 STRS。
类型经普通解析器规范化为模块限定名称、泛型元数及实参树，泛参由摘要声明 ID 与槽位标识。
保存摘要节点、调用依赖、泛参绑定和捕获条件，消费端使用相同求值器。摘要引用的私有依赖
须保持原可见性，不因进入元数据就成为公开 API。FT 版本沿用此前确认的不升级、
重编方式；记录格式、必需信息校验及旧产物失效处理见符号表主规范。

### 4.3 用于 N08 处理及异常信息提示

2026-09-21 人工确认以下提示方式和严格边界检查原则。语言规则的正式变更仍按
各阶段 Todo 更新到对应主规范，本文记录决策及实施边界。

#### 4.3.1 LSP 提示方式（已确认，阶段一）

当前展示规则以[异常主规范 §6](../specifications/feng-exception.md#6-编译期异常摘要)为准：
按 P31 的人工 Review 仅保留 Hover 的异常类型列表，移除原调用处 Information。
Hover 与严格边界错误消费同一份元信息，展示策略不影响摘要或强检查。

将来是否增加强制显式处理要求属于独立后续议题，不纳入本次实施；若启用，应作为
编译错误检查，不以 Information 或 warning 代替强制要求。

#### 4.3.2 严格边界的证明要求（已确认，分阶段实施）

只有能够证明相应边界的逃逸异常集合为空，才允许通过；存在已知异常、未能消除
的开放条件或未知部分，均不能视为已证明安全。判断必须在摘要求解和内部捕获处理
完成后进行，不能把调用前的摘要直接当成边界最终结果。

| 边界 | 检查范围 | 无法证明为空时 |
| --- | --- | --- |
| 普通 callable | 对外传播信息，不增加必须处理要求 | 按 §4.3.1 提示，允许继续传播 |
| `@abi` callable，阶段一 | 该 callable 向 C ABI 边界逃逸的异常 | 编译期报错，LSP 显示同一错误 |
| defer，阶段二 | defer 块自身向外逃逸的异常 | 编译期报错，LSP 显示同一错误 |

未知来源可以由边界内部的兜底 catch 完整处理；如果处理代码本身也没有逃逸异常，
最终集合可以为空，因此“调用目标未知”不等于该边界必然报错。相反，只捕获部分
类型、处理代码再次传播异常，或仅由边界外的 catch 处理，都不能证明该边界安全。
现有 defer 直接 `throw` 语句禁令按主规范保留，不因增加摘要分析而放宽。

阶段一迁移 `@abi` 检查时，应记录新增诊断及其依据；这可能使过去因分析不完整而
接受的代码被拒绝，不能通过将未知当作不抛异常来规避。C ABI `extern func` 等已有
契约明确的入口仍按主规范分类，不能仅因缺少函数体而一律归为未知。

异常摘要与 LSP 提示属于阶段一；defer 异常逃逸检查属于阶段二。阶段一保持现行
defer 规则，N08 用例继续完整注释保留，阶段一的全量回归通过不表示 N08 已修复。

优先在编译期完成分析及信息传递。若 N08 的最终方案仍需要 runtime／ABI 修改或
增加运行时开销，需先列明具体协议与成本，单独通过人工 Review。

## 5 分阶段实施 Todo

阶段一已于 2026-09-21 获准实施。以下技术落地及问题记录属于本次实施，完成后随代码和
验证结果一并提交人工 Review；不改变已确认的语言规则。

### 5.0 阶段一技术落地与问题记录

- 在现有类型和调用解析完成后构建统一摘要，移除旧布尔缓存对 `@abi` 的最终判定职责。
  严格边界诊断在摘要求解完成后生成；LSP 读取已完成的异常事实，避免中间迭代状态泄露。
- 提示的输出范围和层次边界遵循[异常主规范 §6](../specifications/feng-exception.md#6-编译期异常摘要)。
  Semantic 只提供声明摘要、调用本身及经外围 catch 筛选后的异常事实，不生成异常传播 Information。
  LSP 读取事实后仅在 Hover 展示类型；CLI 不执行提示判断。保留原有 orphan fit 等
  通用信息及原有前端回调接口，不增加 editor 标记或提示订阅开关。
- 摘要使用不可变的类型项、并集、捕获筛选、非空条件、调用及 callable 值依赖组成的图。
  类型参数和 callable 参数均使用所属声明内的槽位；捕获的值与 Lambda 自身参数保持独立。
  普通分支仅合并，catch 条件则保留到实参替换后求值。
- 声明摘要、调用点结果和导入摘要使用同一查询模型。调用点绑定属于查询上下文，不改写
  声明图；创建或返回闭包的结果依赖与该调用本身的异常分开记录。
- FT 仅保存摘要及其必要依赖，不保存原函数体、局部变量或通用控制流图。新摘要是必需的
  编译事实，缺失或损坏的发布产物应诊断并要求重编；失效的本地缓存沿用源码重建路径。
  FT 版本不升级，运行时及 ABI 不变。
- 递归实例按模板及绑定驻留，以反向依赖工作队列收敛；沿创建依赖链再次遇到同一模板时，
  若某个参数严格包含此前该槽位的参数（包括扩大后的并集），仅将该增长槽位扩大为
  unknown。已经扩大后的类型组合保持 unknown，防止再次无限嵌套。普通实例之间不做
  此扩大，也不设“达到次数后当作空集合”的退出路径。无异常的递归仍可得到空摘要；
  真正依赖增长槽位抛出或分发的路径保留未知，并继续受 catch 过滤和严格边界约束。
  对递归返回的 callable 环境同样检查结构增长：只有已确认的调用依赖环，且新返回
  环境嵌套旧返回值时，才扩大该返回值为 unknown；普通分支增加有限候选目标
  不触发扩大。未知返回值不意味着工厂调用本身抛出，后续调用该值时才贡献未知异常。
- 实施问题按“现象／证据 → 分析 → 处理 → 验证”在本节追加；涉及新的行为取舍时由人工决定。

本阶段“不变更 ABI”指生成程序与 runtime 的协议，不表示编译器内部 API 没有变化。
以下接口调整属于当前实现，供人工 Review：

| 内部接口 | 用途与范围 |
| --- | --- |
| Semantic 的异常摘要构建／查询接口及 `FengSemanticAnalysis.exception_analysis` | 保存、求解与读取编译期摘要；普通 callable 也参与摘要计算，但不新增必须捕获异常的要求 |
| `FengSemanticModule.exception_metadata_user`／`get_exception_template` | 从导入包获取摘要，支持无源码的跨包传播 |
| Symbol 摘要查询及 FT 摘要编解码接口 | 为缓存、跨包分析和 LSP 提供持久化信息；FT 版本号不变 |
| `FengExceptionCallFacts`、`feng_semantic_exception_call_facts`／`call_count`／`call_at` | 按表达式或序号提供位置、调用异常与经过外围 catch 筛选的异常集合；数据不携带受众、诊断级别或提示文本，LSP 自行决定如何展示 |
| `feng_semantic_validate_abi_exception_effects` | 在元信息收集完成后单独检查 `@abi` 边界，失败时构造 AE1313；普通 callable 不执行必须捕获检查 |

以上具体接口设计由实施时选择，未逐项单独确认；它们不应被表述为“内部接口未变”。
除摘要展示外，LSP 还修复了 P26 的成功分析后诊断发布条件。标准库线程入口的执行路径
变更及批准范围见 P16；runtime、codegen、lexer 和 parser 文件没有本阶段改动。

| 编号 | 现象及证据 | 分析与处理 | 验证 |
| --- | --- | --- | --- |
| P01 | `resolve_try_expr` 以捕获深度屏蔽整个保护表达式的异常，具体 catch 未按类型过滤 | 用统一摘要图替换捕获深度对异常逃逸的判断；保留已有语法和类型检查 | 精确／兜底／重抛及嵌套组合的定向语义用例通过 |
| P02 | `callable_may_escape_exception` 对无函数体／无缓存返回 false；间接值解析超过深度或找不到目标也返回 false | 将导入摘要、已证明为空、未知来源分开；严格边界消费最终结果 | 间接调用、两个 FT profile 和 `@abi` 定向用例通过 |
| P03 | 首次定向构建发现成员名位于成员联合体内；resolver 持有的 analysis 为 const 查询视图 | 按已有 sidecar 记录接口适配 const 视图，区分字段名与 callable 名称 | 定向构建已通过 |
| P04 | 现有导入查询测试使用位置初始化；直接扩展查询结构使 `-Wmissing-field-initializers` 报错 | 将异常摘要查询作为导入模块自身的中性元数据入口，保持顶层查询接口不变 | 既有 semantic / symbol 测试已通过 |
| P05 | 新增 `fit Box<T>` 调用测试显示 `Box<string>` 的方法摘要仍为 `T`；摘要注册尚未取得 nominal fit 的 owner 泛参 | 复用现有 fit 目标解析，按目标声明建立 owner 槽位 | 修复并增加源代码／FT 组合验证 |
| P06 | 检查后处理入口发现对象字面量未持久化目标类型，for-in 的协议调用没有源码 call 节点 | 保存已解析的对象类型，并消费现有迭代器语义元数据；不重复解析或修改 lowering | 补齐构造器／字段默认值／迭代协议测试 |
| P07 | 固定点中未就绪的返回值与真正动态目标需要区分；否则暂未解析到的 spec 接收者会过早贡献 unknown | 保留 bottom 状态；参数同时保留值来源与静态类型，稳定后再发布结果 | 补齐 spec 分发和返回 callable 测试 |
| P08 | 无源码 FT 测试中，`work<W>` 未利用消费方已选择的 witness，得到 unknown | 导入声明没有源码声明期 selection；同时读取已验证的 witness 成员选择 | 补齐两个 FT profile 的 spec 分发测试 |
| P09 | LSP 新用例仅发送 didOpen，等待通知超时，Hover 已通过 | 现有 didOpen／didChange 刷新查询状态，didSave 才请求诊断；补发 didSave 并使用 readiness barrier 等待发布 | 调整新增用例，不修改既有用例 |
| P10 | 对照 Codegen 发现显式成员混入由 source constructor 初始化，不能只计复制字段默认值；全局 var 也不能将初始 callable 当成永久目标 | 摘要消费现有 mixin 初始化事实，可变全局保留 unknown；`@abi` 只检查 callable 注解，不能由类型注解扩大为构造限制 | 增加对应语义与严格边界用例 |
| P11 | 独立 CLI 复跑曾漏设宿主 UBSan 链接选项；两层包的新 LSP fixture 使用版本依赖后触发 registry 查找 | 定向运行沿用 `test-sanitize` 的编译器环境；bundle fixture 沿用既有 direct-build 打包辅助函数，消费项目显式加载两个本地包 | 不修改依赖管理产品逻辑，复跑新增协议用例 |
| P12 | 复核固定点发现：即使参数不变，递归工厂也可能把上次返回的 callable 再捕获进新闭包，使返回环境不断嵌套 | 对确认存在依赖环的返回值增加结构增长检查，有限目标并集仍精确收敛 | 补充工厂创建／调用分离与递归返回测试 |
| P13 | 后台诊断发布只接受非 parse 项；成功分析为空时不发布，诊断刷新不完整 | 成功且文档版本仍一致的分析也发布空诊断；成功语义缓存的发布与诊断发布相互独立，失败／过期结果仍遵循既有保护 | 新增编辑后 Hover 更新、保存后空诊断发布的协议断言，包含同版本已发布语义缓存 |
| P14 | 新增 `T.work()` 静态 spec 方法用例，代入已知 `W` 后仍为 dynamic spec method | 静态目标必须使用已解析 owner 类型的实参域，不能依赖不存在的实例接收者 | 修复后同时验证源码／FT 及方法值路径 |
| P15 | 源码数组 fit 分发通过，无源码 FT 的 `work<i32[]>` 却为 dynamic spec method | witness 匹配遗漏 ARRAY subject；复用已选择的 fit 目标，结构匹配并绑定 owner 泛参，避免把数组统一误认成同一 subject | 增补闭合／开放数组 fit 的两个 FT profile 用例 |
| P16 | CLI 回归构建 std 时，`std.thread.threadTrampoline` 的动态 `ThreadFn` 调用触发 AE1313 | 既有 `@abi` 边界确实无法证明动态用户函数不逃逸，不能放宽摘要规避；在该入口内部兜底捕获后调用 C ABI `abort`，保持进程终止语义 | 人工已批准此阶段一迁移及增加的 try 边界开销；交付后另行讨论 callable-form spec 异常信息 |
| P17 | 新增同名不同泛型元数的对象初始化用例：`Item<string>{}` 错误查询到无泛参的 `Item`，摘要为空 | nominal 源声明查询除完整模块名外还必须匹配泛型元数 | 补齐对象初始化和 spec subject 的同名不同元数测试 |
| P18 | 沙箱外 `make test` 的 CLI 阶段加载残留的 `std-0.1.0.fb`，其中 `std/async.ft` 缺少必需摘要而失败 | 符合“不升级 FT 版本、重编产物”的既定要求；保留旧生成目录备份，重建 std／FCTS 包后重跑，不改依赖管理语义 | 首次完整回归退出 2；重建及复验结果见交付记录 |
| P19 | 对照 `cg_emit_module_binding_ensure_init_call` 和静态字段 ensure-init 路径，发现摘要中的全局读取只查询初始化结果，没有合并初始化异常；静态字段也缺少已解析来源 | 将惰性初始化作为普通的隐式调用：读取／赋值均计入初始化器摘要，泛型静态字段绑定 owner 实参，spec 静态字段复用已选 witness | 源码、两个 FT profile、严格 ABI 及捕获组合定向验证通过；不改初始化时机或 runtime |
| P20 | 第二轮全量回归的既有类型 Hover 用例失败：异常字段插入 signature 与 Kind 之间，还把类型声明的内部初始化摘要作为类型 Hover 展示 | 异常摘要只附加于 callable 声明／调用，排在既有展示内容之后，保持类型分类及文档展示次序 | 保留原断言，修复产品后复跑 |
| P21（核查项） | 核对完全捕获的多层间接调用是否仍进入固定点，避免调用点 Hover 错误显示为空 | 实际图保留 `WHEN(protected, handler)` 的依赖，即使 handler 为空仍会求解 protected；源码探针得到正确的 bool 摘要，无需新增求解机制 | 增补多层工厂与不同实参断言；初版嵌套 Lambda 测试缺少目标类型触发既有 AE0520，改用显式 Action 局部绑定，保持现有语言规则 |
| P22 | 复核 FT 边界发现：摘要根只检查 symbol／function ID 有效，尚未核对函数形状；把根改指另一个有效函数可绕过图内部校验，后续 owner 实参绑定依赖不匹配的槽位数量 | 在根挂接时校验声明类别、owner＋callable 泛参、显式参数及 receiver capture 数量，不接受错配根 | 增补两个 profile 的有效 ID 错配根及参数／capture 数量损坏用例，拒绝发生在 FT 读取阶段 |
| P23（验证过程） | 普通回归的增量构建检查发现 `make all` 有待重编文件 | 回归执行期间为 P22 修改了 `ft_read.c`，本轮结果已不对应固定源码快照；属于验证过程干扰 | 保存本轮日志，完成 P22 定向测试后冻结代码，重新执行完整 `make test`；不修改原增量构建断言 |
| P24（验证脚本） | 既有 cache-retention Python 脚本在缓存断言前失败，写死的包路径是 `build/*.fb`，实际输出在 `build/pkg/*.fb` | 路径由 2026-07-26 的 `ef8289af` 调整，本次未改输出路径；该手工脚本不在 `make test` 中，未同步修改 | 人工允许修正现有脚本，仅更新路径；原缓存断言保持不变 |
| P25（验证脚本） | 基础 LSP 性能脚本以 std_test 为输入，交互请求完成后等待进程退出超时 30 秒 | 脚本将子进程 stderr 接入 PIPE 却从不读取；依赖构建的约 467 KB Information 写满管道。诊断时仅将 stderr 写入日志，原请求／断言均通过，证明不是交互或后台求解超时 | stderr 改为继承输出，测试请求、断言及性能门槛不变，现有验证全部通过。此前此项批准状态待确认；人工在确认不影响执行和验证后，同意保留。仅调整日志去向，不新增 Python 文件 |
| P26 | 最终普通 CLI 回归中，跨包编辑用例得到新 Hover `none`，但未观察到空诊断；同轮 UBSan 路径已通过 | 新用例误以为 didChange 请求诊断；实际只有 didSave 请求，之前通过依赖尚未执行的保存任务被合并。改为先等待原 Information，再编辑等待 Hover，最后保存并分别验证同步 parse 与后台完整诊断。后台成功分析即使与已发布缓存同版本，也应独立发布诊断，不能用缓存是否替换作为成功条件 | 修正新增用例与 P13 成功判据后，独立 CLI 及冻结代码后的 UBSan／普通全量回归均通过，保持现有通知语义 |
| P27 | 人工 Review 的终端截图显示大量异常传播 `info`，定位被既有打印器渲染为红色；上轮全量虽退出 0，CLI 输出范围仍不符合 LSP 提示要求 | 异常 Information 进入共享语义 infos，所有 CLI 接收者一并打印。首版尝试增加受众标记在前端过滤，随后按 P30 的人工意见移除此设计，由 LSP 自行生成提示 | 首版及 P30 最终方案均完成沙箱外 `make test`；最终完整日志的异常传播提示为 0 条 |
| P28（构建环境） | P27 首次普通增量链接发现 `lexer.o`／`dump.o` 引用 `__ubsan_handle_*`，普通链接没有 sanitizer runtime 而退出 2 | 当前目标目录保留了 UBSan 产物；不同编译选项不能混用。确认没有其他构建运行后，清理生成物并统一重编 | 清理重编和独立 CLI 回归通过；不改编译器逻辑或测试断言 |
| P29（新增用例构造） | 新写的 ABI 用例将普通 callable-form spec `Action` 放在参数中，先被既有 AE1310 签名规则拒绝，未到达目标 AE1313 异常检查；另有两处新断言误以为失败分析返回 infos、CLI JSON 包含诊断码 | 修正新增夹具：ABI 函数不接收参数，改为读取 Feng 可变 callable；成功路径验证通用提示；错误回调／文本输出验证 AE1313，JSON 按既有字段验证 `severity: error`、semantic 来源和对应错误正文，全部错误路径须非零退出。产品签名规则、异常规则及 JSON 协议均不改变 | 新增矩阵、独立 CLI 及最终 UBSan／普通全量回归通过 |
| P30（分层修正） | 人工明确核心编译器不应感知 editor／提示；普通场景不能因生成提示而执行额外的强制处理检查 | 移除 P27 首版的 `audience`、`include_editor_infos` 及核心异常 Information 构造；核心保留结构化异常事实与 `@abi` 错误检查，LSP 读取调用点事实后独立生成 Information。普通调用继续计算、传递摘要，不检查必须捕获；阶段二不提前实施 | 核心无异常提示、CLI 安静、LSP 提示及严格错误的用例全部通过；全量回归及现有 LSP 缓存／调度／性能脚本通过，见下方最新记录 |
| P31（提示收敛） | 人工要求移除不必要的提示，仅保留可能异常类型；进一步明确核心摘要应始终只有可能异常类型，不需要 `types_only` | 撤回首版的 `types_only` 和新增格式化 API。现有摘要格式统一为去重后的类型列表，未知为 `unknown`；ABI 错误和 LSP 读取同一份摘要，不保留两种展示格式。泛型代入及 catch 筛选条件仍仅用于内部推导。LSP 移除调用处 Information，无异常时不增加 Hover 条目。不改变 FT 布局、版本及元信息查询 API | 源码、跨包、缓存、未知及条件／重复类型、空集合、编辑更新和 ABI error 覆盖通过；冻结代码后的沙箱外 `make test` 退出 0，现有 LSP 缓存／调度／性能验证通过，见最新记录 |
| P32（新增用例构造） | P31 定向测试中，新增条件类型夹具误写 `try { throw x; }`，报告 SE0006；新增 LSP 断言又误以为协议输出包含 AE1313 编码 | try 主体要求表达式，改用泛型函数调用承载 throw。既有 LSP 诊断只序列化 severity／source／message，错误码已由语义测试和前端回调验证；LSP 用例按既有协议验证 error、semantic 来源及已知／未知异常正文 | 仅修正本轮新增夹具与断言，不改语法、协议或既有用例；语义、FT 和完整 CLI 定向测试均已通过 |

P33：人工要求 FT 仅保留结构化符号及编译信息、源码文档注释。审计发现 unknown 原因文本
和预格式化 `display` 均被持久化，且原因参与节点驻留键。先从构造入口移除原因文本，
以求解后的结构化根替代 `display`；导出仅保留模板、声明结果及必要依赖。FT 记录规则以
[符号表规范](../specifications/feng-symbol-table.md#异常摘要元数据)为准，外层版本不变，
旧文本记录必须失效重编。中性查询返回结构化结果；LSP 按需生成类型文本，`@abi` 在单独的
检查入口消费完整结果并生成错误。阶段二 defer、本次 runtime／ABI 协议均不变。

- [x] 完成元信息、消费者及 FT 编解码调整，保持泛型、捕获条件和跨包行为。
- [x] 验证 FT 不包含原因／类型列表／诊断文本，源码文档注释正常保留；覆盖结构损坏和旧产物拒绝。
- [x] 补齐声明、调用、缓存及 LSP 查询，并验证已知／未知 ABI 错误与普通传播。
- [x] 沙箱外运行 `make test` 及现有 LSP 缓存／调度／性能验证，回填结果，等待人工 Review。

P16 已于 2026-09-21 获人工批准，具体迁移涉及 `std/std/src/thread/Thread.ff`：通过既有 C ABI 声明
`@cdecl("libc", "abort") extern func abortUnhandledThreadException(): void;`，将 trampoline
末尾的 `fn();` 改为 `try fn() catch { abortUnhandledThreadException(); }`。
线程函数正常执行路径会增加 try 边界的建立／退出成本，异常路径在边界内终止进程；
不新增 runtime API，不变更 ABI，不吞掉异常后正常返回。新增 subprocess 用例应验证
未处理线程异常终止进程，原有正常线程与内部自行捕获异常的行为仍须通过回归。
本次不放宽严格边界规则；阶段一交付后再独立讨论 callable-form spec 的异常信息。

两阶段分别遵循“文档 → 实现 → 补齐测试 → 定向验证 → 全量回归 → 交付”的顺序。
阶段一独立交付并完成人工 Review 后，再开始阶段二；阶段二的回归不能代替阶段一
的验收。本文的 `throws` 信息指编译器记录的异常摘要，不代表新增用户必须填写的语法。

### 5.1 已完成基础与前置事项

- [x] 确认 N08 在修复前的 codegen/runtime 中同样存在。
- [x] 独立记录复现、根因和基线证据，与嵌套 catch 修复分开交付。
- [x] 完成嵌套 catch 修复的全量回归，N08 用例按人工决定完整注释保留。
- [x] 确认异常收集与传播规则：记录具体类型或开放泛参，普通分支取并集，合并内部调用，按各自捕获范围扣除，并计入处理代码的新异常及重抛。
- [x] 确认兜底 catch 扣除全部传入异常；匿名重抛恢复当前 catch 接收的剩余摘要，带表达式的再次抛出按结果类型记录，不统一恢复全部原异常。
- [x] 按最新人工决定收敛阶段一提示，见 §4.3.1；普通调用不强制处理。
- [x] 确认严格边界无法证明无异常逃逸时编译期报错；阶段一用于 `@abi`，阶段二用于 defer。
- [ ] 完成嵌套 catch 修复的人工 Review。

### 5.2 阶段一：完整记录异常摘要并提供 LSP 提示

交付结果：每个 callable 都有可查询的对外异常摘要，跨包调用可读取该摘要，LSP
使用同一份语义结果提示用户。“完整”包含所有已有 callable 和调用入口；可确定
的部分记录类型，无法确定的部分显式保留未知，不能将缺失信息当成无异常。
本阶段不增加 defer 异常逃逸禁令，不改变 §1 的普通调用自动传播行为。

阶段一按人工要求设置以下三项验收条件，任一项未完成均不能交付：

| 要求 | 验收依据 |
| --- | --- |
| 泛型正确 | §4.1.1 的开放身份、调用点替换、捕获条件求值和多调用点隔离均有结果断言，覆盖类型级、函数／方法级及两域组合 |
| 跨包正确 | §4.2 的发布与缓存路径完成端到端验证，包含依赖源码不可见时的泛型摘要替换与 LSP 提示，结果与源码路径一致 |
| 尽可能无遗漏的覆盖 | §5.2.3 的基础维度与关键交叉组合均对应实际用例、断言和验证记录；未覆盖项须说明原因并经人工确认，不能静默省略 |

#### 5.2.1 规范与方案

- [x] 定义 §4.1 的摘要模型：具体类型身份、泛参依赖、未知部分、已证明的空集合，以及集合合并、去重和比较规则。
- [x] 完成 §4.1.1 的参数化摘要方案：泛参声明身份、owner／callable 联合替换、开放转传、捕获条件归约及调用点缓存隔离。
- [x] 按 §4.1 已确认规则细化传播算法：分支并集、调用依赖与递归收敛、各保护范围的精确／兜底 catch 筛选，以及处理代码的新异常和重抛如何继续向外传播。
- [x] 明确函数、方法、构造函数、Lambda、callable 值、spec 方法及泛型约束调用的摘要来源和替换规则；无函数体、间接目标及 ABI 边界按已有契约区分处理。
- [x] 设计 §4.2 的 FT 摘要导出、导入恢复、兼容校验和缓存失效方案；完整保留参数化摘要及捕获条件，明确私有实现异常如何汇总到可跨包调用的声明。
- [x] 按 §4.3.1 已确认的 Hover 形式细化可能异常类型展示；缓存与交互性能遵循 [LSP 性能方案](./feng-lsp-performance-optimize.md)。
- [x] 将已审定的语言规则更新到对应主规范，实现细节保留在本文；完成阶段一方案 Review。

#### 5.2.2 实现

- [x] 实现统一异常摘要及查询接口，供编译器和 LSP 复用；`@abi` 迁移为 §4.3.2 的严格检查，已知异常及无法证明安全的部分均须正确诊断。
- [x] 完成函数体与调用链分析，计入参数、接收者、构造／初始化等已有求值路径的异常，避免仅扫描显式 `throw`。
- [x] 完成类型级、函数／方法级及两域联合的泛参替换、开放转传、直接与间接调用传播、递归收敛，以及替换后的捕获覆盖和重抛分析。
- [x] 将摘要接入 symbol、FT 读写和导入恢复，使调用方看不到依赖源码时仍取得正确结果。
- [x] 将摘要接入 LSP 持有的语义结果与符号缓存，按审定形式展示已知类型、未知部分及尚未捕获的异常；交互请求不重新遍历调用链。
- [x] 保持摘要为编译期信息与工具提示；不引入新的 runtime／ABI 协议或 defer 限制。std 线程入口按 P16 的人工批准增加内部 try 与 C ABI abort。

#### 5.2.3 用例与定向验证

- [x] 建立覆盖矩阵，将以下维度和关键交叉组合映射到具体用例、断言及结果；发现遗漏后补齐，不以少量代表用例代替完整检查。
- [x] 在 `test/` 验证摘要内容：空集合、单一／多种异常、重复类型、泛参依赖、未知与已知混合、缺失信息，以及精确类型身份。
- [x] 在 `test/` 验证传播组合：直接／多层调用、直接／相互递归、分支与循环、全部／部分／不匹配捕获、多层 catch、具名再次抛出、匿名重抛和处理代码再次抛出。
- [x] 验证普通分支不按条件真假裁剪摘要，具体类型与开放泛参均按并集合并；同时验证 catch 类型筛选在泛参代入后仍准确，二者不混用。
- [x] 验证捕获范围隔离：只扣除自身保护范围的异常，catch 中同类型再次抛出、内部调用抛出及匿名重抛均保留；同组其他 catch 不误扣除，外围捕获及处理代码内的嵌套捕获仍正确生效。
- [x] 验证 §4.1 的兜底 catch 与再次抛出示例，并扩展到开放泛参和未知来源：全部处理后为空、匿名重抛全部／剩余类型、仅再次抛出特定类型、改抛其他类型、普通分支中重抛；嵌套 catch 的重抛摘要不得串用。
- [x] 覆盖所有已有 callable 与调用入口：顶层函数、静态／实例方法、构造函数、Lambda、函数值的传递／返回／字段存储、spec 方法和泛型约束调用，以及已知和无法确定的间接目标。
- [x] 覆盖泛型函数、泛型类型方法、方法泛参、两域组合、fit／spec 方法、构造函数、共享体与具体实参替换；分别验证显式实参与推导实参、开放转传和多层转传后闭合的结果。
- [x] 验证泛参身份与缓存隔离：不同声明中的同名参数、多次调用同一泛型声明且使用不同实参、调用顺序变化、owner 与方法实参独立变化，结果不得串用。
- [x] 验证依赖泛参的捕获条件：代入后完整匹配、部分匹配、不匹配、多层捕获、匿名重抛、捕获后产生新异常，以及仍开放时继续转传的摘要。
- [x] 覆盖合法异常载荷的泛型代入：标量、string、enum、tuple、引用和值类型、闭合泛型实例；同布局但不同声明、不同泛型实参的异常须保持精确身份。
- [x] 在 `test/` 验证 FT 往返、依赖源码不可见、多层跨包调用、私有辅助函数汇总、缺失／损坏摘要、缓存失效及依赖变化后的重分析。
- [x] 建立“泛型 × 跨包 × LSP”端到端组合：生产包声明泛型 callable，中间包开放转传，消费包闭合并部分／完整捕获；依赖源码不可见时核对摘要、捕获结果和提示，与同源分析对照。
- [x] 在发布 FT 与 workspace 缓存路径分别覆盖两域参数、捕获条件、导入别名、跨模块同名异常及跨包函数值；验证依赖或实参变化后不会沿用旧摘要。
- [x] 补齐 LSP 协议用例：本地／跨文件／跨模块／跨包、泛型实参显示、已处理与未处理异常、未知信息、编辑后的提示更新和清除、分析失败及取消时的缓存一致性，并验证既有性能门槛。
- [x] 覆盖 `@abi` 的严格证明：空摘要通过，已知／未知／未消除的泛参条件报错，内部完整捕获后通过，部分捕获或处理代码再次逃逸报错；CLI 与 LSP 一致，普通调用只传播元信息。
- [x] 区分声明与执行 callable：创建／返回会抛异常的 Lambda 不误计为当次调用抛出，后续实际调用仍恢复其摘要；跨包及泛型捕获环境同样覆盖。
- [x] 在 `fcts/` 验证普通调用的未捕获异常继续向上传播、具体与兜底捕获及重抛行为；现行合法 defer 行为继续通过，N08 保留用例仍按 §2.3 注释。
- [x] 执行上述定向验证并逐项记录结果；所有失败先记录、再分析、再修复，不确定事项交由人工决策。

#### 5.2.4 阶段一独立交付

- [x] 逐项验收本阶段的泛型、跨包及覆盖矩阵三项要求，确认实际结果而非仅有用例注册或编译成功。
- [x] 完成定向验证后，在沙箱外执行 `make test` 全量回归，记录环境、命令、日志和各目标结果。
- [x] 修复本阶段发现的问题并完成必要复验及全量回归，确认无未解决的阶段一阻塞项。
- [x] 回填阶段一覆盖清单与交付记录，明确 N08 尚未修复，输出英文 commit message，不自动提交。
- [x] 完成阶段一人工 Review；人工已提交阶段一并批准开始阶段二。

#### 5.2.5 阶段一覆盖映射与验证记录

新增用例只追加 C／Feng 文件、测试函数和注册，不改写既有测试断言；N08 的注释用例保持原样。
P24 的现有 Python 脚本路径修正与 P25 的 stderr 改动均已获人工批准，不新增 Python 文件。
以下按断言目标映射，结果以定向测试及全量回归日志为准。

| 维度 | 用例及断言 |
| --- | --- |
| 摘要基本集合及传播 | `test/semantic/test_exception_effects.c` 的 `effects_control_flow`：空、具体类型、去重、分支／循环／match、多层调用、精确／兜底／不匹配捕获、处理代码调用和再次抛出、嵌套重抛及捕获范围隔离 |
| 泛参身份与调用隔离 | `effects_generic_domains`：开放转传、显式／推导实参、owner／method 双域、两域同时抛出、同名泛参、多实例调用顺序颠倒、依赖代入的捕获／改抛／剩余重抛 |
| callable 值及递归 | `effects_callable_values`：创建／传递／返回／调用分离、泛型闭包捕获、静态／绑定方法值、字段及可变 callable 的未知信息、直接／相互递归、递归类型实参增长、返回闭包环境增长 |
| 隐式求值 | `effects_implicit_calls`：显式／默认构造、对象字面量、字段默认值、mixin constructor、泛型构造、参数／接收者／索引／tuple 元素、for-in 两种协议调用、同名不同元数的初始化 |
| 惰性初始化 | `effects_lazy_initializers` 与 FT 用例：全局 let／var 的读取、赋值和复合赋值；泛型静态字段两种实参及开放转传；spec 静态字段 witness 读写；初始化器返回 callable 与调用分离；严格 ABI 及具体捕获 |
| spec 分发 | `effects_spec_dispatch`：type／fit／builtin／闭合及开放数组 fit、已知 subject 和未知 subject、泛型实例／静态方法调用、同名不同元数的 subject |
| 载荷精确身份 | `effects_payload_matrix`：20 类合法载荷代入及具体 catch；覆盖标量、string、enum、tuple、引用／值／ABI 类型及闭合泛型；不同声明和不同实参不误匹配；原开放 nominal throw 限制仍保留 |
| 未知及严格边界 | `effects_payload_matrix`、`effects_abi_boundaries`：已知与未知并存、未知兜底消除／重抛／改抛、重复具体捕获排除、`@abi` 已知／未知逃逸拒绝、内部完整捕获通过、C extern 保持空摘要 |
| 调用位置 | `effects_call_facts`：同一 try 的多个实参调用分别过滤，未捕获的 bool 与对应源码 token 保留在中性元信息中；核心不发布异常 Information |
| 对外类型集合 | `effects_type_summary`：开放条件保留可能类型、已知与多种未知合并、去重排序、嵌套泛型参数、空集合、查询边界；格式化不改变内部图 |
| 完全捕获的调用点 | `effects_caught_call_queries`：单层／多层泛型工厂返回闭包，bool／string 实参；函数摘要为空，内部实际调用点仍有具体类型摘要 |
| 无源码 FT 与缓存 profile | `test/symbol/test_exception_effects.c`：写出后销毁生产 AST／analysis，分别读取 public 与 workspace FT；私有辅助依赖、开放／闭合及两域参数、fit／spec／数组／静态方法值、递归和跨包闭包；复用导入缓存进行不同调用点查询，核对声明类型文本与具体调用、混合 unknown 及捕获后的集合 |
| FT 身份及完整性 | 同文件：别名及跨模块同名异常；37 类元数据损坏分别在两个 profile 验证，包含有效 ID 的错误根形状、旧文本记录、非类型名称、未求解根及错误布尔条件引用；直接检查 STRS 不含生成的类型列表／原因／诊断文本，源码文档注释正常保留；17 类载荷的导入泛型调用与具体 catch 对照源码行为 |
| 本地 LSP | `test_lsp_exception_effects`：纯文本／Markdown Hover，声明开放 T、调用点 bool／string、混合 unknown、条件筛选及空集合不加条目；保存的 parse／完整语义诊断均无异常 Information；`test_lsp_exception_effects_diagnostics` 验证 unknown 及已知／未知 ABI 错误 |
| 跨文件／模块／包 LSP | `test_lsp_exception_effects_source_modules` 验证不预建 FT 的跨文件／模块源码；`test_lsp_exception_effects_packages` 验证两个无源码 bundle 的开放转传、消费端闭合、别名、未保存编辑后 Hover 更新及保存后提示清除；同步 parse 与后台完整诊断分别断言 |
| 既有 LSP 生命周期及性能 | 继续运行 `test_lsp_local_project_dependency_cache_lifecycle` 等既有 CLI 用例，以及现有 cache-retention、scheduler 与性能脚本；异常摘要随成功语义 session／symbol 生命周期管理，没有独立全局结果缓存 |
| ABI 线程入口迁移 | 新增 `test_exception_effects_thread_boundary`：真实 std 依赖、动态线程回调异常、隔离子进程必须 SIGABRT；正常／自行捕获线程由新增 FCTS 验证 |
| 提示范围与未知传播 | `test/cli/exception_info_routing.inc`：核心 infos 不含异常传播提示，通用 orphan fit 信息和回调计数保留；CLI direct、build、pack、check text／JSON、tool semantic／check 无异常提示；源码不可见的多层调用保留 unknown，完整／部分捕获、重抛与改抛结果及 LSP Hover 均验证；`@abi` 已知／未知逃逸均通过 error 回调报告 AE1313，完整捕获通过；现行 defer 未提前启用阶段二限制 |
| 语言行为 | `fcts/fcts_bin/src/test_callable_exception_effects.ff`：普通传播、具体／兜底 catch、类型转换重抛、闭包执行、合法 defer、正常线程及线程内部捕获；既有 N08 注释不启用 |

验证环境：macOS arm64。定向 C 测试采用与 `make test-sanitize` 一致的 UBSan 编译参数；
生成程序通过 `FENG_CC=clang FENG_CC_FLAGS=-fsanitize=undefined` 使用宿主 sanitizer runtime。

- 定向 `test_semantic`、`test_symbol` 已通过，包括新增源码和两个 FT profile 矩阵。
- 新增 LSP 协议用例和线程终止测试已在沙箱外全量测试的 CLI 阶段执行通过。
- 首轮沙箱外 `make test` 在既有 DAP 用例加载旧 std 包时因缺少摘要退出 2，见 P18；
  日志保留为 `temp/callable-effects-prior-logs/callable-exception-effects-regression-old-ft.log`。
  旧生成目录移出后已完成标准库重编，后续全量回归重新生成并验证标准库与 FCTS 包。
- 后续一轮在既有 Hover 断言处发现 P20，日志为
  `temp/callable-effects-prior-logs/callable-exception-effects-regression-hover.log`。
  修复后的一轮 UBSan 全部目标通过，普通阶段 std 607／607、FCTS 1470／1470；随后因
  P23 的并发源码修改使增量构建检查失败，日志为
  `temp/callable-effects-prior-logs/callable-exception-effects-regression-before-ft-hardening.log`。
- P22 的 21 类损坏矩阵和 P21 多层 callable 查询已完成定向验证；随后全量回归的
  UBSan 全部通过，普通阶段在新增跨包 LSP 编辑用例发现 P26，失败日志为
  `temp/callable-effects-prior-logs/callable-exception-effects-regression-edit-race.log`。
- P26 修复后独立 CLI 全部通过。一次独立运行退出 1，但日志被既有 CLI 的 `temp/`
  清理删除，未能据此定位失败；改为内存收集完整输出后复跑通过。最终完整回归同样在
  结束后写入 `temp/callable-exception-effects-regression.log`，不修改既有清理用例。
  最终冻结代码后的沙箱外 `make test` 完整退出 0。

阶段一首轮交付验证（2026-09-21，P27 修复前）：

本轮的 `temp/` 日志已被后续回归的既有清理流程删除；下表保留当次执行结果和日志路径。

| 验证项 | 结果 |
| --- | --- |
| 沙箱外 `make test` | UBSan 与普通构建两轮全部通过；日志 `temp/callable-exception-effects-regression.log` |
| 两轮标准库／FCTS／smoke | 每轮分别为 607／607、1470／1470、91／91；均无失败或跳过 |
| 其余全量目标 | archive、lexer、parser、semantic、runtime、codegen、debug、CLI、CLI paths、symbol、direct／project CLI、初始化、性能约束均通过；普通构建另通过增量空构建、发布／安装、macOS finalize、bundled packages 与 toolchain fetch |
| 手工缓存及调度 | 现有 cache-retention 与 scheduler 脚本均通过；日志 `temp/callable-effects-lsp-cache-final.log`、`temp/callable-effects-lsp-scheduler-final.log` |
| LSP 基础交互性能 | 200 次采样，交互 P99 0.046 ms；修正 stderr 后正常退出；日志 `temp/callable-effects-lsp-performance-final.log` |
| 推导 callable 性能与恢复 | 现有脚本全部场景及原门槛通过，包含错误恢复、外部符号和缓存；日志 `temp/callable-effects-lsp-inferred-performance-final.log` |
| LSP 规模矩阵 | 1 万／10 万／100 万行、200 次采样，整体交互 P99 0.157 ms、Max 0.294 ms；原门槛通过，日志 `temp/callable-effects-lsp-matrix-final.log` |

P27／P30 修正后的验证（2026-09-21，P31 实施前）：

| 验证项 | 结果 |
| --- | --- |
| 定向语义测试 | 编译及 `test_semantic` 通过；调用点保留异常事实，核心不生成异常传播 infos，调用点查询不改变语义结果 |
| 沙箱外 `make test` | 冻结实现后完整退出 0；UBSan 与普通构建每轮 std 607／607、FCTS 1470／1470、smoke 91／91，均无失败或跳过；所有其余全量目标通过。完整日志 `temp/exception-facts-regression.log` |
| CLI 输出与 LSP 提示 | 完整回归日志中的 `exceptions may propagate from this call:` 为 0 条；CLI 各入口无该提示，原有通用提示保留；LSP 的本地／跨包 Information、Hover、编辑更新／清除及 ABI error 用例通过 |
| 手工缓存及调度 | 两个现有脚本通过；日志 `temp/exception-facts-lsp-cache.log`、`temp/exception-facts-lsp-scheduler.log` |
| LSP 基础交互性能 | 200 次采样，交互 P99 0.043 ms，原门槛通过；日志 `temp/exception-facts-lsp-performance.log`。此结果使用含 P25 stderr 改动的脚本；该改动后续已获人工确认保留 |
| 推导 callable 性能与恢复 | 现有脚本 200 次采样的全部场景及原门槛通过；日志 `temp/exception-facts-lsp-inferred.log` |
| LSP 规模矩阵 | 1 万／10 万／100 万行、200 次采样，整体交互 P99 0.127 ms、Max 0.351 ms；原门槛通过，日志 `temp/exception-facts-lsp-matrix.log` |

P31 验证（2026-09-21，P33 实施前）：

| 验证项 | 结果 |
| --- | --- |
| 定向回归 | `test_semantic`、`test_symbol` 及沙箱外完整 `test_cli` 均通过；覆盖类型集合、FT 两个 profile、Hover 文本／Markdown、空集合、未知、泛型调用、跨包编辑和严格 ABI error |
| 沙箱外 `make test` | 完整退出 0。UBSan 与普通构建每轮 std 607／607、FCTS 1470／1470、smoke 91／91，失败和跳过均为 0；其余全量目标全部通过。日志 `temp/exception-types-hover-regression.log` |
| 提示与强检查 | 全量日志中异常传播 Information 为 0 条，UBSan runtime error 为 0 条；LSP 保存时分别验证 parse 和完整语义诊断均无该提示；已知／未知 ABI 逃逸仍为 error |
| 缓存及调度 | 现有两个脚本通过；日志 `temp/exception-types-hover-lsp-cache.log`、`temp/exception-types-hover-lsp-scheduler.log` |
| 基础交互性能 | 200 次采样，交互 P99 0.071 ms，原门槛通过；日志 `temp/exception-types-hover-lsp-performance.log`。此结果使用含 P25 stderr 改动的脚本；该改动后续已获人工确认保留 |
| 推导 callable 性能及恢复 | 现有脚本每个稳定场景 200 次采样，全部原门槛通过；日志 `temp/exception-types-hover-lsp-inferred.log` |
| 规模矩阵 | 1 万／10 万／100 万行、200 次采样，整体交互 P99 0.131 ms、Max 0.269 ms，原门槛通过；日志 `temp/exception-types-hover-lsp-matrix.log` |

该轮全量回归至 LSP 脚本结束，产品代码及测试内容保持冻结。P31 当时保留现有元信息查询 API，
仅统一可能异常类型的文本表示，并由 LSP 在 Hover 使用；没有 `types_only`、新增格式化 API、editor 标记或提示订阅参数。
CLI 前端回调与 `FengSemanticInfo` 结构保持原状，普通调用没有新增必须处理异常的检查。

P33 最新验证（2026-09-21）：

| 验证项 | 结果 |
| --- | --- |
| 定向回归 | `test_semantic`、`test_symbol` 及沙箱外完整 `test_cli` 均退出 0；两个 FT profile 的结构化声明查询、跨包重新代入、unknown 传播／捕获、37 类损坏拒绝、源码注释保留及 LSP Hover 均通过 |
| 沙箱外 `make test` | 完整退出 0；UBSan 与普通构建每轮 std 607／607、FCTS 1470／1470、smoke 91／91，无失败或跳过；其余全部回归目标通过。完整日志 `temp/exception-structured-regression.log` |
| 文本与元信息隔离 | 回归日志中异常传播 Information 为 0 条、UBSan runtime error 为 0 条；已重编 std／FCTS 库，实际 std FT 扫描未发现原 unknown 原因、Hover 标签或 ABI 诊断正文；受控 FT 用例直接检查 STRS 并核对源码注释 |
| 缓存及调度 | 现有两个脚本通过；日志 `temp/exception-structured-lsp-cache.log`、`temp/exception-structured-lsp-scheduler.log` |
| 基础交互性能 | 200 次采样，交互 P99 0.050 ms，原门槛通过；日志 `temp/exception-structured-lsp-performance.log`。此结果使用含 P25 stderr 改动的脚本；该改动已获人工确认保留 |
| 推导 callable 性能及恢复 | 现有脚本每个稳定场景 200 次采样，全部原门槛通过；日志 `temp/exception-structured-lsp-inferred.log` |
| 规模矩阵 | 1 万／10 万／100 万行、200 次采样，整体交互 P99 0.131 ms、Max 0.249 ms，原门槛通过；日志 `temp/exception-structured-lsp-matrix.log` |

定向符号测试原有的 `temp/` 清理删除了重定向日志，进程退出码已确认；后续 CLI 与全量
验证先在内存收集输出，再在结束后写入日志，不改既有测试清理行为。P33 之前的临时日志
已由后续测试清理；历史表格保留当时记录，本次完整日志以上表为准。

独立 CLI 验证后至全量回归和 LSP 脚本结束，产品代码与测试内容保持冻结，指纹均为
`c0f1929bf413269983b21feebab9a751f6103c35dd96ef3bba93e1defbb6fceb`
（`git diff HEAD -- src test fcts std scripts | shasum -a 256`）。
声明、调用及 Symbol 查询返回结构化结果；类型文本仅由消费者按需生成，`@abi` 错误
通过独立检查入口构造，不写回异常元信息。FT 外层版本不变，旧文本格式须重编。

本阶段没有新增 Python 文件，FT 版本号不变，runtime／ABI 协议不变；线程入口按 P16
的批准使用现有 C ABI。N08 保留源码及断言继续完整注释，阶段二尚未实施。
代码未自动提交，阶段一等待人工 Review。

### 5.3 阶段二：基于异常摘要处理 defer 异常逃逸

交付结果：利用阶段一的 `throws` 信息检查 defer 清理边界，处理 N08。
设计方向是异常不能逃出 defer；普通函数继续向上传播的能力按 §1 保留。
本阶段检查 defer 自身的出口，不能因为外围函数或调用方有 catch 就认为该边界安全。

2026-09-21 人工调整交付范围：先独立交付阶段二 defer 检查及已完成的配套修复，
将既有 release 异常展开问题 S11 移至
[独立修复方案](./feng-release-exception-unwind-bugfix.md)。按人工澄清，既有 BIND10 用例
在非 release 模式下能通过，无需注释；保留其原断言及全部 defer 用例，执行沙箱外 `make test`。
这次拆分不表示 S11 已修复，也不将默认回归结果记作完整 release 异常行为通过。

#### 5.3.0 实施记录与既有用例迁移

2026-09-21 人工批准开始阶段二。边界语义与 `AE1507` 的定义集中在
[defer 规范 §6](../specifications/feng-defer.md#6-与异常模型的关系)。实现复用阶段一的图及
固定点求解,在遍历每个 defer 时保存该块独立的结果根和块内调用的局部传播根;
这些根不接受外围 catch 的扣除。求解完成后由独立的 defer 检查入口消费结果并构造诊断。
关联调用位置复用既有 `FengSemanticRelatedLocation`,不向异常元信息或 FT 写入诊断文本。
本阶段不引入新的 runtime／ABI 协议,不改变 FT 布局或版本。

既有测试的必要迁移先列明,获人工确认后执行;新增用例可以独立推进。

| 编号 | 现有用例 | 迁移内容 | 状态 |
| --- | --- | --- | --- |
| M01 | `test/semantic/test_exception_effects.c` 的 `defer{fail();}` 成功摘要断言 | 保留原源码,改为断言 `AE1507`;新增内部完整捕获的合法摘要断言 | 人工已批准,已迁移 |
| M02 | `test/cli/exception_info_routing.inc` 的 `cleanup(f)` 无捕获 defer | 安静输出夹具改为 defer 内兜底捕获,对应函数摘要断言改为空;原无捕获源码作为新增 CLI／LSP 错误用例保留,其他提示断言不变 | 人工已批准,已迁移 |
| M03 | `test_nested_exception_lifetime.ff` 的 `nestedExceptionEscapingLandingCleanup` 及对应成功行为断言 | 原辅助函数和断言完整注释保留,新增对应编译期负例;N08 原有注释源码保持原样,其他合法生命周期用例继续运行 | 人工已批准,已迁移 |
| M04 | `fcts/fcts_lib/src/test/lib_generic_cross_feature_coverage.ff` 的 `libCrossFeatureDeferObserve` | defer 内的 `observer(current)` 增加兜底捕获;意外异常用 `Process.exit(1)` 使测试失败,正常／展开路径的原回调、值及生命周期断言不变 | 人工已批准,已迁移;FCTS 语义检查通过 |
| M05 | `test/debug/loop_breakpoints.ff` 的 `deferLoop` | defer 内的 `println` 增加兜底捕获;意外异常用 `Process.exit(1)` 使测试失败,保留原循环、调用断点和命中次数断言 | 人工已批准,已迁移;完整 DAP 及两轮全量回归通过 |

问题按“现象／证据 → 分析 → 处理 → 验证”追加记录。未完成全量回归前不标记交付。

| 编号 | 现象／证据 | 分析与处理 | 验证 |
| --- | --- | --- | --- |
| S01（新增夹具） | 首轮外围 catch 用例得到 `SE0006`;后续 spec 夹具得到 `SE0604`／`AE0704`;新增 FCTS 匿名重抛场景的空 if 分支得到 `AE1101`;新增 Codegen C 夹具遗漏 `mkdtemp` 的头文件声明,泛型尾部与赋值连写又得到 `SE0610`,均尚未进入目标验证 | 有结果的 try 改用局部绑定;spec 及其实现的方法签名补齐既有规则要求的 `:void`;FCTS 的 if 表达式补齐正常完成分支的结果;C 夹具补齐 `unistd.h` 和泛型尾部必要空格,不调整产品语法规则 | 80 个源码场景、两种 FT profile 各 39 个场景、FCTS 及 Codegen 均已通过 |
| S02 | 定向检查 FCTS 在 `fcts_lib/src/test/lib_generic_cross_feature_coverage.ff:88` 的 defer 报 `AE1507: unknown`,依赖包构建被拒绝 | 声明环境中外部传入的 `Action<T>` 不能证明无异常;原调用者仅赋值,原用例验证泛型捕获及生命周期。按 M04 迁移,不把 unknown 当空集合 | 人工批准后迁移,原行为断言在 FCTS 中通过 |
| S03 | 完整 CLI 回归在 DAP 循环断点夹具第 141 行的 defer 报 `AE1507: string, unknown`;新 CLI／LSP 边界用例及原提示路由用例此前已通过 | 数值 `println` 保留 `string` 异常,多余 unknown 的独立原因见 S05;原用例验证清理循环的断点和隐藏帧,按 M05 在内部处理异常,不放宽检查 | 人工批准后迁移;完整 DAP 与两轮全量回归通过 |
| S04 | 新 FCTS 的开放泛型清理在 C 编译时报未定义的 `_T`、`_U`、`_td`;涉及泛型函数及同时使用 owner／方法泛参的清理块。不含任何异常的 `cleanup<T>(value:T){defer{consume(value);}}` 也复现 `_T` 未定义 | `cg_emit_defer` 已捕获值存储及部分局部描述符,但没有恢复共享体的泛参／owner／callable 描述符环境;代码生成仍引用外层 C 函数的名字。复用现有 Lambda 的泛型环境表示,由清理栈闭包保存并在 helper 中恢复,不对具体类型或异常调用特判 | 阶段一对照编译器复现相同错误,确认既有缺口。人工批准后实施,源码与两种 FT profile 的宿主 C 矩阵及跨包行为通过 |
| S05（println 调用链核查） | 人工追问 `println` 的异常来源。隔离调用发现纯字符串重载、格式化函数、`Stdio.writeLine` 均通过;数值重载为 `string, unknown`;直接 `int.toString` 为 `string`。另以 `first<T:Display>(value:T)` 和 `indexed<T:Display>(values:T[])` 比较,同一 `int` 实参分别得到 `string` 和 `unknown` | `string` 来自 `i64.toString → byte[].clone(start,end)` 的显式越界 throw,符合不按实参值／分支条件消除异常的规则。额外 unknown 源于 `resolve_expr` 的 INDEX 分支未保存 `infer_expr_type` 已能提供的元素类型事实。补齐下标表达式的通用类型事实记录,复用现有泛型 witness 求解;不改异常规则、对外 API 或 FT 格式 | 源码及两种 FT profile 的泛型数组元素调用覆盖通过;真正动态的 spec／callable 元素继续 unknown |
| S06（S04 嵌套捕获验证） | 新 Codegen 矩阵在 defer 内创建捕获外部泛型值的 Lambda 时报告 `CE0102: lambda capture 'value' was not lowered to a capture cell` | defer 扫描跳过 Lambda 的捕获清单,且 helper 别名未保留 capture-cell 身份和普通 address 参数的 `is_storage_address`。扫描消费语义层已确定的捕获清单;对已有 cell 借用其指针并复用原 cell 别名机制,保留存储表示。每个捕获仍仅一个地址槽,不另造 cell、不增加堆分配或 ARC;Lambda 与清理访问同一变量 | S04 嵌套闭包、动态值及跨包实际行为验证通过 |
| S07（S04 C 类型验证） | 新矩阵在宿主 C `-Werror` 下报告 const 参数地址写入 `void *` 丢限定符,以及共享方法 `_self` 的 `void **` 与具体 owner 指针地址不匹配 | 捕获按既有值种类及可重新绑定性选表示：不可重新绑定的托管引用直接借用稳定引用,可重新绑定的普通局部仍借用槽地址;已有 cell 和动态存储分别保留原表示。动态参数地址进入统一存储视图时显式转为 `void *`,源代码可写性仍由语义层控制。避免把擦除的 `void **` 当具体类型的指针地址;不新增分配、引用计数或运行开销 | 宿主 C 的默认／`-O2` 编译及默认模式实际行为验证通过 |
| S08（实际行为） | 定向 FCTS 的前七个新增清理场景通过,随后“动态回调异常由清理内部兜底捕获”场景触发 `panic: uncaught exception (unwind reason=5)`,进程信号 6。无泛型、无 std 的最小程序用阶段一编译器仍同样终止 | 生成 C 的 defer helper 有 try 区间及 catch landing pad,却缺少普通函数已有的 CFI personality／LSDA 标注,平台展开器不会查询该帧的处理器。提取共用的静态展开元数据生成函数供普通函数和 defer 使用;defer 不可外抛,不需要新增函数清理标记。只补生成 C 的汇编元数据,不增加正常路径指令、清理栈操作、ARC、runtime API 或 ABI | 默认模式的正常退出、展开中清理及嵌套捕获全部通过;生成 C 的 `-O2` 编译通过。release 异常行为限制单独交由 S11 处理 |
| S09（M05 断点复验） | 获批迁移误用了无用的结果绑定;同一行出现两个原生断点位置。分行／块表达式改写曾使旧断言通过,但人工指出这不是正确处理 | 现行 try/catch 语句本就丢弃返回值,改回 `try println(...) catch { Process.exit(1); }`。该简单调用每轮不应因生成的辅助代码额外停顿;不能推导为所有同行多语句一律只停一次。继续核查通用源位置映射,不以无用绑定、`if true` 或改断言回避问题 | 语句及多行分支 DAP 验证通过;整体源映射问题见 S12 |
| S10（release 验证） | release FCTS 在重编 std 时报告 CFI 指令不位于 `.cfi_startproc`／`.cfi_endproc` 内,定位到仅设置 `_dispatchingMouse` 的 defer helper | 该 helper 优化为无调用的叶函数,宿主编译器不生成展开区间。仅在 helper 自身实际生成 catch 区间时输出 CFI;生成器记录当前函数是否有区间,嵌套 Lambda 保存／恢复该状态,纯清理保持原输出。不通过增加运行时函数调用或栈标记强行制造非叶函数 | 新增宿主 C `-O2` 编译验证通过,完整 release 构建通过;运行时发现的独立缺陷见 S11 |
| S11（release 执行） | 额外 release 行为验证发现既有异常展开失败,已确认早于阶段一 | 人工决定从阶段二拆出,完整原因、原始触发用例、历史对照及待 Review 方案见[独立修复文档](./feng-release-exception-unwind-bugfix.md) | 尚未修复;默认模式可通过的既有触发用例保持启用,阶段二单独全量回归 |
| S12（表达式断点范围复查） | 人工指出源断点正确性应覆盖所有表达式位置;现有新验证集中在 try 及局部绑定 | 盘点既有 DAP 矩阵并实测赋值、返回、实参、条件等位置,采用共用局部存储调试用途规则替换语法及消费位置补丁 | 新增真实 DAP 矩阵、完整 CLI／LSP／DAP、Codegen 及沙箱外 `make test` 两轮全量回归通过 |
| S13（全量回归） | 沙箱外 `make test` 的 UBSan 阶段中,semantic、runtime、Codegen 通过;独立 `test_debug` 在 `test/debug/test_debug.c:403` 的条件声明数量断言失败 | 原测试只按行首 `bool _fcond` 识别生成 C 声明;S12 共用局部存储规则在声明前增加调试属性宏,旧扫描器因此漏计。适配声明识别,保留两个结果、两个分支分别属于源码第 4／7 行及宿主 C 编译的全部断言;不为测试恢复内部局部的调试信息 | 人工要求直接修复;扫描适配后独立 Debug 及沙箱外 `make test` 两轮全量回归通过 |

S09 早期核验与修补记录（相关局部补丁已由 S12 共用实现替代）：语句形式仍重复命中。
LLDB 将同一源码行绑定到 helper 的 `+96`
（异常区间注册）和 `+248`（受保护表达式求值）两个地址。前者是编译器生成的
异常处理准备代码,不代表额外的用户语句。去除准备代码的位置后,原生断点还会定位到
同一行的 try 标记弹出。因此修复覆盖完整的异常处理辅助代码：注册、标记、landing
分发和区间收尾使用无源码位置,受保护表达式及 catch 内用户语句保留源码位置;
复用现有生成片段重映射抽象,不改变机器执行逻辑。

修复后原生 LLDB 对同一行的语句、表达式探针均只绑定一个位置。增加真实 DAP
用例验证普通函数、结果绑定、defer 和实际 catch 的每次求值命中次数,同时保留
M05 原有断言。此验证与会清空 `temp/` 的完整 CLI／Codegen 套件串行进行。
人工进一步要求验证多行 `if`／`match`：条件或匹配入口、各分支的用户语句分别保留
源码停点,命中序列随实际分支选择变化,不能把整个受保护表达式压成一个停点。

新增真实 DAP 用例发现泛型调用结果绑定仍多停一次：受保护表达式与最终结果绑定
处于不同生成 C 词法块,同一源码行因此分裂。曾尝试去掉受保护表达式的 C 包装块,
18 次 DAP 命中序列通过,但随后的跨包泛型用例报告跳转跨过变长数组初始化：该块
还负责隔离共享体的动态临时存储,不能删除。恢复原 C 作用域和执行逻辑,继续从
表达式求值及生成结果搬运的源位置区分入手修复;不能用取消存储作用域解决调试问题。
结果汇合槽（`if`／`match`／`try` 共用）已完成求值,后续局部绑定只搬运结果并登记
资源,不应再次形成该初始化语句的停点。由内部表达式结果携带这一事实,绑定生成器
据此将搬运片段标为无源码位置;不根据被调用函数、结果类型或表达式行数判断。
保留用户变量的调试记录、全部 C 作用域及存储／清理代码,并用真实 DAP 和跨包 C
编译验证两方面约束。
复查时补齐内部表达式结果新增标志的初始化／释放复位,避免普通表达式读取未初始化
的调试状态;以修正后的构建重新执行完整 CLI 验证。

S12 实测记录（当前工作区编译器、macOS arm64、debug 构建、原生 LLDB）：

- 28 个单行位置各执行两次。普通调用、普通赋值、数组字面量、下标访问各命中 2 次;
  `try` 的语句及当前局部绑定修补路径各 2 次;`return try ...`、`target = try ...`、
  `consume(try ...)`、`1 + (try ...)` 各 4 次,说明局部绑定修补没有解决通用消费位置。
- 多行 `if`／`match` 在返回、绑定、赋值、实参、算术运算共 10 个入口位置各执行两次,
  每处均命中 2 次。不能把未覆盖的位置推断为正确,也不能声称已确认所有 if/match 有问题。
- 同行 if/match 包含条件与分支用户语句时也观察到多次命中;这与单条语句展开出的
  辅助代码不同,不能只按次数判定错误。需同时核对源码语句、原生地址及实际执行路径。
- 现有 `#line` 只表达文件／行,没有可靠的 Feng 列映射;原生 C 词法块及内部临时变量
  仍可能影响断点解析。修复应从共用源码位置和内部存储调试表示入手,替换只在局部
  绑定消费 `is_evaluated_join` 的临时方案,不继续按 try／return／赋值逐项补分支。
- 同一探针用阶段一编译器复验,返回／赋值／实参中的重复命中同样存在。对生成 C
  仅为泛型实参及结果临时存储增加既有 `FENG_CODEGEN_NODEBUG` 后,返回、绑定、赋值、
  实参、表达式语句、算术运算中的 if/match/try 各执行两次均命中两次。条件体及短路
  仍按真实执行路径保留多个位置。此为原因隔离实验,不能按临时名字前缀实现产品修复。
- 2026-09-21 人工明确要求通用实现,允许暂不提供列级断点。现有 try 专用片段隐藏和
  `is_evaluated_join` 的绑定专用处理均已移除,不作为最终交付。
  在共用局部存储声明中记录调试用途,最终结合既有用户变量映射保留被接管的存储和
  变量读取所依赖的载体;纯内部局部统一抑制变量调试信息。源码位置继续复用现有
  文件／行锚点机制,保持 C 作用域与执行代码。主规则仅定义于
  [CLI 调试规范](../specifications/feng-cli.md#22-feng-dap),此处记录实施证据。
  验证范围包括表达式种类与使用位置的组合、同行／多行、真实分支及短路、循环与递归、
  用户变量读取、泛型存储、Lambda／defer,以及全量 `make test`。
- 共用存储规则首轮验证消除 28 个探针中的生成临时存储额外命中;真实 DAP 复验进一步
  在多行 if 的入口发现结果汇合槽和条件缓存等声明尚未全部接入该规则。
  先记录该遗漏,继续统一局部声明规则;不恢复 try 隐藏片段或修改预期命中断言。
- 声明用途审计发现具体 catch 的局部 binding 原先只登记值存储,未登记 `.fd` 用户变量
  映射。补齐相同的用户 binding 元信息,使其可见性与普通 binding 一致;不能将用户
  catch 变量错误归入纯内部临时存储。增加 catch 与捕获绑定的真实调试读取验证。
- 统一声明规则及 catch 变量映射完成后,新增真实 DAP 单行矩阵 27 个位置／54 次命中、
  多行矩阵 10 个位置／20 次命中、catch 与捕获变量读取 2 个位置／3 次命中全部通过。
  完整 CLI／LSP／DAP 套件及 Codegen 套件均通过,没有修改既有断点断言;
  此结果不能替代最终 `make test` 或 S11 的 release 验证。
- S11 后续按人工决定独立处理,调查记录与待审定边界集中在
  [release 异常展开修复方案](./feng-release-exception-unwind-bugfix.md)。

断点规则参考：[LLDB 逻辑断点与多个位置](https://lldb.llvm.org/use/tutorial.html)、
[.NET 可见／隐藏序列点](https://github.com/dotnet/runtime/blob/main/docs/design/specs/PortablePdb-Metadata.md#sequence-points-blob)、
[VS Code 列断点](https://code.visualstudio.com/docs/debugtest/debugging#inline-breakpoints)。
行断点、精确位置断点、单步是不同操作;不能以相同行号去重来掩盖映射问题。

S06 首次构建的集合查询误用了不存在的辅助函数名;改用既有 `cg_defer_name_in_list`,
不新增重复的名称集合实现。

S08 的新增代码生成成本断言首次把 `feng_frame_pop` 一律排除,误伤既有 try 标记的
正常弹出;改为核对不生成函数级 `FengFrameMarker`／`feng_frame_push`,保留内部 try
所需的原有清理操作。

S04 拟修复范围：将 Lambda 既有的 reification context 提取为内部共享抽象,统一生成
环境字段、初始化及入口恢复,供 Lambda 和 defer 使用。保留已实现的值存储捕获规则;
涵盖顶层／owner／方法泛参、无值捕获但使用泛参的清理以及清理中的 Lambda。
2026-09-21 人工已批准 S04 在阶段二一并修复及下述开销。上下文中仅保存既有只读描述符／泛型参数表指针,无堆分配或 ARC 操作,不修改 runtime
或函数 ABI。普通非泛型 defer 不增加字段;泛型 defer 的栈环境最多增加 P + 3 个指针
（P 个泛参描述符及存在时的 owner、callable、generic-arguments 指针）,并增加对应赋值。
这是代码生成修复,与单纯边界检查分别记录;实施后补齐生成 C、实际行为及跨包验证。

#### 5.3.1 规范与方案

- [x] 按 §4.3.2 已确认的严格原则细化 defer 边界检查：未知及未消除的开放条件不得放行，内部完整捕获后可证明为空；覆盖间接调用、catch 内再次传播及现有直接 `throw` 禁令。
- [x] 定义编译期诊断码、定位与相关调用位置；CLI 与 LSP 使用同一检查结果。
- [x] 盘点现有 defer 调用和测试受新限制影响的范围，列出迁移方案；涉及修改既有用例的内容先取得人工确认。
- [x] 更新 defer 与异常主规范的对应规则，本文仅引用；阶段二边界规则及 S04 泛型环境开销已获批准。S11 后端／runtime 方案移至独立文档 Review。

#### 5.3.2 实现

- [x] 在 defer 边界复用统一摘要计算可能逃逸的异常，覆盖块内调用、表达式求值及其内部 catch，不另建独立的函数异常推导。
- [x] 接入编译期拒绝与 LSP 错误展示，正确报告具体异常和无法证明安全的部分；不依赖用户开启编辑器提示才能阻止 N08。
- [x] 按已审定方案完成受影响代码及用例迁移，保留合法的 defer 内部异常处理和普通函数异常传播。
- [x] 将 §2.3 保留的 N08 复现纳入可执行验证；禁止逃逸方案对应编译期负例，原注释源码和候选断言继续保留供追溯。

#### 5.3.3 用例与定向验证

- [x] 建立阶段二覆盖清单，逐项对应合法／非法源码、诊断位置、语言行为和验证结果。
- [x] 在 `test/` 验证拒绝路径：直接 `throw expr`／`throw;`、直接和多层调用逃逸、部分或错误类型捕获、catch 中调用再次逃逸、外围 catch 无法封闭 defer 边界。
- [x] 验证接受路径：已证明不抛异常的调用、具体 catch 完整覆盖、未知来源由内部兜底 catch 完整处理且处理代码不再逃逸、被调用函数内部已经完整处理异常。
- [x] 覆盖递归、泛型函数与类型／方法泛参、Lambda、callable 值、spec 方法、泛型约束和跨包调用；已知、未知及混合摘要均按审定规则验证。
- [x] 验证 N08 原始复现及等价的跨函数、跨包、间接调用形态均被正确阻止，不再进入损坏 cleanup chain 的执行路径。
- [x] 在 `fcts/` 验证合法清理行为：正常退出、return、break／continue、异常展开、多个 defer 的 LIFO 顺序、内部捕获及嵌套 catch 上下文恢复、载荷析构次数和时机。
- [x] 在 `test/` 验证 CLI／LSP 诊断一致，以及修改被调用函数、捕获范围或依赖摘要后诊断的更新和清除。
- [x] 执行全部阶段二定向验证，核对阶段一的摘要和非强制提示不回退；新增问题先记录、再分析、再修复，不确定事项交由人工决策。release 异常展开问题按人工决定移交 S11。

#### 5.3.4 阶段二独立交付

- [x] 完成定向验证后，再次在沙箱外执行 `make test` 全量回归，记录本阶段独立的环境、命令、日志和各目标结果。
- [x] 完成阶段二范围内的问题修复、必要复验及全量回归，逐项核对 N08 与合法 defer 行为；S11 按人工决定独立跟踪。
- [x] 回填阶段二覆盖清单、N08 处理结论及交付记录，提供英文 commit message，不自动提交。
- [x] 独立交付 defer 修复，等待人工 Review。

#### 5.3.5 阶段二覆盖对应

| 维度 | 用例与核验内容 | 定向结果 |
| --- | --- | --- |
| 源码边界 | `test/semantic/test_defer_exception_effects.c`: 80 个场景,覆盖直接／多层／递归调用、未知和已知 callable、普通与静态成员、初始化、迭代、泛参、数组元素 witness、内部／外围 catch、重抛和直接语句禁令;另核对每个错误的关键字位置及相关调用 | 整个 semantic 套件通过 |
| 跨包证明 | `test/symbol/test_defer_exception_effects.c`: 两种 FT profile 各 39 个 consumer 场景,销毁 producer AST 后读取,重复使用导入缓存;覆盖泛型代入、owner／方法泛参、callback、witness、初始化和内部捕获 | 整个 symbol 套件通过 |
| CLI／LSP | `test/cli/defer_exception_effects.inc`: 6 个源码场景分别验证 frontend、direct、check text／JSON;同一 LSP 会话 7 次编辑验证错误更新／清除,源码隐藏的两级依赖包验证泛型传播。保留阶段一安静输出及 Hover 断言 | 整个 CLI／LSP／DAP 套件通过 |
| 生成 C | `test/codegen/test_defer_generic_context.c`: 源码及两种 FT profile 的泛型函数、类型／方法／fit、构造泛型参数、无值捕获、嵌套 Lambda、静态 witness、标量／托管／聚合表示;宿主 C 使用 `-Werror`,另核对异常元数据且不增加函数清理标记 | 整个 codegen 套件通过 |
| 实际行为 | `fcts_bin/src/test_defer_exception_effects.ff` 的 23 个场景及 `fcts_lib/src/test/lib_defer_exception_effects.ff`: 正常、return、break／continue、LIFO、展开／landing 清理、内部动态和具体捕获、嵌套处理器、重抛、泛型跨包／闭包、完整聚合值及析构次数 | 默认模式全部通过,23 个用例继续保留;完整 release 异常行为由 S11 独立验收 |
| 既有用例迁移 | M01–M05 均获批准;原 N08 注释源码和断言未删除,原 landing 外抛场景完整注释保留。原 DAP 循环断点断言验证 12 场景、24 次命中、未执行分支 0 次 | 原断言通过 |
| println 类型事实 | 重编 std 后,纯字符串重载无逃逸;数值 `println<int>`、直接泛型值及泛型数组元素调用均得到 `string`,没有多余 unknown;真正动态来源的 unknown 由上述源码及 FT 用例保留验证 | 定向检查符合预期 |

以上定向验证之外,完整回归记录见 §5.3.6。阶段二实现、覆盖及 S11 拆分后的
交付回归均已完成。S11 尚未修复,保留在独立文档中跟踪。

#### 5.3.6 全量回归记录

2026-09-21,macOS arm64,在沙箱外执行 `make test`,退出码为 0。
UBSan 与正常构建两轮均完成所有 Makefile 回归目标,每轮 smoke 91／91、
标准库 607／607、FCTS 1,492／1,492,性能约束检查通过;编译器、Debug、
CLI／LSP／DAP、符号及构建／打包相关套件均通过。日志未出现 UBSan runtime error
或断言失败,原断点数量、行号和行为断言均保留。

本机完整日志：`/private/tmp/feng-defer-stage2-full-make-test-after-debug-fix.log`。
S13 首次失败日志另行保留,未以成功日志覆盖。上述 FCTS 按 `make test` 的默认
模式运行,不包含 S11 所用的 release 异常行为验证。本次没有加入相关
`noinline`、`optnone` 或 `asm goto` 产品实现;S11 后续独立处理。

S11 拆分后的交付回归（2026-09-21）：再次在沙箱外执行完整 `make test`,退出码为 0。
BIND10 原用例及其断言保持启用,其文件与 HEAD 无差异,两轮均实际通过;
本次拆分没有减少 FCTS 活跃用例,阶段二的 23 个 defer 行为用例全部保留并通过。

| 验证项 | 本次结果 |
| --- | --- |
| UBSan 与普通构建 | 两轮全部 Makefile 目标通过,无 UBSan runtime error、断言失败或 make 失败 |
| 编译器与工具 | semantic 的 80 个源码场景、两种 FT profile 各 39 个 consumer 场景、Codegen、Debug、CLI／LSP／DAP、Symbol 等套件全部通过 |
| smoke | 每轮 91／91 |
| 标准库 | 每轮 607／607,失败与跳过均为 0 |
| FCTS | 每轮 1,492／1,492,失败与跳过均为 0；包括 BIND10 原用例及全部新增 defer 行为 |
| 性能及构建发布 | 两轮性能约束检查通过；完整回归中的增量构建、release 脚本、打包及工具链测试目标通过 |

完整日志：`/private/tmp/feng-defer-stage2-delivery-regression.log`。
运行前后产品代码及测试指纹一致：
`c9faceefb296254fd7df0b53b221430fb5c8406e83b0c0d1d76644258c34891a`
（`git diff HEAD -- src test fcts std scripts | shasum -a 256`）。
结束后仅回填文档。本次结果不包含完整 release 异常行为通过的结论。

#### 5.3.7 交付结论与建议 commit message

阶段二的 defer 边界检查及相关修复已经完成，N08 的非法外抛由编译期拒绝；
合法的内部捕获、泛型共享体、跨包调用和清理生命周期按 §5.3.5 验证。
原 N08 及获批迁移的 landing 外抛源码仍完整注释保留，对应编译期负例继续执行。
通用源码断点修复及既有断言均已通过，不保留 try／绑定位置专用补丁。

本阶段没有修改 runtime 源码、运行时结构布局、函数 ABI 或 FT 格式，未新增 Python 文件。
泛型 defer 的描述符栈环境成本按 S04 已获批准的范围实施。
S11 按人工决定移至[独立修复方案](./feng-release-exception-unwind-bugfix.md)，尚未修复。
代码未自动提交，等待人工 Review。

```text
fix: enforce defer exception boundaries and preserve cleanup context

- Reject concrete, unknown and unresolved generic exception escapes with AE1507
- Preserve generic descriptors, captures and EH metadata in defer helpers
- Record generic array element type facts for exception inference
- Use shared local-storage debug metadata for consistent source breakpoints
- Cover source, FT, CLI/LSP/DAP and cross-package cleanup behavior
- Document the existing release unwind defect separately as S11

Validation: make test passed in UBSan and normal builds, with 91 smoke,
607 standard-library and 1492 FCTS cases passing in each round.
```
