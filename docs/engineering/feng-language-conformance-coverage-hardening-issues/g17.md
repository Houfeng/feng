# G17 实施问题记录

> 所属实施文档：[Feng 语言正确性用例补齐实施文档](../feng-language-conformance-coverage-hardening-pending.md)
>
> 交付组：G17

## ISSUE-G17-001：主规范和用户手册仍描述可省略 `catch`

### 归属

- 发现组：G17；
- 关联组：G12；
- 发现用例：EXC01～EXC03 的实施前语法、规范与历史核对。

### 现象与分析

- 当前 Parser 在 `try` 后没有任何 `catch` 时产生 `SE1401`，G12 已有诊断码、token、行列、唯一
  诊断和空 AST 的直接证据；
- 实施前 `feng-exception.md` 和中英文用户手册仍写明 `catch` 可以省略并自动传播，和实际语法矛盾；
- Git 历史显示，后续提交 `2ac095fd` 的 commit message 为
  `feat(parser): forbid try without catch clause`，因此实际 Parser 行为代表较晚的语法决策；
- 未被当前函数捕获的异常本来就会沿普通调用链传播，不需要引入无 catch 的 `try` 作为传播标记。

### 决策与处理

- 状态：已关闭；
- 分类：文档问题；
- 人工确认：每个 `try` 必须至少包含一个 `catch`；
- 处理：删除“省略 catch”条款和示例，对齐主异常规范、中英文用户手册及 G17 TODO；Parser、AST、
  Semantic、Codegen 和现有测试均不改动；
- 是否涉及运行时性能：否；
- 是否涉及 runtime 私有 ABI、公开 ABI 或 `.ft` 格式：否。

## ISSUE-G17-002：值形式 catch 错误地把 `return` 当作分支结果

### 归属

- 发现组：G17；
- 关联组：G15、G16；
- 发现用例：EXC12～EXC16 的实施前语义和当前实现核对。

### 现象与分析

- 当前 Semantic 的 `block_terminal_return_value()` 会把值形式 catch 的末尾 `return expr` 提取为结果，
  `validate_try_expr()` 和 `validate_try_catch_clause_result_presence()` 又明确把它作为合法结果；
- Codegen 随后按真正的 `return` 发码，因此以下程序进入 catch 时直接返回外围函数，而不是让 try
  表达式产生 `1`：

  ```feng
  func run(): int {
      let value = try fail() catch {
          return 1;
      };
      return value;
  }
  ```

- 现有 Semantic、Codegen、smoke 和 FCTS 中已有多处正向断言固定了该实现行为；
- 值形式 `if` / `match` 当前会因末句 `return` 不属于结果表达式而拒绝最简单形态，但若在 `return`
  后再写一个不可达结果表达式，仍可能通过结果提取。三种值表达式尚未真正共用“不得返回外围
  callable”的完整边界；
- `return` 是 callable 控制转移，不是块结果表达式。只移除 try 的末尾提取特例仍不能覆盖普通嵌套
  block、条件、循环或“return 后有不可达结果”的情况，也会留下三种值表达式不一致。

### 决策与处理

- 状态：已关闭；
- 分类：Semantic 产品缺陷、既有用例语义迁移；
- 人工决策：值形式 `try` 与值形式 `if` / `match` 一致，结果分支禁止目标为外围 callable 的
  `return`；普通嵌套结构不改变目标，嵌套 Lambda 自己的 `return` 合法；语句形式的 `if`、`match`
  和 `try/catch` 均不形成该限制，其分支可返回当前 callable；
- 实施必须建立三种值表达式共用的编译期 callable-return 边界，不得只扫描 catch 末句，也不得给
  try 添加独立特判；边界只能在表达式 AST 上生效，不得改变三种语句 AST 的语义、诊断或发码；
- 值形式 catch 的非法 return 使用 `AE1405` 并定位实际 `return` token；if / match 使用共同的
  `AE1110` 分支控制转移段；catch 缺少正常结果的 `AE1401` 文案删除 `'return'`；
- 人工已批准实施文档 §21.3 明列的既有测试迁移；实际迁移严格限制在该清单及其对应断言内，没有
  修改其他既有用例；
- 已删除 `block_terminal_return_value()` 及其在结果选择、目标贴合和 catch 完整性检查中的使用，
  三种值表达式复用 `ExpressionBranchBoundary` 检查外围 callable return；新增 direct、末尾仍有
  结果、普通 block、条件、match、循环和嵌套 try 的精确反向用例，并建立语句形式与嵌套 Lambda
  的正向 Semantic/FCTS 证据；
- 是否涉及运行时性能：否；只增加编译期上下文检查，合法程序不得新增运行时判断、分支、调用、
  分配或 ARC；
- 是否涉及 runtime 私有 ABI、公开 ABI 或 `.ft` 格式：否。

## ISSUE-G17-003：跨 ABI 异常效果分析延期

### 归属

- 发现组：G17；
- 关联领域：ABI、package-public `.ft`、异常传播分析；
- 发现位置：原 G17 的“异常声明 / 调用方传播要求”范围核对。

### 决策

- 状态：延期；
- Feng 普通 callable 没有 checked-exception 声明，普通调用方也没有必须满足的异常声明要求；
- 异常跨 `@abi` 边界的静态效果分析以及相关事实是否需要跨 package-public `.ft` 保留，需要另行设计
  和人工决策；
- 本次 G17 不修改 `AE1313`、异常效果分析、`.ft` 格式、公开 ABI 或 runtime 私有 ABI，也不以既有
  ABI 用例推导该领域已经完整覆盖；
- G17 只覆盖普通 Feng 调用链的传播、捕获和重抛。若全量回归暴露 ABI 相关失败，先记录，不在本组
  顺带修复。

## ISSUE-G17-004：专项普通链接误用上一轮 UBSan 对象

### 归属

- 发现组：G17；
- 发现步骤：完成表达式分支边界首轮实现后的 Semantic 专项构建。

### 现象与分析

- `src/semantic/analyzer.c` 已使用普通 `-Werror` 参数编译成功，但链接 `build/bin/test_semantic` 时出现
  多个 `__ubsan_handle_*` 未定义符号；
- 当前 `build/obj` 来自此前 UBSan 构建，本次普通增量链接没有 UBSan `LDFLAGS`，属于两种构建参数
  共用可再生对象目录造成的本地构建状态不一致，不是 Feng 源码或 G17 语义失败；
- Makefile 的 `clean` 目标只删除仓库内可再生的 `build/` 目录，不涉及源码、测试或用户数据。

### 处理

- 状态：已关闭；
- 已执行 `make clean` 清除混合参数对象，并用一致的普通参数重建、运行 Semantic 专项；专项通过；
- 沙箱外完整 `make test` 的 UBSan 与 normal 两阶段随后均以退出码 `0` 通过；
- 是否涉及产品代码、运行时性能或 ABI：否。

## ISSUE-G17-005：`unknown` catch 绑定在普通 `throw` 子表达式中被错误放行

### 归属

- 发现组：G17；
- 发现用例：EXC10 的 `unknown` catch 绑定使用位置反向矩阵。

### 现象与分析

- 规范仅允许 `throw error` 原样重抛 `catch error: unknown` 捕获的原始异常；
- 实施前 Semantic 在解析 `throw` 的整个操作数表达式时增加 `unknown_value_depth`，所以不仅直接
  `throw error` 合法，`throw identity(error)`、`throw error + 1` 等普通子表达式也会绕过 `AE1404`；
- 普通子表达式会消费、转换或重新包装该值，已经不具备“原始异常对象直接重抛”的语义，且
  `unknown` 没有可供此类运算使用的 Feng 类型。

### 决策与处理

- 状态：已关闭；
- 分类：Semantic 产品缺陷；
- 处理方案：以 AST 身份记录当前 `throw` 允许直接重抛的操作数；只有该操作数节点本身就是
  `unknown` catch 绑定时放行，嵌套在调用参数、成员接收者、普通 throw 子表达式或其他运算中的
  标识符仍产生 `AE1404`；
- 新增直接原样重抛正向用例，以及绑定初始化、参数、返回值、成员接收者、普通 throw 子表达式和
  运算反向用例；
- 已以 `allowed_unknown_rethrow_expr` 记录直接操作数 AST；Semantic 专项和 FCTS 原样重抛行为均通过；
- 是否涉及运行时性能：否；变更只收紧编译期名称使用检查；
- 是否涉及 runtime 私有 ABI、公开 ABI 或 `.ft` 格式：否。

## ISSUE-G17-006：获批 Lambda 用例迁移后暴露 `int` / `i32` 主路径差异

### 归属

- 发现组：G17；
- 发现步骤：运行迁移后的 FCTS；
- 发现用例：`g15_nested_try_lambda_argument`，属于实施文档 §21.3 已获批迁移清单。

### 现象与分析

- 原用例的 Lambda 目标为 `G15I32Picker(...): i32`，`try` 主表达式
  `g15_maybe_throw(inner)` 的静态结果为 `int`；
- 旧 catch 中的 `return 78` 会直接退出 Lambda，主表达式正常完成后则执行后续 `throw`，因此旧
  控制流不要求 `try` 的 `int` 主结果与 Lambda 的 `i32` 返回目标贴合；
- 初次迁移为 `return try ... catch { 78; };` 后，主表达式结果也成为 Lambda 返回值，Semantic
  正确拒绝 `int` 到 `i32` 的隐式非字面量转换，调用点因此产生 `AE0512`；这不是 G17 分支边界回归。

### 处理

- 状态：已关闭；
- 在已获批迁移点将主表达式写为 `(i32)g15_maybe_throw(inner)`，使正常路径与 catch 字面量共同贴合
  `i32`，同时继续验证参数位置 block Lambda、嵌套 try 和固定宽度返回目标；
- 对应 FCTS 断言文案由“catch return”对齐为“catch result”，执行结果仍为 `78`；
- 修正后 FCTS 全部 `1123/1123` 通过；
- 是否涉及产品实现、运行时性能或 ABI：否，仅修正已获批迁移用例的显式类型关系。
