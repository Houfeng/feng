# G12 实施问题记录

> 所属实施文档：[Feng 语言正确性用例补齐实施文档](../feng-language-conformance-coverage-hardening-pending.md)
>
> 交付组：G12

## ISSUE-G12-001：用户不可达的 SE 发码点不应驱动 G12 用例或错误码重构

### 归属

- 发现组：G12；
- 关联组：无；
- 发现用例：Parser 稳定诊断实施前的源码、规范与现有测试反向审计。

### 现象与结论

- 最小复现：沿 `feng_parse_source`、`parse_program` 和各静态 Parser helper 的唯一调用链，核对每个
  `SE` 发码点的调用前置条件；
- 实际结果：当前实现存在 15 个用户输入不可达的 `SE` 发码点，涉及 13 个不同错误码。其中 10 个
  由调用方 token、循环退出条件或数量上限保证，是纯内部不变量防御；1 个 lambda arrow 诊断被
  候选识别条件遮蔽；4 个 `extern` 相关下层诊断被顶层“`extern` 只能跟 `func`”分派提前截获；
- 规范与实现另有 `SE0311`、`SE1107`、`SE1404` 三个当前没有实现发码点的条目；连同仅存在于不可达
  分支的 `SE0306`、`SE0401`、`SE0513`、`SE0516`、`SE0801`，共 8 个现行条目没有用户可达路径；
- 结论：这些位置不能作为“缺少反向用例”的依据。为它们制造源码用例不可行；把内部防御批量改为
  `IE`、改变 lambda 识别或重排 `extern` 诊断会把 G12 扩大为错误码和 Parser 分派重构，不符合本组
  以正向/反向语法覆盖为主的目标。

### 决策与实施

- 状态：已关闭；
- 分类：既有测试问题；
- 建议方案：G12 只覆盖当前用户输入可达的语法分支；不可达发码点保留审计记录，不计入覆盖率，
  不在本组修改。后续如需收敛 `SE` / `IE` 分类、失效错误码或改善 lambda/`extern` 专项诊断，应另立
  独立任务并由人工决定用户可见诊断兼容策略；
- 是否涉及既有测试修改：否；G12 只新增独立测试；
- 是否涉及运行时性能：否；本问题不修改产品实现；
- 是否涉及 runtime 私有 ABI、公开 ABI 或 `.ft` 格式：否；
- 人工决策与批准范围：已确认 G12 的关键目标是更全面覆盖正向和反向用例，使核心编译器及后续变更
  更有保障；错误码具体取值和不可达防御重构不是本组重点；
- 实际变更：仅在 G12 实施文档和本问题文件中固定覆盖边界，未修改 Parser 或错误码主规范。

### 验收与关闭

- 本组专项复验结果：`make build/bin/test_parser && build/bin/test_parser` 通过；不可达分支未进入新增
  反向用例，所有新增失败源码均为直接调用 `feng_parse_source` 可达的 Parser 路径；
- 本组沙箱外 `make test` 结果：通过，退出码 0；UBSan 与 normal 两个干净阶段均完成；
- 关闭依据：人工已确认上述范围；本问题的处理目标是阻止不可达分支驱动伪用例或大范围错误码改动，
  该范围已写入实施文档。若未来启动诊断分类收敛，应使用新的独立问题和交付范围。

## ISSUE-G12-002：`else if` 的简单标识符条件会把后续空块误解析为对象字面量

### 归属

- 发现组：G12；
- 关联组：无；
- 发现用例：PARSE09 / PARSE13，`test_g12_control_flow_and_try_syntax` 正向用例。

### 现象与结论

- 最小复现：

  ```feng
  let ready = false;
  let retry = true;

  if ready {
  } else if retry {
  } else {
  }
  ```

- 预期结果：按流程控制主规范解析为两个条件分支和一个 `else` 分支；`retry` 是第二个条件，紧随的
  `{}` 是该分支的空 block；
- 实际结果：Parser 在第二个 `else if` 条件后面的 `else` token 处报告 `SE0005 expected '{' to start
  block`；
- 原因：`parse_if_statement()` 对首个条件使用 `parse_expression_before_block()`，会在控制流 head 中
  抑制未分组对象字面量 suffix；但后续 `else if` 条件直接调用 `parse_expression()`。当条件末尾是可接
  对象字面量的标识符且 block 为空时，`retry {}` 被整体吞成对象字面量，之后才尝试解析分支 block；
- 规范依据：`docs/specifications/feng-flow.md` 第 1、2 节已经明确支持 `if / else if / else`，并要求
  `if` 与 `else if` 使用相同的 bool 条件规则；本问题不需要修改语言规范。

### 决策与实施

- 状态：已关闭；
- 分类：产品缺陷（既有编译 Bug）；
- 建议方案：后续 `else if` 条件与首个 `if` 条件统一调用现有
  `parse_expression_before_block()`，不增加新的语法特判；
- 是否涉及既有测试修改：否；新增独立 G12 正向用例固定简单标识符条件、空 block 和三分支 AST；
- 是否涉及运行时性能：否；只调整编译期 Parser 的既有入口复用，不改变生成代码和 runtime；
- 是否涉及 runtime 私有 ABI、公开 ABI 或 `.ft` 格式：否；
- 人工决策与批准范围：用户已批准补齐 G12，并要求增补用例发现的编译 Bug 在记录、分析后解决；该
  修复遵循既有明确规范且不引入运行时开销，不存在待选择语义；
- 实际变更：把 `parse_if_statement()` 的后续 `else if` 条件从 `parse_expression()` 切换为与首个条件
  相同的 `parse_expression_before_block()`；新增正向用例核对两个 clause、最终 `else` block 和简单
  标识符条件。

### 验收与关闭

- 本组专项复验结果：`make build/bin/test_parser && build/bin/test_parser` 通过，新增最小复现已解析为
  两个 `FengIfClause` 和一个最终 `else` block；
- 本组沙箱外 `make test` 结果：通过，退出码 0；UBSan 与 normal 两个干净阶段均完成，FCTS
  1072/1072、smoke 90/90、std 601/601；
- 关闭依据：规范行为、正向 AST、既有 Parser 测试及完整全量回归全部通过；变更只影响编译期解析，
  不增加生成程序运行时开销。

## ISSUE-G12-003：直接书写的 `if` 表达式 `else if` 链被 Parser 拒绝

### 归属

- 发现组：G12；
- 关联组：无；
- 发现用例：PARSE09 / PARSE13，`test_g12_expression_and_postfix_syntax` 正向用例。

### 现象与结论

- 最小复现：`let result = if first { 1 } else if second { 2 } else { 3 };`；
- 预期结果：合法 `if` 表达式；条件按顺序判断，AST 可归一化为根 `if` 的 `else` block 中嵌套第二个
  `if` 表达式；
- 实际结果：`parse_if_expression()` 消耗 `else` 后只接受 `{...}`，在 `if` token 处报告 `SE0005
  expected '{' to start block`；
- 原因：statement form 已用 clause 数组支持 `else if`，且结果分支末尾的 statement-form `if` 链已
  能转换为嵌套 `FENG_EXPR_IF`；但直接 expression form 没有把 `else if` 归一化到同一既有 AST；
- 规范依据：`docs/specifications/feng-flow.md` 第 1、2 节定义 `if / else if / else`，第 4.2 节定义
  `if` 可作为表达式并支持前述 `if` 形式。

### 决策与实施

- 状态：已关闭；
- 分类：产品缺陷（既有编译 Bug）；
- 建议方案：`parse_if_expression()` 在 `else` 后遇到 `if` 时递归解析嵌套 `FENG_EXPR_IF`，再用单一
  expression statement 的 block 承载它；普通 `else {...}` 保持现有路径；
- 是否涉及既有测试修改：否；新增独立 G12 正向用例核对根/嵌套 `FENG_EXPR_IF` AST；
- 是否涉及运行时性能：否；该 AST 与用户显式写成 `else { if ... }` 的既有形式相同，不新增语义、
  codegen 分支或生成代码开销；
- 是否涉及 runtime 私有 ABI、公开 ABI 或 `.ft` 格式：否；
- 人工决策与批准范围：用户已批准按规范补齐 G12，并要求增补用例发现的编译 Bug 在记录、分析后
  解决；主规范和既有 AST 对预期行为均已有明确依据，不存在待选择语义；
- 实际变更：`parse_if_expression()` 在 `else` 后识别直接 `if`，递归构造嵌套 `FENG_EXPR_IF`，并以
  单一 expression statement block 承载；新增正向用例核对根与嵌套节点。普通 `else {...}` 路径未改。

### 验收与关闭

- 本组专项复验结果：`make build/bin/test_parser && build/bin/test_parser` 通过，直接 `else if`
  expression 已归一化为预期嵌套 AST；
- 本组沙箱外 `make test` 结果：通过，退出码 0；UBSan 与 normal 两个干净阶段均完成，FCTS
  1072/1072、smoke 90/90、std 601/601；
- 关闭依据：主规范、正向 AST、既有 Parser/Semantic/Codegen 路径及完整全量回归全部通过；生成 AST
  与显式 `else { if ... }` 相同，不增加生成程序运行时开销。

## ISSUE-G12-004：新增合法 Parser 边界缺少直接 FCTS 行为证据

### 归属

- 发现组：G12；
- 关联组：无；
- 发现用例：PARSE16 / PARSE17，G12 追加验收时对 Parser 正向源码与现有 FCTS 的映射复核。

### 现象与结论

- 最小复现：`test_g12_control_flow_and_try_syntax` 已证明语句形式
  `if ready {} else if retry {} else {}` 能生成正确 AST；
  `test_g12_expression_and_postfix_syntax` 已证明表达式形式
  `if first { 1 } else if second { 2 } else { 3 }` 能归一化为嵌套 `FENG_EXPR_IF` AST；
- 实际结果：现有 FCTS 已分别覆盖普通语句 `else if`、两分支 `if` 表达式，以及 `if` / `match` /
  `try` 表达式的上下文贴合和无目标推导，但没有直接书写的 `if` 表达式 `else if` 链，也没有以真实
  运行用例覆盖简单标识符条件紧邻空 block；
- 结论：Parser AST 只能证明语法接受与树形，不能独立证明三个结果块的选择、目标类型向嵌套链的
  传播，以及各结果块参与推导或被 `throw` 排除的行为。G12 的正向证据需要补充 FCTS；其余新增
  合法语法结构应映射到已有 FCTS，不重复建立同义用例。
- 规范依据：`docs/specifications/feng-flow.md` 第 2 节定义 `else if` 的顺序选择，第 4.1 节统一定义
  `if` / `match` / `try` 结果分支的目标选择与贴合，第 4.2 节规定 `if` 表达式必须具有结果和 `else`。

### 决策与实施

- 状态：已关闭；
- 分类：既有测试缺口；
- 建议方案：新增独立 G12 FCTS 文件，覆盖语句空 block、直接表达式链的三个可达结果块、完整上下文
  目标、无目标推导中非字面量锚点位于三个不同块、全数值字面量默认推导和三个位置的 `throw` 排除；
  同时在实施文档记录其他 Parser 正向结构到已有 FCTS 的行为映射；
- 是否涉及既有测试修改：只在 FCTS 入口登记新的独立测试函数，不改写既有测试用例；
- 是否涉及运行时性能：否；本问题只增加测试和实施记录；如追加用例发现产品缺陷，必须另行记录、
  分析其修复是否改变生成程序路径；
- 是否涉及 runtime 私有 ABI、公开 ABI 或 `.ft` 格式：否；
- 人工决策与批准范围：用户明确要求核对直接 `else if` 表达式链的各块贴合和推导，并要求合法正向
  用例在 FCTS 中也有覆盖。
- 实际变更：新增 `fcts/fcts_bin/src/test_g12_parser_positive_semantics.ff` 及其 FCTS 入口，以 5 个独立
  运行用例覆盖语句空 block、三个结果块、完整 union-form 和四种其他上下文目标、三种非字面量锚点
  位置、整数/浮点默认推导及三个 `throw` 位置；实施文档同时建立其余 Parser 正向结构到既有 FCTS
  的分组映射。

### 验收与关闭

- 本组专项复验结果：`make fcts-tests` 通过，新增 5 个用例均实际执行，完整 FCTS 1077/1077；Parser
  专项继续通过并输出 `parser tests passed`；
- 本组沙箱外 `make test` 结果：第二次完整执行通过，退出码 0；UBSan 与 normal 两个干净阶段均完成，
  FCTS 均为 1077/1077、smoke 均为 90/90、std 均为 601/601；
- 关闭依据：PARSE16 / PARSE17 已形成 Parser AST 与 FCTS 运行行为两层直接证据，全量回归与
  `git diff --check` 通过；未发现需要修改 Semantic 或 Codegen 的产品缺陷。

## ISSUE-G12-005：全浮点字面量默认推导用例误把结果声明为 `float`

### 归属

- 发现组：G12；
- 关联组：无；
- 发现用例：PARSE17 首次执行新增 G12 FCTS。

### 现象与结论

- 最小复现：无上下文目标的直接 `else if` 表达式三个结果块分别为 `10.5`、`20.5`、`30.5`，先以
  `let value = ...` 推导，再从返回类型为 `float` 的辅助函数返回 `value`；
- 实际结果：Semantic 报告 `AE1003 expression 'value' does not match expected type 'f32'`；
- 原因：Feng 内建类型主规范明确规定，无显式目标时浮点字面量默认推导为 `double`（`f64`），而
  `float` 是 `f32` 的别名；三个结果块正确地分别默认推导为 `double`，错误来自新增测试夹具把预期
  类型写成了 `float`；
- 结论：不是 Parser、Semantic 或 direct `else if` 链的产品缺陷。

### 决策与实施

- 状态：已关闭；
- 分类：新增测试问题；
- 建议方案：把辅助函数的返回类型改为 `double`，保留先无目标推导、后通过返回检查静态类型的测试
  结构；不得给 `if` 表达式直接添加 `f32` 目标，否则会把测试含义错误改成上下文贴合；
- 是否涉及既有测试修改：否，只修正尚未交付的新增 G12 FCTS；
- 是否涉及运行时性能：否；
- 是否涉及 runtime 私有 ABI、公开 ABI 或 `.ft` 格式：否；
- 人工决策与批准范围：这是依据既有明确主规范修正新增测试预期，不改变语言语义。
- 实际变更：把新增辅助函数返回类型和名称从 `float` / `g12PickDefaultFloat` 改为 `double` /
  `g12PickDefaultDouble`；仍由无目标 `let` 先完成推导，再通过函数返回类型核对推导结果。

### 验收与关闭

- 本组专项复验结果：修正后 `make fcts-tests` 通过，完整 FCTS 1077/1077；
- 本组沙箱外 `make test` 结果：第二次完整执行通过，退出码 0；UBSan 与 normal 两阶段相同用例均
  通过；
- 关闭依据：用例现在直接符合浮点字面量默认 `double` 的主规范，并在专项和全量回归中通过；未修改
  产品实现或语言语义。

## ISSUE-G12-006：首次追加全量回归的 normal bundled init 子进程被系统终止

### 归属

- 发现组：G12；
- 关联组：无；
- 发现用例：PARSE16 / PARSE17 追加交付首次在 Codex 沙箱外执行 `make test`。

### 现象与结论

- 最小复现：首次全量回归进入 normal 阶段的 `init-bundled-packages-test` 后，脚本第 46 行依次启动的
  多个 `feng init demo ...` 子进程被系统以 `SIGKILL(9)` 终止，随后因预期生成的 `feng.fm` 不存在而
  退出；
- 已知边界：同一次全量回归的 UBSan 阶段已经完整通过相同 bundled init 检查，以及 Parser、Semantic、
  Codegen、FCTS 1077/1077、smoke 90/90、std 601/601 和性能约束；normal 阶段在此之前的 smoke
  90/90、CLI direct/project 也已通过；
- 分析结论：首次输出没有 Feng 诊断、断言失败或崩溃栈，`SIGKILL(9)` 也不是程序自行报告的失败；
  随后的独立目标和第二次完整全量回归均通过，无法复现该中断。现有证据只能将其归为外部环境的
  一次性进程终止，不能据此认定或修改 Feng 产品实现。

### 决策与实施

- 状态：已关闭；
- 分类：外部环境偶发中断；
- 建议方案：先在沙箱外独立复验 `make init-bundled-packages-test`；若通过，再重新完整执行一次
  `make test`，只有第二次全量两阶段全部通过才关闭；若稳定复现，则继续收集退出状态和最小调用，
  在未确定前不修改产品代码或测试脚本；
- 是否涉及既有测试修改：否；
- 是否涉及运行时性能：否；
- 是否涉及 runtime 私有 ABI、公开 ABI 或 `.ft` 格式：否；
- 人工决策与批准范围：用户要求 G12 完成全量回归，并要求过程中问题先记录、再分析和解决。
- 实际变更：未修改产品代码、测试用例或测试脚本；在沙箱外独立复验失败目标，并重新执行完整
  `make test`。

### 验收与关闭

- 本组专项复验结果：首次失败后独立执行 `make init-bundled-packages-test`，立即通过；
- 本组沙箱外 `make test` 结果：首次执行退出码 2；随后第二次完整执行通过、退出码 0，UBSan 与
  normal 两阶段的 bundled init、FCTS 1077/1077、smoke 90/90、std 601/601 及其余检查全部通过；
- 关闭依据：同一目标独立复验和第二次完整全量回归均无法复现 `SIGKILL(9)`，且首次失败前后的 Feng
  专项结果一致；没有证据表明是产品或测试脚本缺陷，因此按一次性外部进程中断关闭，不据此引入
  任何实现变更。
