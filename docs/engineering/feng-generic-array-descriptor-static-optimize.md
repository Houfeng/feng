# 开放数组泛型实参描述符静态化优化

## 1. 状态与范围

日期：2026-09-09。状态：独立优化记录，待 Review，尚未实施。

来源为 [G24 问题记录](./feng-language-conformance-coverage-hardening-issues/g24.md) 中的
ISSUE-G24-015。本次仅将后续优化独立成文；G24 对既有字段扩展成本的接受，不等于已经消除
运行时临时描述符，也不等于批准本专项的实现与成本变化。

本专项针对共享体内开放数组类型作为泛型实参时的描述符临时构造，不扩大为所有描述符的
通用重构。不合并 `reified_spec_view_coercions` 重命名或三张 `reified_*` 元信息表的 `_count` 补齐。

语言语义继续以 [泛型规范](../specifications/feng-generics-draft.md) 和
[类型规范](../specifications/feng-type.md) 为准；本文不新增或重定义语言行为。

## 2. 已确认的现状

### 2.1 最小示例

```feng
func identity<U>(value: U): U {
    return value;
}

func relay<T>(values: T[]): T[] {
    return identity<T[]>(values);
}
```

`relay` 接收 T 的泛参描述符；调用 `identity<T[]>` 时，内层 U 的实际类型是 T[]，因此需要
描述 T[] 的泛参描述符，而不是直接复用描述 T 的记录。

当前 [Codegen](../../src/codegen/codegen.c) 的 `cg_closed_array_descriptor_expr` 分成两条路径：

- 当前发码点的数组类型已经完整闭合，例如 `i32[]`：生成并复用静态 `FengTypeDescriptor`。
- 当前发码点仍含开放泛参，例如共享体中的 `T[]`：用传入的 `_T` 生成函数内的
  `&(const FengTypeDescriptor){...}`，其中元素泛参数组也在局部组装。
  `cg_generic_descriptor_expr` 再为这个数组类型生成相应的临时 `FengGenericParamDescriptor`。

第二条路径确实存在运行时临时描述符初始化，不是纯粹传递静态地址；这里不是创建或复制数组，
也不是运行时检查约束是否满足。宿主编译器可能优化个别调用，但不能把依赖内联或消除临时对象
作为共享体及跨包调用的实现保证。

### 2.2 描述信息不依赖实例

这组记录只依赖具化后的元素类型及数组类型属性，不依赖 `values` 的实例指针、数组长度或
元素内容。T 的描述符通过运行时参数传进共享体，并不意味着 T[] 的描述信息只能在运行时生成。

例如 `relay<i32>` 与 `relay<string>` 可分别在具化点生成 `i32[]` 与 `string[]` 的静态
描述符，再让同一个共享体通过各自的具化上下文取得正确记录。不应在共享体内缓存第一次传入的
T，也不能把含 `_T` 的局部初始化表达式机械改成一个共用的 `static` 变量。

### 2.3 为什么当前会临时构造

当前 [依赖收集](../../src/semantic/reifiable_deps.c) 的 `try_collect_type_ref` 会把
`List<T>` 这类具名泛型实例作为完整类型依赖收集；对数组节点则只递归检查元素类型，
未把本场景所需的数组节点本身作为完整类型描述符依赖接入静态读取链路。
Codegen 在这里取得的是 T 的描述符，没有直接取得具化后的 T[] 描述符，因而采用局部组装。

这是类型依赖收集、具化和读取路径未接通的问题，不是数组描述符必须依赖实例的语言要求。
历史字段增大造成的初始化与栈布局实测结果保留在 ISSUE-G24-015；那些数字不是本专项优化后
或补齐 `_count` 后的测量结果。

## 3. 优化目标与方案方向

### 3.1 目标：与普通泛型容器一致

2026-09-09 人工明确：T[] 是语言内建容器类型，应与 `List<T>` 等普通泛型容器一样，
将只依赖具化类型的描述信息在具化点静态生成。共享体内仍写作开放 T[]，不构成运行时动态
构造描述符的理由。数组与普通容器的实例布局可以不同，但本项静态具化原则应一致。

`List<T>` 仅作为普通泛型容器对照，编译器不能感知 std 或按 `List` 名称增加处理分支。

将本场景需要的数组类型描述符、元素泛参数组及相应泛参描述符在具化点静态生成，消除共享调用
中的这一组临时描述符构造。完整闭合数组现有静态路径保持有效，实际类型与生命周期语义不变。

### 3.2 复用既有具化依赖机制

建议沿用 [泛型具化设计](./feng-generic-optimize-dev.md) 的既有机制：

1. Semantic 记录共享体实际需要的完整开放数组类型依赖，保留嵌套层次、元素可写性及泛参归属。
2. 使用现有依赖归属和稳定槽位规则；类型初始化域与 callable 域分别处理，不因引用了类型级 T
   就把方法自身的依赖并入类型初始化域。
3. `.ft` 保存恢复这些依赖所需的信息，使 consumer 在没有 provider 源码时仍能完成具化；
   格式契约仅在 [符号表规范](../specifications/feng-symbol-table.md) 中定义。
4. 在最终具化点生成正确的静态描述符及依赖图。相同闭合记录可以复用，但不得破坏开放槽位对应。
5. 各层共享体通过现有描述符通道的固定槽位读取、转传静态地址；闭包若需保存描述信息，
   沿用既有捕获机制，不能捕获本次调用中的临时描述符地址。

具体复用哪类已有依赖槽、如何提供内层所需的泛参描述符，须在实施前完成设计核对；本文不预先
决定新增 runtime 字段、隐藏参数、依赖类别或 `.ft` 节，也不借用无关投影表的槽位。

### 3.3 成本和不变边界

- 不增加运行时描述符工厂、堆分配、首次调用缓存、动态类型搜索或约束满足检查。
- 不通过读取数组实例的运行时对象信息替代编译期类型依赖，不增加实例相关分支。
- 不新增方法转接层，不改变数组默认零值、元素布局、复制及 ARC 协议。
- 不为某种 T、某个包、std 或固定数组层数增加非通用特判。
- 静态记录和依赖表可能增加制品体积；固定槽位读取也须与现有临时初始化作完整比较，
  不能将“无运行时构造”直接表述为“没有任何运行时读取或总耗时必然下降”。
- 不涉及开放数组实参的共享体、普通闭合调用及三张现有 `reified_*` 元信息表保持原路径。
  若需要新增私有 ABI 字段、额外执行成本或扩大优化范围，先记录原因并交人工决策。

## 4. 分步实施与验收 TODO

以下步骤均未开始，待方案 Review 后实施。

### 4.1 现状与设计核验

- [ ] 保存最小示例及直接闭合调用的生成 C、汇编基线，区分数组描述符、元素描述符表及
  泛参描述符的临时初始化，不能只检查外层的一处复合字面量。
- [ ] 以自定义普通泛型容器 `Container<T>` 的静态依赖路径作对照，覆盖 `Container<T[]>`、
  `Container<T>[]` 与 `T[][]`，验证容器和数组各层依赖均完整具化；FCTS 可使用 `List<T>`
  补充行为对照，但编译器实现不依赖标准库类型名称。
- [ ] 确认现有类型／callable 依赖槽和描述符生成缓存的复用方案，列出实际需要调整的模块、
  `.ft` 信息及成本；任何新增 ABI 或成本先由人工批准。

### 4.2 接通编译期依赖与跨包恢复

- [ ] 按完整开放类型身份收集本场景的直接依赖，验证类型级／方法级泛参、同名泛参、
  多层数组及具化后类型相同但开放槽位不同的情况，不产生错误合并或排序漂移。
- [ ] 补齐 package-public 与 workspace-cache 两种 `.ft` profile 的真实往返；无 provider
  源码的 consumer 能恢复完整依赖，多层共享调用及成员绑定初始化的归属保持正确。

### 4.3 静态生成与读取

- [ ] 具化点生成整组长期有效的静态记录，共享体改为固定槽位读取并沿既有通道转传；
  不保留数组描述符已静态、但外层泛参记录仍逐次组装的部分实现。
- [ ] 验证顶层函数、实例／静态方法、fit 方法、成员绑定初始化及构造／析构中的合法调用，
  以及多层转传、递归调用和逃逸闭包，均取得与当前具化实例对应的描述符。

### 4.4 正反用例与既有断言迁移

- [ ] `test/` 覆盖依赖与槽位、静态发码、两个 `.ft` profile、非法依赖记录；保留非法类型实参、
  不满足约束及不合法可写性转换的编译期反例，不以实现优化改变语言接受范围。
- [ ] `fcts/` 覆盖同包及真实跨包行为：标量、引用、平凡值、含托管槽值及 spec／union 元素，
  空／非空数组、多层数组、只读／可写数组、交替具化、共享转传和默认零值；验证正常／异常
  退出及闭包逃逸后的所有权，不仅检查编译成功。
- [ ] 核对并单独申请迁移
  [test_codegen.c](../../test/codegen/test_codegen.c) 中的
  `test_open_generic_param_descriptor_remains_runtime_scoped`：它当前断言局部数组描述符和
  泛参描述符存在。拟改为验证 provider 记录开放依赖、consumer 静态具化及共享体不临时组装；
  仍保留“不把 `_T` 写入文件级静态初始化器”的防错目标。本条只是列出迁移对象，尚未获修改批准。
- [ ] `test_generic_runtime_type_kind_codegen` 的闭合数组静态描述符断言保持通过；其他旧用例
  如需修改，逐条列出原因再次由人工决策，不以删除断言、改弱预期或绕过共享体替代修复。

### 4.5 独立回归与交付

- [ ] 对同一探针比较优化前后生成 C 与汇编，包括不可依赖内联消除的独立调用边界；确认消除
  本组临时初始化，记录静态体积、读取指令、栈空间及生命周期操作，不预设具体收益数字。
- [ ] 在沙箱外执行完整 `make test`，测试制品放在工程目录内；记录命令、日志和退出状态。
- [ ] 独立交付实际变更、正反及跨包覆盖、成本结果和遗留项，再更新本专项状态；不能以 G24
  的历史验收结果替代本次优化验收。

## 5. 实施过程问题记录

当前仅登记上述已确认的静态化缺口，未实施优化，未确认新的编译或运行错误。

后续发现问题时，先在本节记录最小用例、实际与预期结果，再记录分析依据、处理方案和验证结果。
需要改变语义、迁移未获批旧断言、增加成本或扩大范围时，记录人工决策后再继续；不把未验证的
风险直接登记为已发生的 Bug。
