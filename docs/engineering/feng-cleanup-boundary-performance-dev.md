# Feng 空清理边界性能优化方案

## 1. 状态与目标

状态：2026-09-25，C-R01／C-R02 已实施；四组性能测量、沙箱外完整 `make test`
及五目标完整 std／FCTS release 验证均已完成，等待人工 Review。两处既有断言迁移
及新增测试注册均经人工批准。本文承接
[原生异常性能方案的 O04／O11](./feng-native-exception-performance-dev.md#9-实施记录)，
独立定义清理边界优化，ARC 方案仍未进入实施。

目标：根据编译期能够证明的实际清理责任，省略不必要的函数／try marker、配套
TLS 清理链操作和空异常清理入口。在保持 C IR 与原生 EH 的前提下，同时降低正常
调用、try 正常返回以及异常经过无资源函数时的成本。

基线为 `a0e0956a`，实施时冻结其配套编译器、runtime、插件与测量产物。本文不预先
承诺收益比例，也不把所有函数／try 都省略 marker 作为目标。

语言规则只引用主规范：

- [对象生命周期](../specifications/feng-lifetime.md)：所有权、终结器与循环回收。
- [异常](../specifications/feng-exception.md)：展开、匹配、重抛及异常载荷所有权。
- [defer](../specifications/feng-defer.md)：作用域退出与清理顺序。
- [插件协议](./c-ir-llvm-exception-plugin-dev.md)：C 发码与原生 EH 的交接。

## 2. 优化前的实现与成本

| 事实 | 代码依据 | 含义 |
| --- | --- | --- |
| 普通 callable 入口建立函数 marker 和根 cleanup region | `src/codegen/codegen.c` 的 `cg_emit_function_eh_prologue` | 没有本地清理资源的函数也可能保留 push／pop；异常经过时进入空清理再 resume |
| try 保护体前建立 marker，正常退出时弹出 | 同文件的 `cg_emit_try_expr`、`cg_release_scope` | 当前不依据保护体实际清理责任省略正常路径登记 |
| marker 初始化写字段并修改本线程清理链 | `src/runtime/feng_exception.c` 的 `feng_frame_push`、`feng_try_frame_push`、`feng_frame_pop` | 不是 ARC 计数操作，但仍有调用、栈写入和 TLS 成本 |
| catch begin 将 try marker 转为异常记录的清理所有者 | 同文件的 `feng_exception_catch_begin`、`feng_exception_catch_end` | 不能因为保护体没有资源，就把 catch 的所有权登记一起删除 |
| 正常退出和显式控制转移已共用作用域清理入口 | Codegen 的 `Scope`、`Local`、`cg_release_scope`、`cg_release_through` | 可复用既有清理组织，不另建按语法形式分支的退出机制 |

已有耗时、汇编及原生 C++ 对照集中保留在
[O04](./feng-native-exception-performance-dev.md#o04正常路径剩余的-try-专属成本2026-09-25待人工决策)
和 [O11](./feng-native-exception-performance-dev.md#o11原生控制结果与函数边界剩余项2026-09-25待人工决策)。
这些证据支持继续检查空边界，不足以把完整 Feng／C++ 的差额全部归因于 marker。
marker 在 S11 前已经存在，其收益与 S11 新增成本的消除必须分别报告。

## 3. 范围与职责

本方案只改变已经证明无必要的清理边界发码。已有资源的登记、释放、ARC 计数、
defer 执行及 catch 记录管理仍走现有机制。

| 层次 | 拟承担的工作 |
| --- | --- |
| Feng Codegen | 复用 `ExprResult`、`Local`、`Scope` 与实际登记入口，收集清理需求；据此统一决定边界建立、正常退出及异常退出发码 |
| 通用 LLVM 插件 | 消费既有协议，继续执行原生 EH 转换与已有 CFG／nounwind 优化，不识别 Feng 类型或 runtime 名称 |
| runtime | 使用现有接口与布局；本方案不新增或修改函数签名、结构字段、TLS 表示和清理顺序 |
| 验证 | 增加发码／IR 检查和语言行为覆盖，复用异常性能基准，分别验证正常路径与异常路径 |

不改变公开插件协议、版本、FT、语言异常检查、ABI、工具链版本、发布流程及
sanitizer 配置。暂不做逐个资源的静态异常清理替代、清理链重构、ARC 消除或异常
记录分配优化。[ARC 所有权优化](./feng-arc-ownership-performance-dev.md)独立 Review、
实施和交付。

## 4. 通用清理需求判定

### 4.1 判定对象

判定单位是一个 callable 的清理边界，或一个 try 保护体的清理边界。事实来自实际
发码所建立的所有权，不以 AST 中是否出现对象声明、是否有 `throw` 或参数是否为
标量代替证明。

至少记录以下清理需求：

1. 托管局部和临时值，包括调用参数保活、转换、装箱、捕获及表达式结果存储。
2. 按值聚合体、开放泛型和重化存储的清理，包含条件初始化后的资源。
3. defer 登记及其环境中由本边界负责的资源。
4. 活跃 catch 的异常记录所有权，以及异常退出时必须移除的清理节点。

仅当覆盖该边界所有可达路径，且证明没有上述责任时，结果才是“可省略”。存在
资源或事实不完整时保留当前行为。已清空但仍留在 LIFO 链中的节点同样属于责任，
不能因其不再执行 release 而忽略。

函数是否可能抛出，与本层是否需要清理是两个独立事实。调用的异常集合为
`unknown`，本身不意味着调用者拥有资源；反之，函数不会外抛，也不意味着没有
ARC 或 defer。隐藏桥接／临时值仍须计入，不能用源码异常集合推导清理为空。

### 4.2 收集与发码

建议复用现有局部登记和退出发码入口维护结构化的编译期事实，在实际资源登记、
defer、catch 所有权及条件存储建立处完整记录；子作用域释放编译器元数据前，将
需要的事实归入所属边界。不能在最终 `Scope.items` 上计数，因为子作用域可能
已经弹出，早退路径也可能已发码。

先完成边界内的事实收集，再组织入口、主体和各出口的输出。优先采用现有发码
缓冲与结构化边界记录，禁止通过搜索／替换生成 C 文本来删除 push、pop 或 label，
也不增加运行时“是否为空”的判断。

`return`、`break`、`continue`、正常落出及异常路径必须消费同一份判定。Lambda、
defer thunk、方法和生成的 callable 各有独立根作用域；不能把外层函数的判定直接
用于内层 callable。

### 4.3 泛型与跨包

共享体的证明须适用于它支持的全部实参。开放描述符可能要求托管／聚合清理时，
保留边界；不能因为某次调用传入 `int` 就删除共享体的清理。仅借用且无需本层管理
的值，与拥有独立副本的值，按已有调用和存储约定区分。

跨包调用继续使用已有参数／返回所有权约定和当前实际生成的桥接。省略调用者的
空边界不要求读取被调用函数体，但必须证明调用者自己没有清理责任，并保持被调
用者正常返回和原生展开时的既有责任。缺少必要事实时保留边界，不新增 FT 契约。

## 5. 两项发码优化

### 5.1 无资源 try：把 marker 建立移到匹配 catch 的路径

对通过 §4 证明的 try：

| 路径 | 拟发码行为 |
| --- | --- |
| 正常进入／完成保护体 | 保留 EH 区域和异常后继，不执行 try marker push／pop |
| 正常 return／break／continue | 按真实作用域退出，恢复父 EH 区域，不弹出不存在的 marker |
| 匹配某个 catch | 在调用现有 `feng_exception_catch_begin` 前建立该 context 的 try marker，然后保持原 catch 所有权流程 |
| 本层没有匹配 catch | 不调用针对该 try 的 `feng_frame_release_to`，以同一异常记录向父区域传播 |

`FengCatchContext`、异常记录和 selector 的使用仍遵守既有协议；延后的是 marker
登记，不是重新抛出异常，也不是省略 catch begin／end。记录从 landing 交接到
catch owner 前不能插入可能外抛的用户代码；匹配完成后先建立有效 owner，再执行
绑定、转换和 catch body。

保护体中存在资源、defer，或内层 catch 所有权可能经过该边界时保留现有登记。
本 try 自己的 catch 所有权在匹配路径建立，与保护体是否需要正常路径 marker
分别判断。

### 5.2 无资源 callable：省略函数 marker 和根清理区域

对通过 §4 证明的 callable，成组省略函数 marker 的声明、push／pop、根 cleanup
region、对应 `release_to` 和传播入口。该函数仍可向调用者传播异常，不得因此
标记为 `nounwind`；确有 catch 的区域也不能因省略纯 cleanup 而丢失。

首轮证明要求整个 callable 没有自身清理责任；包含 catch 所有权、defer、资源或
未知清理的 callable 保留函数边界，不扩展为逐条路径的动态 marker 懒初始化。
EH 区域编号及父区域须一致更新，不能留下指向已删除根区域的引用；协议配置是否
仍需输出按既有协议处理，不靠删除异常元信息强迫宿主忽略异常。

两项分别实施、测量，再检查组合效果。资源存在时仍使用原清理机制，不通过
关闭内联、降低优化级别或增加堆分配实现简化。

## 6. 正确性验证矩阵

`test/` 检查清理需求判定、C／LLVM IR 与协议结构；`fcts/` 检查可观察行为。
每类正向优化用例都要有必须保留边界的反例，不能只验证标量成功路径。

| 维度 | 必须覆盖 |
| --- | --- |
| 边界组合 | 函数空／非空、try 空／非空、只消除函数、只消除 try、组合；相邻和多层嵌套 |
| 控制流 | 普通落出、各层 return、循环 break／continue、短路、if／match／try 表达式及分支抛出 |
| 资源 | 局部、临时值、参数保活、结果合流存储、条件初始化、托管字段聚合体、defer 和已清空但未弹出的节点 |
| 异常 | 具体／兜底 catch、匹配／不匹配、内层处理后继续、重抛／抛新值、catch 内调用再抛及异常记录准确一次释放 |
| 泛型 | 函数／类型／方法泛参、开放共享体与闭合实例、受管值／平凡值／聚合值、描述符恢复与隐式桥接 |
| 调用边界 | 普通函数、方法、Lambda、defer thunk、spec／callable 间接调用、跨包且生产者源文件不可见 |
| 生命周期 | 终结器顺序、defer LIFO、返回值存活、嵌套异常保活、原有终结器异常屏障 |
| 工具与目标 | O0／O2／O3、普通／现有 sanitizer 配置、DAP 断点及单步、五个既有目标平台 |

复用既有 `test/codegen/test_native_exception.c`、`test_nested_exception.c`、
`test_defer_generic_context.c` 及相应 FCTS 的覆盖组织，新增针对性用例。
既有测试预期若依赖“所有函数必有 marker”等旧结构，先逐项列明迁移原因并获
人工批准，不能弱化语言行为、调试停靠或安全性断言。

## 7. 性能验证与验收

测量方法复用 [异常性能基准说明](../../test/exception_perf/README.md)：固定源码和
配套产物，动态输入、预热、交替多轮测量，区分 O0／release、同翻译单元／跨包。
本方案至少保存“基线、只优化 try、只优化函数、组合”四组，不能只给组合总收益。

除原有正常调用、try、浅层／深层／稀疏抛出外，补足有资源及无法证明为空的对照。
记录 marker／TLS 操作、清理入口和 resume 的静态结构及热路径执行情况、栈大小、
代码／异常表体积和耗时。无 marker 不等于没有任何异常相关的寄存器或栈成本。

验收需同时满足：

- 能证明为空的边界确实消除了对应操作；有责任或无法证明的边界仍正确清理。
- 正常与异常退出保持语义，不能用 release 的收益掩盖 O0 退化。
- 分别报告相对 `a0e0956a`、S11 前有效行为样本和同工具链 C++ 的结果；对比口径
  参见原生异常性能方案，不把旧 S11 错误执行纳入有效性能基线。
- 沙箱外完整 `make test` 与显式完整 std／FCTS release 回归通过；五目标分别记录
  编译与执行结果，模拟执行不能代替原生性能测量。缺失环境不得标为通过。
- 未新增运行时检查、分配、ARC、锁或协议操作；若出现性能取舍，先记录并交人工决定。

如按方案不改插件源码，无需重建插件；若实施发现必须改变公开协议、runtime、ABI
或 FT，停止该扩展，提交原因和替代方案 Review，不顺带实施。

## 8. Review 决策与 Todo

| 编号 | 已批准的实施边界 |
| --- | --- |
| C-R01 | 越过原方案 §4.4 的冻结边界，按本文实施 try 登记延后及空函数根清理省略 |
| C-R02 | 只优化已证明为空、资源存在／事实不完整时保持原机制，不加入运行时判空与资源链重构 |

- [x] Review 并确认上述边界：人工于 2026-09-25 明确要求按本文开始实施。
- [x] 冻结 `a0e0956a` 基线与配套产物，建立成本和正确性对照。
- [x] 审计所有资源登记及出口，形成编译期清理需求事实；验证隐式路径没有遗漏。
- [x] 实现无资源 try 优化，补齐正例／保留反例，完成单项测量。
- [x] 实现无资源 callable 优化，验证区域父子关系和全部出口，完成单项测量。
- [x] 完成组合、泛型、跨包、嵌套异常、生命周期、DAP 和现有 sanitizer 配置覆盖。
- [x] 对需迁移的既有测试先取得逐项批准，保留全部语义要求：C01／C06 与新增测试注册已获批准。
- [x] 通过完整回归，记录各目标结果与性能残余，提交人工 Review。
- [x] 输出英文 commit message，由人工提交；未自动提交。

实施问题按“现象与复现条件 → 原始证据 → 分析 → 方案／人工决定 → 修复与验证”
记录于本文后续实施记录，尚未解决或未验证的项目不得写成完成。

## 9. 实施记录

### C01：发码组织与覆盖审计（2026-09-25，已实现）

当前 Codegen 会先写入口及早退出口，随后才遇到完整资源登记；短路表达式、Lambda
和 defer 还会使用独立发码缓冲。仅在函数尾统计局部，或只修改 prologue，均不足以
保持全部出口一致。

采用编译器内部的条件发码：每个边界保留独立、单调的清理需求记录，最终组装 C
时先输出由这些记录确定的编译期常量，再由 C 预处理指令选择入口与出口片段。
短路表达式会将语句缓冲转成表达式字符串，不能只在原缓冲上登记字节区间；预处理
片段可通过既有字符串组合完整保留。它不搜索或匹配 C 文本，不进行第二次有副作用
的 AST 发码，不向 runtime 传入标志，也不产生运行时条件判断。原始 C 中保留条件
片段，预处理后的 C 和 LLVM IR 必须实际消除未选择的操作，测试以此区分两层产物。
实际资源登记、catch 所有权与 defer 使所属边界及其词法外层边界保守保留；嵌套
callable 使用独立作用域。

既有 `test_native_exception.c` 用内联后两个 `feng_frame_push` 验证 callee 已内联。
无资源 callee 的 marker 被删除后，该计数不再能表达内联事实。候选 O2 IR 已复现
该断言失败，而 `clean` 保留自己的 marker，`fail` 的条件及 `feng_throw` 已进入
`clean`。经人工批准，已迁移为检查保留本地 marker、存在内联后的 throw、没有对 `fail` 的调用，
并保留原 O0／O1／O2／O3、sanitizer、属性与协议检查。

冻结基线编译器 SHA-256 为
`1ee3a1d1f63e917edb43b0417ce1c49c539fced87bc5b1ca0032e0bd213059f8`。
基线与组合候选均已完成现有九种性能内核的 O0／O2／O3、单翻译单元／跨包构建，
每组 1001 次的独立 oracle 行为检查全部通过；此项不是全量回归结论。

### C02：新增行为夹具的独立构建（2026-09-25，已定位）

为不提前修改待批准的 FCTS 入口，先在 `build/cleanup-boundary/behavior` 建立独立
项目验证新增用例。消除夹具本身违反现有表达式规则的写法后，候选在第一处
`assertEquals(boundaryReturn(0), 11, ...)` 报 `IE0001: codegen: out of memory`。
冻结基线同样复现；最小输入 `assertEquals(1, 1, "same")` 在只导入 `std.test`／
`std.numeric` 时失败，补充 `std.text` 后成功，单独补 `std.collections` 仍失败。
LLDB 定位到 `cg_emit_closed_callable_fdesc` 的嵌套契约描述符构建，与清理边界判定
无关。独立夹具补齐 `std.text` 导入后继续验证；完整 FCTS 本来就有该模块导入。
本次不修改既有契约可见性或诊断实现，`IE0001` 的误导性另留记录。

### C03：聚合体生命周期对照（2026-09-25，已定位）

新增 17 个行为用例首次执行为 14 通过、3 失败：按值聚合体包含普通 `type` 字段时，
观察到终结次数多于夹具预期；泛型函数及类型／方法泛参组合中也有提前终结计数。
冻结基线产生完全相同的计数。保留生成 C 后确认：聚合体／泛型 owner 的引用字段
先构造默认 `BoundaryResource { id: 0 }`，再被显式字段值替换，额外计数来自这些
不同对象，未出现显式资源重复终结。新增夹具同时断言总终结数与对象 id 顺序，
分别验证默认对象已释放、显式对象在剩余 owner 存活时不释放、最后准确释放一次。

### C04：四组成本对照（2026-09-25，已完成）

四组均使用固定的动态输入基准、LLVM 22.1.8 和相同 runtime／插件，在同一台
macOS ARM64 上预热后交替采样七轮。仅 try／仅函数通过独立实验编译器保留另一类
边界实现，不向正式编译器加入开关；每组保存其源码、二进制、IR 和命令。

首轮组合相对基线的 O2 单翻译单元普通条件调用由 2.229 降至 0.905 ns，try 正常
路径由 4.221 降至 0.932 ns；跨包分别由 4.832 降至 1.031 ns、4.750 降至
1.035 ns。O0 try 与密集抛出均下降，但普通条件调用接近持平，其中跨包中位数
6.869 → 7.021 ns，约 +2.2%。因此补做下述普通调用配对采样，不用 release 收益
代替 O0 验收结论；完整表与 S11 前有效样本对照一并保留于本节。

15 轮普通路径复测中，O0 条件调用单文件中位数为 6.792 → 6.725 ns，跨包为
6.824 → 6.778 ns；同轮配对差值中位数分别为 -0.052／+0.014 ns，跨包差值范围
为 -0.168～+0.450 ns，未复现稳定的 +2.2% 退化。无条件普通调用则分别下降
约 0.425／0.395 ns；无调用控制组保持约 4.26 ns。保留两轮全部记录，不只选取
较有利的一轮。

七轮动态输入中位数，单位 ns/次；“普通”指有条件 throw、此次输入不触发异常的
函数调用，“抛出”每次都会抛出并捕获。全量九种模式的逐轮原始数据、配对差值、
IR／汇编及工具身份保存在
`third_party/llvm-c-eh/temp/cleanup-boundary-performance/`，该目录为本地验证归档。

| 模式／布局 | 路径 | 基线 | 仅 try | 仅函数 | 组合 | 同轮 C++ |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| O0／单文件 | 普通 | 6.999 | 6.861 | 6.975 | 7.008 | 3.883 |
| O0／单文件 | try 正常 | 8.734 | 7.279 | 7.814 | 6.225 | 4.161 |
| O0／单文件 | 抛出 | 3196.640 | 3213.685 | 2540.310 | 2523.555 | 2243.815 |
| O0／跨包 | 普通 | 6.869 | 7.122 | 7.066 | 7.021 | 3.721 |
| O0／跨包 | try 正常 | 8.717 | 7.319 | 7.856 | 6.131 | 3.927 |
| O0／跨包 | 抛出 | 3200.160 | 3181.595 | 2535.340 | 2528.650 | 2224.205 |
| O2／单文件 | 普通 | 2.229 | 2.223 | 0.900 | 0.905 | 0.905 |
| O2／单文件 | try 正常 | 4.221 | 2.220 | 2.234 | 0.932 | 0.920 |
| O2／单文件 | 抛出 | 1666.125 | 1663.645 | 1642.745 | 1651.895 | 1831.775 |
| O2／跨包 | 普通 | 4.832 | 4.818 | 1.031 | 1.031 | 0.907 |
| O2／跨包 | try 正常 | 4.750 | 4.116 | 2.721 | 1.035 | 0.921 |
| O2／跨包 | 抛出 | 3172.765 | 3164.915 | 2550.310 | 2533.180 | 2191.015 |
| O3／单文件 | 普通 | 2.226 | 2.231 | 0.899 | 0.898 | 0.897 |
| O3／单文件 | try 正常 | 4.210 | 2.222 | 2.227 | 0.900 | 0.900 |
| O3／单文件 | 抛出 | 1679.245 | 1682.260 | 1654.005 | 1655.125 | 1851.160 |
| O3／跨包 | 普通 | 4.297 | 4.480 | 1.042 | 1.030 | 0.912 |
| O3／跨包 | try 正常 | 4.732 | 4.628 | 2.707 | 1.028 | 0.912 |
| O3／跨包 | 抛出 | 3177.450 | 3184.150 | 2498.015 | 2509.360 | 2188.710 |

普通路径每个样本 2000 万次，密集抛出 20 万次。O2 跨包抛出仍比 C++ 约慢
15.6%，O0 也没有达到 C++ 普通路径水平；不能据此宣称全部异常成本已消除。
Feng／C++ 使用各自异常载荷和 runtime，本表不是共享异常 runtime 的插件微基准。

递归诊断为每样本 2 万次、源码递归深度 8：O0 跨包从 15484.300 降至
8974.950 ns，O2 跨包从 14941.550 降至 2501.100 ns。汇编确认 O2 组合候选
已消除 `relay` 自递归；这里包含宿主重新获得尾递归优化的收益，不能解释为相同
物理展开帧数下 unwinder 快了数倍。

O0 生产者三个函数的代码段由 524 降至 388 字节，异常表由 36 降至 28 字节。
`plain`／`step`／`relay` 最大栈占用分别从 128／176／176 降至 48／96／80 字节；
O2 从 96／96／176 降至 0／32／32 字节。无资源 `step`／`relay` 的根清理
landingpad、resume 和 marker 操作均消失；原生 EH 配置仍按既有协议保留。

与 S11 前 `0575d00d1cf9515a2e26420f7ec055f87f61e94b` 的有效控制程序比较，
使用归档旧二进制及配套 runtime，并为基线／组合重新构建相同源码。下表为七轮
完整进程耗时中位数（ms），包含启动；旧基准循环次数固定，组合 release 的普通／
try 循环可被宿主进一步折叠，严格的逐次成本比较应使用上面的动态输入表。

| 模式／工作量 | 次数 | S11 前 | 本次基线 | 组合 |
| --- | ---: | ---: | ---: | ---: |
| O0 普通调用 | 2000 万 | 103.581 | 112.279 | 81.035 |
| O0 try 正常 | 2000 万 | 149.460 | 150.860 | 90.104 |
| O0 泛型 | 1000 万 | 108.657 | 105.302 | 64.463 |
| O0 资源＋defer | 200 万 | 78.499 | 79.097 | 69.755 |
| O0 抛出 | 20 万 | 410.959 | 591.846 | 458.993 |
| O2 普通调用 | 2000 万 | 49.284 | 47.781 | 5.419 |
| O2 try 正常 | 2000 万 | 97.774 | 88.218 | 4.903 |
| O2 泛型 | 1000 万 | 72.171 | 67.593 | 28.434 |
| O2 资源＋defer | 200 万 | 71.928 | 73.139 | 63.230 |
| O2 抛出 | 20 万 | 不适用：旧 S11 行为错误 | 315.981 | 315.056 |

O0 抛出相对本次基线改善，但仍比 S11 前约慢 11.7%；本次不通过回退正确的原生
异常控制流来追平旧结果。资源和泛型控制组保持原有清理语义及结果校验。

### C05：回归结果（2026-09-25，已通过）

新增 Codegen 矩阵（O0／O2／O3，普通及 ASan+UBSan IR）独立通过；新增 17 个
FCTS 行为用例独立 debug／release 均为 17/17。

新增覆盖位于 `test/codegen/test_cleanup_boundary.c`、
`fcts/fcts_bin/src/test_cleanup_boundary.ff` 与
`fcts/fcts_lib/src/test/lib_cleanup_boundary.ff`。前者验证实际发码的空边界删除及
有资源反例，后两者验证控制流、返回值存活、终结顺序、泛型与跨包所有权。它们与
既有异常、分支清理、union、defer、终结器屏障及 DAP 用例一起进入完整回归。

沙箱外第一次 `make test` 退出 2，准确停在 C01 记录的
`test/codegen/test_native_exception.c:112` 双 marker 断言。此前 archive、lexer、
parser、semantic、runtime 与原生 EH 契约检查通过。当时既有断言迁移及新增测试入口
注册仍待人工批准，尚未修改，因此该次整体回归不通过。

随后独立继续 UBSan 阶段其余检查：新 Codegen 矩阵、debug、CLI（含 DAP）、路径、
符号、smoke、CLI 项目、内建包初始化、std 607/607、原有 FCTS 1516/1516、
性能约束、插件接入与新增行为用例 17/17 均通过。旧 Codegen 断言之后的其他
Codegen 用例当时仍未执行，不用这些独立结果替代完整 `make test`。

人工批准后完成 C01／C06 两处等价断言迁移及 Codegen／FCTS 注册，重新在沙箱外
执行完整 `make test`，退出码为 **0**。两阶段均通过全部 Codegen、runtime、
原生 EH 契约、CLI／LSP／DAP 与符号检查；smoke、项目、性能约束、增量构建、
发行及工具链接入检查全部通过。最终结果如下：

| 完整回归阶段 | std | FCTS（含新增 17 个用例） |
| --- | ---: | ---: |
| macOS UBSan | 607/607 | 1533/1533 |
| 普通 | 607/607 | 1533/1533 |

完整日志为 `third_party/llvm-c-eh/temp/cleanup-boundary-performance/make-test-final.log`。
五平台验证的临时脚本首次在编译前因 `FENG_TEMP_DIR` 目录不存在而退出；补建工作
目录后重新执行，不修改正式工具配置或将该次准备失败记为平台通过。最终使用普通
编译器与既有 bundled 工具链完成以下完整 release 构建与执行，脚本退出码为 **0**：

| 目标 | std／FCTS 构建 | std 执行 | FCTS 执行 |
| --- | --- | ---: | ---: |
| macos-arm64 | 通过 | 607/607 | 1533/1533 |
| linux-arm64-gnu | 通过 | 607/607 | 1533/1533 |
| linux-arm64-musl | 通过 | 607/607 | 1533/1533 |
| linux-x64-gnu | 通过 | 607/607 | 1533/1533 |
| linux-x64-musl | 通过 | 607/607 | 1533/1533 |

macOS 为本机执行；Linux 为既有 Apple Container 中执行，其中 x64 使用模拟执行，
这些结果仅证明行为，不作为原生性能数据。每目标的构建／执行日志及状态列表
保存在同一验证归档目录，全部用例无失败、无跳过。验证后未再改动生产代码或测试。

最终生产代码变更仅在 `src/codegen/codegen.c`；其余为本文、新增测试、两处获批
的断言迁移及入口注册。runtime、ABI、FT、插件及公开协议没有变更，本轮无需
重新预构建插件或工具链。C04 的性能残余保留为后续优化依据，不纳入本轮扩展。

### C06：既有泛型 try 发码断言（2026-09-25，已迁移）

复核后续用例发现 `test_generic_try_body_reified_storage_codegen` 还要求原始 C
中 `}`、`feng_frame_pop()`、父区域 activate 三行直接相邻。临时入口直接调用
该原函数、保持全部断言不变，确认在 `test_codegen.c:12834` 失败：本次编译期
选择指令插入其中，预处理前的文本邻接不再代表实际 C 作用域。

仅将该作用域顺序断言改为检查预处理后的 C，原源码、VLA 存储断言及 C 编译
验证保留。人工已批准该迁移、C01 的内联断言迁移及 Codegen／FCTS 新用例入口
注册；不为迎合旧文本布局给正式发码增加特殊处理。

验证证据：原测试同一 Feng 源码生成的原始 C 不再包含该三行邻接串，`clang -E -P`
后的 C 仍包含它。临时入口在独立子进程中执行全部 258 个原 Codegen 用例及新增
矩阵，所有原断言保持不变，259 个中只有 C01／C06 两处失败，其余 257 个通过。
这用于完整列出迁移项，不视为原 `make test` 已通过。
