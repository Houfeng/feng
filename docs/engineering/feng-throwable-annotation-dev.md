# Feng `@throwable` 注解与统一异常契约开发方案

> **状态**：待 Review，尚未实施。本次仅整理方案，没有回退 `E: throw` 或修改实现。
>
> **日期**：2026-09-20。
>
> **核心规则已确认**：`@throwable` 标记库中定义的异常根 spec；建立对根契约的
> 满足关系时，先检查声明主体的载荷资格，再检查普通契约满足。该规则禁止子 spec，
> 允许 `Error<T>` 等合法具名泛型载荷，不依赖 `@final`。`throw expr` 的类型准入
> 只检查根契约证明，原有非载荷检查不变。普通 spec 泛型约束继续使用既有机制。
>
> **实施前需完成**：第 6 节的完整根视图承载布局、生命周期与 ABI／成本验证，
> 以及第 8 节的人工 Review 决策。本文不将“语义规则已明确”等同于“发码与 ABI
> 闭环已经验证”。

## 1 目标与规范归属

允许库自行定义具有普通成员和父契约的异常根 spec，编译器通过内建注解识别其
身份，不感知具体库、模块、类型名称或 `toString` 等成员名称。

根 spec 同时作为普通值类型和普通泛型约束：前者支持统一持有、传递不同具体
异常，后者证明泛型共享体中的值具有可抛资格。无需增加内建 `throwable` 类型、
关键字或另一套泛型约束系统。

Review 通过后，先将规则写入对应主规范，再修改代码、测试并执行全量回归：

| 内容 | 唯一规范位置 |
| --- | --- |
| 注解清单与规则入口 | [语言总览](../specifications/feng-language.md) |
| 根标记、满足关系的载荷准入 | [`spec` 规范](../specifications/feng-spec.md)、[异常规范](../specifications/feng-exception.md)，分别定义契约入口与异常载荷资格，不重复列举载荷集合 |
| 普通泛型约束、移除内建 `throw` 约束 | [泛型规范](../specifications/feng-generics-draft.md) |
| throw / catch、异常传播与载荷身份 | [异常规范](../specifications/feng-exception.md) |
| 根标记与跨包声明事实 | [符号表规范](../specifications/feng-symbol-table.md) |
| 诊断归属与编号 | [AE 错误码规范](../specifications/feng-error-codes-ae.md) |

本文记录拟议变更、代码依据、待决项和实施任务。落地后将语言规则收敛到主规范，
本文仅保留实施与验证记录。

## 2 已确认的语言边界

### 2.1 注解标记库中的契约

`@throwable` 用于 object-form `spec`。它标记一个已有的普通契约，不生成契约，
也不为其补充成员或实现。其他 spec 形态和其他声明种类不属于该注解的适用位置。

以下只展示用法；名称、所在模块和成员并非已经确定的标准库 API：

```feng
@throwable
open spec Throwable {
  func toString(): string;
}

type Error<T>: Throwable {
  let detail: T;

  func toString(): string {
    return "error";
  }
}

func raise<E: Throwable>(error: E) {
  throw error;
}

func forward(error: Throwable) {
  throw error;
}
```

库也可以按普通父契约规则复用 `Display` 等契约。编译器只根据注解登记的声明
身份判断异常根，根契约的成员满足、可见性、调用和 witness 均沿用普通 spec 规则。
根本身是否允许泛型、是否要求公开及注解参数写法，见第 8 节待决项。

`@final` 是[独立特性](./feng-final-annotation-dev.md)，其通用规则不变。异常根的
子 spec 限制由第 2.2 节的统一载荷资格检查保证，不要求根额外标注 `@final`，
也不依赖该独立特性先实施。

### 2.2 在建立满足关系时检查载荷资格

统一规则：**声明主体建立对异常根契约的满足关系时，必须先通过完整的合法载荷
检查，再通过普通契约满足检查，该满足关系才能成立。**

检查入口覆盖 `type` 声明头、`fit` 的契约关系，以及 object-form `spec` 的父
契约列表。名称解析及原有声明合法性检查通过后，若关系目标是已登记的异常根，
则对关系左侧的声明主体执行同一载荷资格检查；普通契约关系继续使用原有规则。
根的注解登记不建立新的满足关系，不把根声明本身当作具体载荷执行此项检查。

合法载荷检查统一复用异常主规范的既有规则，并提前到声明满足契约时执行。
检查失败在建立满足关系处报告；已被既有声明规则拒绝的位置继续报告原有根因，
不增加重复诊断。通过验证的根满足关系同时保证合法载荷资格与契约能力，后续
throw 只消费该证明，不再按数据类别维护另一套准入判断。

统一规则的直接结果：

- `type Error<T>: Throwable` 允许建立关系：`Error<T>` 是具名 type，不能因为
  声明中有开放泛参而拒绝。T 不必满足异常根：例如 `Error<int[]>` 的载荷是整个
  具名对象，不要求它的数组字段单独具备可抛资格。
- `spec Child: Throwable` 拒绝建立关系：被检查的声明主体是 `Child`，它属于
  spec，不具备具体异常载荷资格。即使成员完全满足根契约也不能通过。因此根不能有
  子 object-form spec；多父列表、泛型子声明和跨包引用均适用，不依赖 `@final`。
- 数组等不具备载荷资格的目标，在声明满足关系时即被拒绝。

现有 `type_ref_is_exception_payload` 同时包含“载荷类别”和“当前类型引用仍含
开放泛参则拒绝”两类判断。实施时应提取共用的载荷资格检查，区分以下职责：

1. 声明验证按载荷类别及具名类型身份规则判定，允许 `Error<T>` 这样的泛型声明，
   不要求声明处已取得每个闭合实例的描述符；也不因此放行数组或任意开放类型。
2. 实际 throw 的发码按第 2.5 节取得闭合实例的精确描述符并构造载荷。
3. 具体 catch 保留类型引用必须闭合的既有要求，不能因复用函数的调整而放开
   `catch error: E` 或 `catch error: Error<T>` 等仍含开放泛参的写法。

这里前移的是载荷资格检查；语句及表达式相关的非载荷检查仍在 throw 处执行，
具体边界见第 2.3 节。

### 2.3 throw 只消费根契约证明

对于 `throw expr`，完成表达式解析和通常的语句检查后，类型准入统一判断：
**能否按既有契约关系证明该表达式满足已登记的异常根。**

- 具体类型及 `Error<T>` 等具名泛型类型，根据已验证的满足关系证明。
- 直接泛参 E，根据普通 spec 约束证明；`E: Throwable` 无需另加内建 `throw` 约束。
- 已形成的根 spec 值可直接抛出；能够按既有契约关系证明根能力的合法交叉契约
  视角，复用已有根投影，不另造结构匹配或联合类型准入规则。
- 无约束泛参、无关契约约束及没有根满足关系的具体类型不能抛出。

禁止根的子 spec 不改变普通泛型约束规则：`E: Throwable` 的实际类型仍可为
`Throwable` 本身或能够证明根能力的合法交叉契约。共享体必须保留实际 spec 值，
并按第 6 节从该视角形成真实载荷，不能假定所有 E 都是具体实体类型。

合法载荷检查已在建立满足关系时完成，throw 不再重复执行，也不维护另一套数据类别
判断。根值形成、泛型转传、字段保存、函数返回和跨包消费必须维持同一证明，不能绕过
关系准入。

以下非载荷规则不变：表达式解析与非 void 要求、匿名 catch 中 `throw;` 的作用域、
defer 限制、控制流终止与可达性、try 表达式规则、异常清理和 C ABI 边界规则。
根 catch 是具名 catch，不自动取得匿名 catch 的 `throw;` 权限。

### 2.4 catch 的允许形式

- 具体类型 catch 保持具体描述符身份的精确匹配；泛型实例按完整实际类型区分。
- 允许 `catch error: <根 spec>`，将任意合法异常绑定为根契约值，供普通成员调用、
  保存和传递使用。
- 保留匿名 catch。除根以外，不开放其他 object-form spec、intersection-form、
  union-form 或 callable-form spec 的 catch。
- `catch error: E` 继续禁止；也不因本方案顺带开放仍含泛参的 catch 类型引用。

具体 catch 的可用载荷类别沿用异常规范。根 catch 与匿名 catch 的排序和重复诊断
见第 8 节；不以支持根 catch 为由增加运行时的 spec 满足集合查询。

### 2.5 泛型与实际异常身份

是否属于泛型类型不是载荷资格的否决条件。发码必须能取得实际闭合类型对应的
描述符，正确形成载荷，并使具体 catch 与之使用同一身份。

`Error<T>: Throwable` 的声明检查与运行时的实际类型信息分属两个阶段：声明处
证明具名载荷资格，实际执行 throw 时取得 `Error<int>` 等完整实例的精确描述符。
共享体源码中仍写作 `Error<T>` 不构成拒绝理由；具化通过已有描述符重化机制完成，
不要求共享体源码先静态替换成具体类型，也不要求为每个实参复制一份函数体。

当前闭合泛型实例已经支持 throw/catch；共享体内静态类型仍为 `Error<T>` 的 throw
则被旧 Semantic 提前拒绝，旧 throw 描述符发码也不能直接视为已接通全部重化路径。
本方案需要补齐该通路，不能仅删除语义检查就宣称完成。

泛型共享体分别复用方法／函数隐式描述符和类型 owner 描述符中的既有重化信息，
覆盖引用类型及值类型。不得把具体异常身份替换成 E、泛型模板或根 spec 的身份。

## 3 根信息登记与唯一性

### 3.1 每次编译持有一个根信息记录

编译期状态保存 **spec 信息**，不是 bool。建议记录：

- 根声明的规范化身份；
- 可供语义检查和发码使用的声明引用／查询句柄；
- 所属模块及用于重复诊断的来源信息。

成员、父契约和类型信息通过该声明查询，不复制另一套成员模型。记录归当前分析
上下文所有，生命周期覆盖相关阶段；不使用跨编译任务共享的进程级静态变量。
LSP 重新分析时按当前源码与依赖重建记录，避免旧标记残留。

### 3.2 登记规则

| 状态与候选 | 处理 |
| --- | --- |
| 尚无根，遇到合法标注 | 保存 spec 信息 |
| 再次得到同一规范化声明 | 幂等复用；重复引用和菱形依赖不构成重复定义 |
| 遇到另一个被标注的声明 | 编译错误，指出两个根的模块与声明来源 |

同一源声明重复书写注解，与同一声明被重复导入是两件事；前者的写法规则待 Review。
短名称相同不表示同一声明，导入别名不同也不表示不同声明。跨文件符号引用可复用
既有模块身份与符号 ID 的组合，不能只比较裸 `symbol_id` 或跨进程指针。
源码、缓存及不同 profile 的来源覆盖先遵循既有规则，再归一化为当前声明身份。

登记范围为当前编译的源码及实际参与编译的依赖闭包，不扫描未参与编译的安装包。
根必须在契约资格与函数体检查前可用，结果不能依赖源码或 import 顺序。

### 3.3 复用已有加载流程

当前 `feng_symbol_provider_add_bundle` 会读取 `.fb/mod/` 下的全部公开 `.ft`；
Semantic 则只接入被引用及其签名依赖的外部模块。因此，不能仅在 Semantic 已接入
模块中发现根，否则会漏掉输入依赖中未被直接引用模块上的第二个标注。

建议在已有声明收集、符号读取与归一化流程中同步收集根候选／模块摘要，再通过
中立的导入查询接口交给编译上下文统一登记。每个候选只需与已有根比较，不额外
遍历所有包的所有类型。核心 Semantic 不依赖 `.ft` section、reader 或 zip 路径。

注册摘要是编译期索引，不是运行时反射表。根的声明与成员可按既有导入机制解析，
无需为查找一个根而合成所有依赖模块的 AST。

## 4 `.ft` 与跨包消费

### 4.1 必须保存定义标记

定义根的包必须将标注作为 spec 的声明事实写入 `.ft`。建议追加所属 spec 的
`ATRS` 属性，复用现有声明、父契约和成员记录；不序列化 AST 指针或 witness 地址。
具体属性编号在符号表主规范中分配。

链路需要完整覆盖：源码标记 → 归一化符号事实 → writer → reader → 查询视图 →
导入声明恢复 → 根登记。package-public 和 workspace-cache 均需保留相应事实，
复制、LSP 符号形状比较和跨平台公开事实比较不能丢失标记。

已有 spec 导出会调用 `apply_decl_annotations`，但导入 spec 分支当前没有调用
`synthesize_decl_annotations_from_symbol`。因此增加属性读写之外，还必须补齐
spec 注解恢复或等价的统一声明事实访问，不能产生源码与二进制包两种判断结果。

### 4.2 定义与引用分开处理

假设 A 定义异常根，B 和 C 依赖 A，应用依赖 B、C：

- A 的 `.ft` 保存根定义标记。
- B、C 按已有机制保存使用根的类型引用和契约关系，不重新定义根。
- 依赖管理层展开并去重传递依赖；编译上下文最终登记同一个 A 中的声明。
- 若另一个依赖 D 定义了不同根，必须报告冲突，不按加载先后选择其中之一。

当前公开表不保证收录私有根或私有模块。根的公开性必须按第 8 节决定：若允许
私有根，需要明确额外的分发元信息与必要声明保留方式，不能默默跳过其唯一性检查。

根标记恢复后，源包建立满足关系时的载荷资格与消费侧的类型事实必须一致。
跨包同样覆盖 type、fit、泛型声明以及对子 spec 声明的拒绝，不以 provider 源码
可见为前提，也不以额外的 final 标记作为拒绝子 spec 的依据。

建议沿用已采用的 `.ft` 2.0 加完整重编策略，具体兼容声明在 Review 后写入主规范。
重编范围包括依赖包、consumer、符号缓存及相关生成制品；不默许混用旧异常规则下
生成的库，也不为旧 `E: throw` 元信息增加隐式兼容语义。

## 5 基于当前代码的接入点

| 位置 | 当前事实与实施任务 |
| --- | --- |
| `src/lexer/token.h`、`src/parser/parser.c` | 登记内建注解，复用通用注解解析；不增加关键字 |
| `src/semantic/semantic.h` | 当前分析上下文保存根信息；导入查询接口提供中立根摘要／引用 |
| `src/semantic/analyzer.c` 的注解与声明收集 | 验证标注并在契约及函数体检查前完成根登记 |
| 同文件 `type_ref_is_exception_payload` | 提取共用载荷资格检查，分离声明处的泛型载荷资格与具体 catch 的闭合类型要求 |
| 同文件 `validate_type_declared_specs_and_satisfaction`、`validate_fit_declaration_contracts` | 建立根满足关系时先检查声明主体的载荷资格，再进行普通契约满足检查 |
| 同文件 `validate_spec_parent_spec_list` | 父列表引用异常根时，对当前子 spec 主体执行相同载荷资格检查并拒绝该关系 |
| 同文件 `generic_spec_constraint_satisfied` 等 | 复用已有普通 spec 约束与蕴含关系，不创建内建替代约束 |
| 同文件 `validate_throw_stmt` | 类型准入改为根契约证明，保留非载荷检查 |
| 同文件 try/catch 检查 | 接入根 catch，继续拒绝其他 spec catch 和泛参 catch |
| `src/symbol/` 的 export、FT、provider、imported-module | 传递根声明事实、关系与编译期登记摘要 |
| `src/codegen/codegen.c` 的 throw、spec 视角与泛型描述符路径 | 在 throw 端形成或复用完整根视图，保留 subject 的精确异常身份；按第 6 节完成承载布局与生命周期验证 |
| 同文件 `cg_emit_try_expr_catch_binding` 与 catch 分派发码 | 将具体绑定的载荷来源抽为表达式参数，传入根视图的 subject；根 catch 单独按普通 spec 值绑定 |
| `src/cli/lsp/`、`std/std/src/compiler/` | 在各自现有能力内同步注解展示、AST 与符号缓存；不扩建无关 Parser 能力 |

新增函数和结构体须有注释。诊断先按现有错误码规范检索、归类和 Review，再进入
代码；未知类型等前置错误不得产生依赖性重复诊断。

## 6 发码与异常传播：根视图承载与 catch 复用

### 6.1 已核实的基础

| 当前代码事实 | 对方案的含义 |
| --- | --- |
| `FengCatchClause.type` 为具体描述符或 NULL；`feng_region_matches_exception` 只比较描述符身份或匹配 NULL | 具体 catch 可继续精确匹配；所有合法抛出值受根约束后，根 catch 的匹配选择可使用已有兜底能力 |
| `FengUnwindException` 保存 `value`、`desc` 和匹配子句，不保存 spec witness | 新方案在 throw 端携带完整根视图，具体承载布局仍需确定 |
| object-form spec 值是 `{ subject, witness }` 聚合值；subject 是托管槽，witness 指向静态表 | 可沿用普通 spec 的复制与持有机制；该聚合值本身不是可直接交给 `feng_release` 的托管对象 |
| `cg_emit_throw` 已通过 `cg_emit_value_box_subject` 将值类型装入 `ValueBox<T>` | 值类型装箱是当前已有行为；根视图的 subject 可承载该箱体，不应重复装箱原始值 |
| `cg_emit_try_expr_catch_binding` 直接读取 `feng_caught_value()`，按具体引用类型或 ValueBox 提取绑定 | 将载荷来源抽成参数后，可对解出的 subject 复用原绑定逻辑 |
| `FengSpecCoercionDescriptor`、重化依赖和泛参描述符已支持共享体形成 spec 视角 | throw 端可复用现有根 witness 选择与视图形成机制；catch 端不需要按具体类型动态查找 witness |
| `feng_throw` 分配异常展开记录，异常清理对 `value` 执行 `feng_release` | 完整根视图必须具有跨栈展开的有效存储与持有关系；不能传入局部聚合值的地址 |
| 现有 spec 默认值生成隐藏 subject 与默认 witness | 默认根值也需纳入闭环，不能只覆盖用户手写的 type / fit |

代码依据：[运行时异常结构](../../src/runtime/feng_runtime.h)、
[异常匹配与清理](../../src/runtime/feng_exception.c)、
[spec 视角、throw 与 catch 发码](../../src/codegen/codegen.c)。

### 6.2 throw 携带完整根视图

本轮讨论收敛的发码模型是：**throw 端形成并传播完整的异常根 spec 视图；具体
catch 按原始载荷的精确身份匹配，再取 subject 绑定；根 catch 直接绑定完整视图。**
这里确定信息传递与处理分工，具体存储布局仍按第 6.4 节 Review。

1. throw 表达式只求值一次，按已有契约转换形成 `{ subject, witness }`。
   引用类型和 string 的 subject 为原托管对象；值类型按既有规则形成 `ValueBox<T>`
   作为 subject。已是根视图时复用该视图；合法交叉契约视角使用已有根投影。
2. 随根视图保留 subject 对应的精确异常描述符：引用载荷使用具体类型描述符，
   值载荷使用既有 ValueBox 的具体描述符。闭合泛型实例继续按完整实参区分，不能改用根
   spec 或外层承载对象的描述符匹配具体 catch。
3. 根 witness 在 throw 端已经选定，并随视图传递。其来源包括直接满足、可见 fit、
   泛型约束、合法交叉契约投影及已有根值；catch 端直接使用被传递的 witness。

建议在 throw 时将原始载荷的匹配描述符保存在现有异常记录的 `desc` 中，使
`feng_region_matches_exception` 继续执行精确描述符比较。无需为了具体 catch
的匹配，逐次解包视图或查询 subject 满足了哪些 spec。根 catch 与匿名 catch 的
匹配选择可使用既有 NULL 兜底；两者的绑定行为仍按第 2.4 节区分。

`.ft` 的根标记负责编译期识别；完整根视图负责传播中的契约成员调用信息。本模型
不要求增加运行时 spec 满足集合，也不要求从具体类型描述符反向搜索根 witness。

### 6.3 具体 catch 解出 subject 后复用绑定逻辑

描述符匹配只决定执行哪个 catch 子句，不提供其绑定值。具体 catch 匹配成功后，
仍须从异常载体取得根视图，再取出 subject，交给现有具体类型绑定逻辑。

`cg_emit_try_expr_catch_binding` 当前在内部直接使用 `feng_caught_value()`。
拟将“待绑定载荷表达式”抽为参数：调用方取得 subject 后传入该表达式，函数内部
继续按已确定的 catch 类型执行以下处理。

| catch 类型 | subject 表示 | 可复用的现有绑定逻辑 |
| --- | --- | --- |
| 引用类型、string | 原托管对象指针 | 按已匹配的具体类型形成绑定 |
| 标量、enum | `ValueBox<T>` 指针 | 从已知箱体布局读取 value |
| tuple、`@value type` | `ValueBox<T>` 指针 | 通过既有聚合赋值／复制路径取值并处理托管字段 |

原有绑定作用域、值复制与持有语义继续保留。具体 catch 的变化集中于载荷来源：
从直接使用异常 `value`，改为使用根视图中解出的 subject；不能把外层载体指针
直接作为具体对象或 `ValueBox<T>` 解释。

根 catch 绑定完整 `{ subject, witness }`，沿用普通 spec 值的复制与持有规则。
绑定保存到 catch 外时必须保持 subject 有效；异常载体清理不能使外部保存的根值
悬空。显式再次抛出该根值仍按第 6.2 节保留具体身份与 witness，匿名 `throw;`
继续复用正在处理的异常。

### 6.4 载体布局、装箱与实施边界

当前 spec 是两个指针组成的聚合值，异常 `value` 则由运行时按托管对象释放。
完整根视图需要具有跨栈展开的有效存储，不能直接把局部 spec 聚合值地址传给
`feng_throw`。形成根视图与为该视图安排跨栈存储，是需要分别处理的两个环节。

讨论过的一种候选布局如下，尚未选定或实施：

| 异常记录字段 | 候选含义 |
| --- | --- |
| `value` | 指向保存完整根视图的托管载体，由该载体持有 subject |
| `desc` | subject 对应的精确异常描述符，供具体 catch 匹配 |

该候选方向可复用现有描述符匹配与托管清理机制，具体 catch 按第 6.3 节解包，
根 catch 取完整视图。但 `value` 从原始载荷变为根视图载体，会改变 throw 与
catch 生成代码之间的载荷解释协议；保持 C 函数签名和结构体字段数量不变，不能
据此认定 ABI 不变。

成本评估必须以当前实现为基线：值类型现有的 `ValueBox<T>` 装箱及异常展开记录
分配已经存在，不能算作新方案新增成本。形成根视图时应复用既有装箱布局和描述符，
已有根视图的 subject 不再重复装箱。若另建独立托管根载体，则该载体的分配与持有
操作需要单独计入，并按引用载荷、值载荷分别与旧路径比较额外分配和持有次数。

沿用当前任务边界：未获人工新决策前，不修改 runtime，不变更 ABI，不引入额外
运行时查询、分配或分派成本。现有载荷构造所需的复制、持有和装箱继续按原值模型执行。

**已明确通过完整根视图传递 witness；尚未证明其承载布局与生命周期能在上述
ABI／成本边界内闭环。** 实施前须给出具体布局、所有权与成本的验证结果，涉及
边界变化时由人工决策，不能用未验证的动态映射或转换回调补洞。

本节记录的候选布局不授权 runtime、ABI 或额外运行成本变更。方案确认后至少先验证：
跨包 throw → 根 catch → 调用库声明的成员 → 保存到函数外 → 再抛出 → 具体 catch，
并覆盖共享泛型体、引用和值类型载荷、两类 catch 的绑定复用及嵌套异常清理。

## 7 `E: throw` 与现有代码的迁移

### 7.1 移除内建约束的专属部分

拟按当前讨论方向定向撤销 `E: throw`，普通 spec 泛型约束不是该特性的新增能力，
不随其撤销。具体执行及既有测试迁移清单需在 Review 后确认。

- 移除该专属约束语法、AST kind、语义分支、专属发码和 `.ft` 属性消费，同步
  dump、LSP、标准库 Parser 与规范；保留新方案仍需使用的通用值操作和描述符能力。
- 将有价值的泛型抛出、跨包、实际身份及所有权测试场景迁移到根 spec 方案，
  不通过删掉验证场景来完成回退。
- 原有不可抛载荷检查早于该功能存在：`359b9820` 已包含数组禁抛等规则，
  `a742bc20` 新增内建约束时复用了它们；这些载荷规则应按第 2 节保留和调整职责。
- `a742bc20` 同时包含标准库 `FengParser.nextToken` / `peekToken` 的 EOF 循环修复。
  该独立修复及相应验证必须保留，不能整笔撤销后遗漏。

历史交付文档 [throw 泛型约束方案](./feng-throw-generic-constraint-dev.md) 在实际迁移
完成后标注被本方案取代，保留历史验证事实，不提前宣称已回退。

### 7.2 标准库与调用方

当前 `std.Error` 是满足 `Display` 的具体 type；`FengCompileError` 也是具体 type。
标准库多处直接抛出 string，并有具体 string catch。新根规则落地后，这些类型必须
在对应可见范围建立合法根关系，或迁移为选定的异常类型，不能无条件沿用旧源码。

库层根声明、成员面与原有异常的适配策略由人工 Review。编译器不得为了兼容旧
用例而自动给类型添加满足关系、硬编码 std，或在缺少根时退回旧 throw 白名单。

统一根值应验证跨字段、容器和泛型返回的保存／再抛出能力。Future 可作为这种能力
的应用场景；本方案不顺带重设计 Future / Promise 的调度、状态机或公开 API。

## 8 待人工 Review 的决策

第 2 节的核心规则不在下表重新选择。以下是尚未确定的细节与实施前提：

| 编号 | 待决事项 | 建议或需要补齐的内容 |
| --- | --- | --- |
| R01 | 根 spec 的公开性及泛型参数 | 建议为公开模块中的公开、非泛型 object-form spec；若允许私有根须补充分发元信息，若允许泛型根须定义唯一根具体指哪个实例。此限制仅针对根声明，不限制 `Error<T>` 等满足者 |
| R02 | 注解参数、重复标注及根缺失 | 建议无实参，空括号等价；同一声明重复标注报错；没有根且没有依赖根的用法时允许编译，需要进行 throw 类型准入时明确诊断根缺失 |
| R03 | 根 catch 的顺序 | 建议与匿名 catch 一样作为最后的兜底；同一 try 不允许再出现第二个兜底或后续具体 catch；非载荷规则仍按第 2.3 节保持 |
| R04 | 完整根视图的承载布局与载荷协议 | 按第 6 节验证 throw 端携带视图、具体 catch 解出 subject、根 catch 绑定完整视图；确定实际存储、所有权及分配成本，验证跨包与重抛；如需改变 runtime / ABI / 性能边界，列出事实与代价供人工决定 |
| R05 | 库层契约与已有异常迁移 | 确定根所在库／模块、名称、成员及是否复用 Display；明确 string、std.Error、FengCompileError 和现有自定义载荷如何建立根关系 |
| R06 | 内建约束撤销、测试修改与制品重编 | 确认定向撤销和既有用例迁移清单，保留独立修复；建议 `.ft` 保持 2.0 并重编全部相关制品，不混用旧规则产物 |

## 9 实施 Todo

- [ ] Review 确认已收敛规则与第 8 节待决项，完成第 6 节发码闭环设计。
- [ ] 先更新异常、spec、泛型、符号表及诊断主规范；语言总览只补入口。
- [ ] 列出内建 `E: throw` 撤销清单、应保留的独立修复及既有用例修改清单，取得相应人工确认。
- [ ] 完成上述定向迁移准备，并保持原有载荷规则与普通 spec 泛型能力。
- [ ] 接入内建注解、根信息记录、源码及依赖登记，保证顺序无关和唯一性。
- [ ] 完成 `.ft` 两种 profile、Provider 摘要、导入声明、缓存比较与跨包关系恢复。
- [ ] 提取共用的合法载荷检查，分离泛型声明准入与具体 catch 的闭合类型要求。
- [ ] 将同一载荷检查接入 type / fit 的根关系及 spec 父列表验证，允许 `Error<T>`，拒绝子 spec 与非法载荷，不依赖 `@final`。
- [ ] 将 throw 类型准入统一为根契约证明，保留所有非载荷检查。
- [ ] 按第 6.4 节验证完整根视图的承载布局、载荷协议和成本，完成 R04 Review。
- [ ] 完成具体类型、开放具名泛型、受约束泛参及根视角的 throw 发码，传播完整根视图与原始载荷的匹配身份，复用已有值类型装箱。
- [ ] 将具体 catch 绑定的载荷来源抽为表达式参数，解出 subject 后复用引用绑定、ValueBox 解包及聚合复制逻辑。
- [ ] 完成根 catch 的普通 spec 值绑定，以及两类 catch 的所有权、清理、保存到外部与重抛闭环。
- [ ] 按 Review 结论完成库层根声明及现有异常适配，不扩大 Future API 范围。
- [ ] 补齐编译器测试与 FCTS；按第 10 节验证源码不可见的二进制包路径。
- [ ] 重编 provider、consumer 和缓存，在沙箱外运行 `make test` 全量回归。
- [ ] 对问题先记录、再分析、再修复；涉及语义、特殊处理、ABI 或运行开销的不确定项由人工决策。
- [ ] 回填实际结果、核对交付范围、收敛文档，给出英文 commit message，由开发者自行提交。

## 10 验证矩阵

`test/` 关注诊断、AST、符号事实和生成代码；`fcts/` 关注语言行为。未经批准不修改
既有用例。执行产物放在工程 `build/` 或 `temp/`，不在系统临时目录执行。

| 维度 | 必须覆盖 |
| --- | --- |
| 注解与根登记 | 正确目标、错误声明种类、其他 spec 形态、Review 确认后的参数／重复／公开性／泛型根规则 |
| 唯一性与顺序 | 同包及跨包两个根、源码与依赖冲突、未被显式引用模块中的根、文件／包加载顺序交换 |
| 身份与缓存 | 菱形依赖、同根重复引用、别名、同名不同模块；重新分析删除／移动标记，缓存覆盖不残留旧根 |
| 统一载荷准入 | 各类目标在声明满足根时统一检查合法载荷资格；非法目标在该处拒绝；throw 仅检查根证明，无根关系拒绝 |
| 满足关系入口 | type 声明头、有体／无体 fit、spec 父列表、内建类型 fit、固定与开放数组 fit、跨包可见及孤儿关系规则 |
| 子 spec 拒绝 | 单父／多父列表中的根、泛型子声明、成员完全满足仍拒绝、同包／跨包及别名、声明顺序；根未标注 `@final` 时同样拒绝 |
| 泛型满足者 | `Error<T>` 声明允许、`Error<int[]>` 字段不要求可抛、多层实例；类型级及方法／函数级共享体能抛出开放书写的 `Error<T>` 并保留实际闭合身份 |
| 共享泛型体 | 已知根约束与无关／无约束泛参、显式／推导实参、重载、转传、递归、闭包、方法值、fit／mixin 路径 |
| spec 泛型实参 | 根本身及合法交叉契约作为 `E: Throwable` 的实参；共享体保留完整视角并抛出底层载荷，不将实际类型误当作具体实体类型 |
| 根值形成与流动 | 具体值、合法交叉契约视角、默认根值；字段、返回、容器保存和再次抛出 |
| 完整根视图传播 | throw 端形成／复用根视图并保留 witness；直接抛出、泛型共享体抛出与根值重抛保持原始匹配身份，不以根或外层载体描述符替代 |
| 精确 catch | 不同具名类型、enum 与底层标量、布局相同的值类型、不同泛型实参、跨包同名类型不混淆；仍含开放泛参的 `Error<T>` catch 保持拒绝 |
| 具体 catch 绑定 | 从根视图取 subject，覆盖引用／string、标量／enum、tuple／`@value type`；复用原有绑定与聚合复制，不把外层载体当作原始值 |
| 根 catch | 绑定完整根视图，通过随视图传递的 witness 调用根成员，保留实际身份、绑定保存到作用域外；其他 spec 和泛参 catch 仍拒绝 |
| 非载荷规则 | void、表达式错误、defer、匿名重抛归属、嵌套 callable 边界、控制流、try 结果类型、C ABI 边界不变 |
| 生命周期 | 表达式单次求值、临时值转移、借用持有、聚合复制、ARC 精确清理、终结计数、嵌套 catch、匿名及具名再抛出 |
| `.ft` 与二进制包 | 两种 profile 往返、根属性 owner 与身份校验、传递依赖、私有源码不可见、泛型共享体及根成员调用；仅恢复根标记即可拒绝 consumer 的子 spec 声明 |
| 工具与迁移 | AST dump、LSP 展示和缓存形状、标准库 Parser EOF 修复、旧专属约束移除、库层适配及批准的用例迁移 |
| 装箱与成本基线 | 当前 ValueBox 与异常展开记录分配作为基线；形成／重抛根视图不重复装箱原值；单独统计候选根载体的分配与持有成本 |
| 性能与 ABI | 匹配描述符、载体／subject 解释、box 与 witness 的使用符合批准方案；评估生成代码间的载荷协议，无未经批准的新分配、动态查找、转接层或 ABI 变化 |
| 全量回归 | 相关定向验证后执行沙箱外 `make test`，记录环境、命令、结果和失败处理过程 |

## 11 当前核查与问题记录

| 编号 | 事实 | 处理状态 |
| --- | --- | --- |
| P01 | 现有载荷检查把开放类型引用与非法载荷类别合并拒绝 | 第 2.2、2.5 节明确允许具名泛型声明、保留具体 catch 闭合限制及接通实际描述符发码；尚未实施 |
| P02 | Provider 已读取全部公开表，但 Semantic 只接入被引用模块 | 第 3.3 节提出既有读取流程收集根摘要，避免漏检及额外全类型遍历 |
| P03 | spec 导入分支当前不恢复声明注解 | 第 4.1 节列入完整符号事实链路 |
| P04 | 异常传播记录未携带根 witness，现有具体 catch 内部直接读取 `feng_caught_value()` | 第 6.2、6.3 节明确 throw 端传递完整根视图、具体 catch 解出 subject 后复用绑定；承载布局与生命周期仍待 R04 Review |
| P05 | 标准库存在直接 string 异常及未声明根关系的具名异常 | 第 7.2 节记录迁移事实，库契约与适配策略待 R05 决定 |
| P06 | 内建约束提交附带独立 EOF 修复及相应测试 | 定向撤销时保留，尚未执行回退 |
| P07 | 当前 throw 已为值类型装箱，而 spec 视图本身是聚合值 | 第 6.4 节区分既有 ValueBox 与候选根载体，按现有基线评估新增分配与载荷协议变化；尚未实施 |

本次只更新 Review 文档；编译器、runtime、ABI、标准库及测试均未修改。尚未运行
新方案的用例或全量回归，不把历史 `E: throw` 的测试通过记录作为本方案的验证结果。
