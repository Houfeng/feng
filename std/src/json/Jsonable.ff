open module std.json;

/**
 * JSON 序列化
 */
open spec JsonSerializable {
  func toJson(): JsonObject;
}

/**
 * JSON 反序列化
 */
open spec JsonDeserializable {
  func fromJson(json: JsonObject): void;
}

open spec JsonableTest {
  func toJson(): JsonValue;
}