open module std.compiler.lexer;

/**
 * 词法单元的源代码位置
 */
@value
open type FengSourceLocation {
  /** 偏移量 */
  let offset: int;

  /** 词法单元所在的行号 */
  let line: int;

  /** 词法单元所在的列号 */
  let column: int;
}