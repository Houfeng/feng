open module std.compiler.lexer;

/**
 * 词法单元的源代码位置
 */
@value
open type FengLocation {
  /** 文件路径, 同文件引用同一个字符串实例 */
  let path: string;

  /** 偏移量（从 0 开始） */
  let offset: int;

  /** 词法单元所在的行号（从 1 开始） */
  let line: int;

  /** 词法单元所在的列号（从 1 开始） */
  let column: int;
}