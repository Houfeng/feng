module hello_world;

import std;
import std.io;
import std.text;
import std.numeric;
import std.time;
import std.collections;
import std.json;
import std.test;
import std.platform;

func test() {
  assert(true, "hello world");
}

func main(args: string[]) {
  // test("test2", test2);
  let obj = JsonObject();
  obj.set("message", "Hello, World!");
  obj.set("code", (double)200);
  obj.set("detail", none);
  println(Json.stringify(obj));
  println("{0}:{1}", SystemInfo.os(), SystemInfo.arch());
}
