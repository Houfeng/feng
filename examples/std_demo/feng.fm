# 当前包基础信息
[package]
name:    "std_demo"
version: "0.1.0"
target:  "bin"
src:     "src/"
out:     "build/"

# 当前包的依赖项，格式为：依赖包名称: 版本号或本地路径
# 本地路径必须以 ./ 或 ../ 开头，
# 本地路径可以是 .fb 文件，也可以是 .fm 文件或包含 .fm 文件的目录
# 如果是使用版本号，必须使用固定版本号，不能使用范围或通配符
[dependencies]
std: "../../std/std"
