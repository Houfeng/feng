module feng.examples;

import std;


/**
 * The main function.
 */
func main(args: string[]) {
  print("Please enter your name: ");
  let msg = readLine();
  for var i = 0; i < 10; i += 1 {
    println("Hello: {0}", msg);
  }
}
