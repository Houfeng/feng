mod test;

spec CommitOptions {
  var message: i32;
}

type User {
  fn commit(options: CommitOptions): void {
    options.message = 1;
  }

  fn commit(message: i32): int {
    
  }
}

// 主函数
fn main(args: string[]) {
  let user = User();
  user.commit(1);
}