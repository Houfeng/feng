mod test;

spec CommitOptions {
  var message: i32;
}

type User {
  fn commit(options: CommitOptions): void {
    options.message = 1;
  }
}

// 主函数
fn main() {
  let user = User();
  user.commit(CommitOptions());
}